grep without -F treats the pattern as a POSIX basic regular expression. * is the repetition operator with no valid operand, so BSD/macOS grep rejects the pattern outright: grep: repetition-operator operand invalid, exits with status 2 (error), not 0 or 1. The grep never ran at all -- it is dead code. Confirmed on the actual files. set -e does not rescue this. Fix: grep -qF. Applied and verified both directions: planted false check present gives harness FAILED exit 1; check corrected gives grep exit 1 cleanly and the harness passes.
