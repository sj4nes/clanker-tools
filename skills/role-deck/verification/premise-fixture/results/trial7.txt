The marker grep pattern is unescaped and is being interpreted as a regex, not literal text. * is a repetition operator in a basic regular expression with no operand to repeat. Both greps on this box reject the pattern: system grep says repetition-operator operand invalid; the grep actually on PATH (ugrep) says error at position 4, empty (sub)expression. Either way grep exits 2 (error). set -e does not help. Confirmed by running it. Fix: grep -qF '*** FAIL'. Both return 0 on the planted failure -- verified.
