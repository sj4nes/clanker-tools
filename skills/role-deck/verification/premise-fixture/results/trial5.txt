The marker is passed as a regex, not a literal string. The second * is a repetition operator with no valid operand. BSD/macOS grep rejects the whole pattern: grep: repetition-operator operand invalid, and exits with status 2 (error), not 0 or 1. A shell if treats any non-zero status as false. Confirmed by running it. set -e does not help. The fallback banner check is not a backstop. Fix: grep -qF '*** FAIL'. Verified: the harness prints harness: FAILED and exits 1. I made no edits to the files.
