# Condition placement

Delete the temporary files if the backup completes.
Deploy the patch when all required tests pass.

If the backup completes, delete the temporary files.
When all required tests pass, deploy the patch.
