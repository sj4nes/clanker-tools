# Task

If the backup completes, delete the temporary files.
If the backup fails, keep the files and report the failure to the on-call engineer.
Send the release summary to the engineering channel.
Return the build ID and the deployment status.

```sh
# the token must be rotated by the service -- this passive line is inside code
```
