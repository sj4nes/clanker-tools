# Counterexample index (generated from results/*.yaml)

`dropped hypothesis`  ->  `witnessing counterexample`  (from which result)

- **bolzano_weierstrass** — bounded_sequence: 'x_n = n: |x_m - x_n| >= 1 for m != n, no Cauchy subsequence'
- **bolzano_weierstrass** — completeness_of_ambient_field: 'in Q: digit truncations of sqrt(2) — bounded, every subsequence Cauchy, none converges in Q'
- **bolzano_weierstrass** — finite_dimension: 'in l^2 (out of scope): the unit vectors e_n are bounded, pairwise distance sqrt(2), no convergent subsequence'
- **cauchy_convergence_criterion** — completeness_of_ambient_field: 'in Q: (1, 1.4, 1.41, 1.414, ...) is Cauchy and does not converge. This is the defining incompleteness of Q.'
- **extreme_value_theorem** — closed_interval: 'f(x) = x on (0,1): neither sup nor inf attained'
- **extreme_value_theorem** — bounded_interval: 'f(x) = arctan x on R: sup = pi/2, never attained'
- **extreme_value_theorem** — continuity_on_set: 'f(x) = x - floor(x) on [0,1] with f(1)=0: sup = 1, never attained'
- **ftc_part1** — f_integrable_on_interval: 'if f is not integrable, F is not even defined'
- **ftc_part2** — f_integrable_on_interval: 'Volterra''s function has a bounded derivative that is not Riemann integrable (out of scope): the RHS is defined, the LHS is not'
- **heine_borel** — closed_set: '(0,1): cover {(1/n,1)}_{n>=2} has no finite subcover'
- **heine_borel** — bounded_set: '[0,inf): cover {(-1,n)}_{n>=1} has no finite subcover'
- **intermediate_value_theorem** — continuity_on_set: 'step function 0 on [1,1.5), 1 on [1.5,2] never takes 0.5'
- **intermediate_value_theorem** — connected_domain: 'f(x) = 1/x on [-1,0) U (0,1] takes -1 and 1 but never 0'
- **lub_axiom** — bounded_above: 'S = N has no upper bound, hence no sup'
- **lub_axiom** — nonempty_set: 'sup of the empty set is left undefined in 0.1 (conventions.md)'
- **mean_value_theorem** — continuity_on_closed_interval: 'f(x)=x on (0,1], f(0):=1 — no c with f''(c) = (f(1)-f(0))/1 = 0'
- **mean_value_theorem** — differentiability_on_open_interval: 'f(x) = |x| on [-1,1] — secant slope 0, f'' = +-1 wherever defined'
- **monotone_convergence_theorem** — bounded_sequence: 'x_n = n (increasing, diverges)'
- **monotone_convergence_theorem** — monotone_sequence: 'x_n = (-1)^n (bounded, diverges)'
- **monotone_convergence_theorem** — completeness_of_ambient_field: 'the sqrt(2) recursion in Q: monotone, bounded, Cauchy, no rational limit'
- **riemann_integral** — bounded_function: 'f(x) = 1/sqrt(x) on (0,1]: unbounded, U(f,P) = +inf for every P — not Riemann integrable (improper integral, out of scope)'
- **riemann_integral** — compact_interval: 'the theory is stated on [a,b]; non-compact domains need improper integrals'
- **riemann_integral** — integrability_itself: 'Dirichlet 1_Q on [0,1]: L(f,P) = 0, U(f,P) = 1 for every P — sup_P L != inf_P U, not integrable'
- **taylor_theorem** — f_n_times_differentiable_on_interval: 'f(x) = x^2 sin(1/x), f(0)=0: differentiable once, f'''' undefined at 0 — no 2nd-order Lagrange remainder there'
- **taylor_theorem** — smoothness_vs_analyticity: 'f(x)=e^{-1/x^2} (f(0)=0) is C^infinity with all f^{(k)}(0)=0, so the Taylor SERIES is 0 though f is not — Taylor''s theorem (finite, with remainder) still holds; the infinite series need not converge to f'
- **uniform_convergence** — uniformity_itself: 'x^n on [0,1] -> pointwise to a discontinuous limit; sup_x|x^n - f| = 1 for all n (instance-checks.bc). Pointwise but NOT uniform.'
- **uniform_convergence** — bounded_difference_for_sup_as_real: 'f_n(x) = x/n on R: f_n -> 0 pointwise, sup_x |x/n| = +inf for every n — not uniform on R (uniform on every bounded set).'
- **weierstrass_m_test** — summable_majorant: 'if sum M_n = inf the test says nothing; e.g. M_n = 1/n'
- **weierstrass_m_test** — sufficiency_not_necessity: 'f_n(x) = (-1)^n x^n / n on [0,1]: sup|f_n| = 1/n, sum 1/n = inf so the M-test fails, yet sum f_n converges uniformly (Abel/Dirichlet). A failed M-test is NOT a proof of non-uniform convergence.'

