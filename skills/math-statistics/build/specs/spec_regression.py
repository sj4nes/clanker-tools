"""Linear regression: Gaussian noise, fixed design."""
from nodespec import N

N("linear_model", "structure", "regression",
  "The Gaussian linear model is y = X beta + eps, where y in R^n is the response, X in R^{n x p} is a FIXED design matrix of full column rank p < n, beta in R^p is the unknown coefficient vector, and eps ~ N(0, sigma^2 I_n).",
  parseable="y = X beta + eps,   X fixed, rank(X) = p < n,   eps ~ N_n(0, sigma^2 I)",
  symbols={"X": "the design / model matrix, type: fixed real n x p matrix, rank p",
           "beta": "the coefficient vector, type: point of R^p (the parameter)",
           "eps": "the error vector, type: N(0, sigma^2 I_n) -- iid, mean 0, common variance, uncorrelated",
           "sigma^2": "the error variance (a second parameter)"},
  tcn="A structure. Key modelling assumptions, each a potential failure point: (1) linearity of E[y|X] in beta; (2) X fixed / exogenous (or inference is conditional on X); (3) full rank (no exact collinearity); (4) errors mean-zero, (5) homoskedastic, (6) uncorrelated, (7) Gaussian (needed only for EXACT small-sample inference, not for OLS's Gauss-Markov optimality). Uses linear_algebra_background throughout.",
  spec=["simple linear regression: p = 2, X columns (1, x_i)",
        "polynomial / spline / dummy-variable regression: still linear IN beta, X holds the transformed features",
        "one-way ANOVA is the linear model with X a matrix of group indicators"],
  cxd={"full_column_rank": "exact collinearity (a redundant dummy, x_2 = 2 x_1): X^T X is singular, beta is not identified, OLS has no unique solution",
       "errors_uncorrelated_homoskedastic": "heteroskedasticity or autocorrelation: OLS stays unbiased but is no longer BLUE and the usual SEs are wrong (need GLS or robust SEs)",
       "gaussian_errors": "only needed for EXACT t / F inference; drop it and the t/F statistics are asymptotically valid by the CLT (if n - p is large and the design is not too leveraged)"},
  misuse=["reading regression coefficients as causal effects without a design or identification argument",
          "trusting exact t/F p-values with small n and clearly non-normal, heteroskedastic residuals",
          "extrapolating the fitted line outside the range of X"],
  apps=["the most widely used statistical model in existence -- econometrics, epidemiology, psychometrics, quantitative finance, A/B test analysis with covariate adjustment",
        "the computational and conceptual template for GLMs, mixed models, and much of supervised learning"],
  related={"required_by": ["ordinary_least_squares", "gauss_markov_theorem", "ols_distribution_under_normal_errors"],
           "uses": ["prob_normal", "linear_algebra_background"], "generalizes_from": ["one_way_anova_identity"]},
  sources=["rao_linear_statistical_inference", "seber_lee_linear_regression", "weisberg_applied_linear_regression"])

N("ordinary_least_squares", "definition", "regression",
  "The ordinary least squares estimator is beta_hat = argmin_{b in R^p} || y - X b ||^2: the coefficient vector minimizing the residual sum of squares.",
  parseable="beta_hat = argmin_{b} || y - X b ||^2",
  symbols={"|| y - X b ||^2 = sum_i (y_i - x_i^T b)^2": "the least-squares criterion",
           "beta_hat = (X^T X)^{-1} X^T y": "the closed-form solution (normal_equations)"},
  tcn="A definition via an optimization problem. The objective is strictly convex in b when X has full column rank, so the minimizer is unique and given by the normal equations. OLS = MLE under Gaussian errors; OLS = method of moments for the moment condition E[X eps] = 0.",
  spec=["p = 1, no intercept: beta_hat = sum x_i y_i / sum x_i^2",
        "simple regression: slope = Cov_hat(x, y)/Var_hat(x), intercept = ybar - slope xbar",
        "OLS = MLE of beta under eps ~ N(0, sigma^2 I) (maximizing the Gaussian likelihood minimizes the SSE)"],
  cxd={"full_rank_X": "collinear X: the argmin is a whole affine subspace; OLS is undefined (ridge / pseudoinverse picks the minimum-norm solution)"},
  misuse=["fitting OLS with more predictors than observations (p > n) -- no unique solution, need regularization",
          "including a variable and its exact linear function (dummy-variable trap)",
          "interpreting a coefficient's sign/magnitude without noting it is 'holding the other X fixed', which may be impossible in the data"],
  related={"required_by": ["normal_equations", "gauss_markov_theorem", "partitioned_regression"],
           "uses": ["linear_model"], "special_case_of": []},
  sources=["seber_lee_linear_regression", "casella_berger_2e"])

N("normal_equations", "identity", "regression",
  "The OLS minimizer satisfies the normal equations X^T X beta_hat = X^T y; with X of full column rank, beta_hat = (X^T X)^{-1} X^T y.",
  parseable="X^T X beta_hat = X^T y;    beta_hat = (X^T X)^{-1} X^T y",
  symbols={"X^T X": "the p x p Gram matrix, symmetric positive definite when rank(X) = p",
           "X^T y": "the p-vector of predictor-response inner products",
           "X^T (y - X beta_hat) = 0": "equivalently: the residual vector is orthogonal to every column of X"},
  tcn="The stationarity condition for the least-squares objective: grad_b || y - X b ||^2 = -2 X^T (y - X b) = 0. An exact algebraic identity. Numerically one solves it via QR or Cholesky of X, not by forming (X^T X)^{-1}.",
  status_label="mathematical_identity",
  hyps=[],
  proof=("set the gradient of || y - X b ||^2 to zero: -2 X^T (y - X b) = 0",
         "linear_algebra_background", "instance",
         "validation/proof-checks.lean Stat.normal_equations_stationary -- grad || y - X b ||^2 = -2 X^T(y - Xb), checked for a fixed 2x2 design via decide on integer matrices"),
  regime="exact",
  spec=["orthogonal design (X^T X diagonal): beta_hat_j = (x_j^T y)/(x_j^T x_j) -- each coefficient is a separate simple regression",
        "adding a column orthogonal to the existing ones does not change the existing coefficients (Frisch-Waugh, partitioned_regression)",
        "centered predictors: the intercept decouples as ybar"],
  cxd={"X_full_rank": "singular X^T X (collinearity): the normal equations have infinitely many solutions; the fitted values X beta_hat are still unique (the projection), but the coefficients are not"},
  misuse=["forming and inverting X^T X explicitly for an ill-conditioned design -- squares the condition number; use QR",
          "reading a near-singular (X^T X)^{-1}'s huge diagonal entries as 'the data' rather than as collinearity"],
  related={"required_by": ["ols_is_projection", "ols_distribution_under_normal_errors"], "uses": ["ordinary_least_squares", "linear_algebra_background"]},
  sources=["seber_lee_linear_regression", "golub_van_loan_matrix_computations"])

N("ols_is_projection", "theorem", "regression",
  "The OLS fitted values are the orthogonal projection of y onto the column space of X: y_hat = X beta_hat = H y, where H = X (X^T X)^{-1} X^T is the hat matrix -- symmetric, idempotent (H^2 = H), with rank(H) = trace(H) = p. The residual vector e = (I - H) y is orthogonal to col(X).",
  parseable="y_hat = H y,   H = X(X^T X)^{-1} X^T,   H = H^T = H^2,   rank(H) = p",
  symbols={"H": "the 'hat' matrix (it puts the hat on y), an orthogonal projector onto col(X)",
           "I - H": "the residual-maker / annihilator matrix, projector onto col(X)^perp, rank n - p",
           "h_ii": "the leverage of observation i -- its influence on its own fitted value; sum h_ii = p"},
  tcn="A geometric statement: least squares IS orthogonal projection. Uses linear_algebra_background (projection matrices, idempotency, rank = trace for idempotents). H and I - H being complementary orthogonal projectors of ranks p and n - p is what drives every distributional result below (via Cochran).",
  regime="exact",
  hyps=[],
  proof=("H = X(X^T X)^{-1} X^T: check H^T = H and H^2 = X(X^T X)^{-1}(X^T X)(X^T X)^{-1} X^T = H; rank = trace = tr((X^T X)^{-1} X^T X) = tr(I_p) = p",
         "linear_algebra_background", "instance",
         "validation/proof-checks.lean Stat.hat_matrix_idempotent -- H^2 = H and tr(H) = p for a fixed integer 3x2 design via decide"),
  spec=["one predictor = the constant: H = (1/n) J, y_hat_i = ybar for all i -- 'regression on an intercept' is just the mean",
        "leverage h_ii in [1/n, 1]; h_ii > 2p/n flags a high-leverage point",
        "the Pythagorean decomposition ||y||^2 = ||Hy||^2 + ||(I-H)y||^2 is the ANOVA / R^2 identity"],
  cxd={"X_full_rank": "rank-deficient X: H is still the (unique) projector onto col(X), of rank rank(X) < p; beta_hat is non-unique but y_hat = Hy is unique"},
  misuse=["interpreting a coefficient without checking leverage / influence diagnostics (a single high-leverage point can determine beta_hat)",
          "forgetting sum of leverages = p when judging whether a design spreads influence well"],
  related={"required_by": ["residual_sum_of_squares", "partitioned_regression"], "uses": ["normal_equations", "linear_algebra_background"]},
  sources=["seber_lee_linear_regression", "hoaglin_welsch_1978"])

N("residual_sum_of_squares", "definition", "regression",
  "The residual sum of squares is RSS = || y - X beta_hat ||^2 = y^T (I - H) y = sum_i e_i^2, where e = (I - H) y are the OLS residuals.",
  parseable="RSS = || y - X beta_hat ||^2 = y^T (I - H) y",
  symbols={"e_i = y_i - x_i^T beta_hat": "the i-th residual (an estimate of eps_i, but with variance sigma^2(1 - h_ii) < sigma^2)",
           "RSS/sigma^2 ~ chi^2_{n-p}": "under the Gaussian linear model"},
  tcn="A statistic. RSS is a quadratic form in y with the idempotent matrix I - H of rank n - p; under normal errors RSS/sigma^2 ~ chi^2_{n-p}, independent of beta_hat (Cochran), which is the engine of the t- and F-tests.",
  regime="exact",
  hyps=[],
  proof=("RSS/sigma^2 = (eps/sigma)^T (I - H) (eps/sigma) with (I - H) idempotent of rank n - p; Cochran => chi^2_{n-p}",
         "cochran_theorem", "cited", None),
  spec=["RSS is nonincreasing as predictors are added (more columns can only reduce the projection residual) -- why RSS alone cannot select a model",
        "RSS/(n-p) = sigma_hat^2, the unbiased error-variance estimate",
        "the F-test compares RSS of nested models: (RSS_0 - RSS)/(RSS/(n-p))"],
  cxd={"gaussian_errors_for_the_chi2_law": "non-normal errors: E[RSS] = (n-p) sigma^2 still holds (it is a trace identity), but RSS/sigma^2 is not chi^2_{n-p}",
       "correct_mean_model": "if E[y] is not X beta (omitted nonlinearity) then E[RSS] > (n-p) sigma^2 -- RSS is inflated by the model bias (the basis of the lack-of-fit F-test)"},
  misuse=["comparing RSS across models with different numbers of parameters without a penalty (use adjusted R^2, AIC, or the F-test)",
          "using raw residuals e_i for diagnostics without standardizing by sqrt(1 - h_ii)"],
  related={"required_by": ["unbiased_error_variance_estimator", "overall_f_test", "r_squared"], "uses": ["ols_is_projection"]},
  sources=["seber_lee_linear_regression", "casella_berger_2e"])

N("unbiased_error_variance_estimator", "proposition", "regression",
  "In the linear model, sigma_hat^2 = RSS / (n - p) is unbiased for sigma^2: E[sigma_hat^2] = sigma^2, for any error distribution with mean 0, variance sigma^2, and uncorrelated components (no normality needed).",
  parseable="sigma_hat^2 = RSS / (n - p),   E[sigma_hat^2] = sigma^2",
  symbols={"n - p": "the residual / error degrees of freedom -- observations minus fitted parameters",
           "the MLE (Gaussian) divides by n": "and is biased low by a factor (n-p)/n"},
  tcn="An exact identity, distribution-free (only E[eps]=0, Cov(eps) = sigma^2 I needed). Proof: E[RSS] = E[eps^T (I - H) eps] = sigma^2 tr(I - H) = sigma^2 (n - p). The regression generalization of bias_of_sample_variance (which is the p = 1, X = 1 case, giving n - 1).",
  regime="exact",
  hyps=[],
  proof=("E[eps^T A eps] = sigma^2 tr(A) for Cov(eps) = sigma^2 I; here A = I - H, tr(I - H) = n - p",
         "prob_expectation_linearity", "instance",
         "validation/proof-checks.lean Stat.rss_expectation -- E[eps^T(I-H)eps] = sigma^2 tr(I-H); the trace identity tr(I - H) = n - p for a fixed integer design via decide, reusing Stat.bias_sample_var's E[quadratic form] algebra"),
  spec=["p = 1 (intercept only): n - p = n - 1, recovers S^2 (bias_of_sample_variance)",
        "sigma_hat = sqrt(sigma_hat^2) is the 'residual standard error' printed in every regression summary",
        "the SEs of the coefficients are sigma_hat sqrt( ((X^T X)^{-1})_jj )"],
  cxd={"Cov(eps) = sigma^2 I": "heteroskedastic or autocorrelated errors: E[RSS/(n-p)] != sigma^2 (there is no single sigma^2); the model-based SEs are wrong -- use White / Newey-West robust SEs",
       "correct_mean_model": "omitted nonlinearity inflates RSS, so sigma_hat^2 is biased UP -- the lack-of-fit test exploits this"},
  misuse=["using the divisor n (the MLE) and reporting biased-low SEs",
          "trusting sigma_hat^2 and the coefficient SEs when residual plots show non-constant variance"],
  related={"uses": ["residual_sum_of_squares", "prob_expectation_linearity", "linear_algebra_background"],
           "generalizes_from": ["bias_of_sample_variance"]},
  sources=["seber_lee_linear_regression", "casella_berger_2e"])

N("gauss_markov_theorem", "theorem", "regression",
  "In the linear model with E[eps] = 0 and Cov(eps) = sigma^2 I (NO normality, NO independence beyond uncorrelatedness), the OLS estimator c^T beta_hat is the best linear unbiased estimator (BLUE) of c^T beta for every c: among all estimators a^T y that are linear in y and unbiased for c^T beta, OLS has the smallest variance.",
  parseable="E[eps]=0, Cov(eps) = sigma^2 I   =>   for every c and every linear unbiased a^T y of c^T beta:   Var(c^T beta_hat) <= Var(a^T y)",
  symbols={"BLUE": "Best (min variance) Linear Unbiased Estimator",
           "'linear'": "linear in the response y (a^T y for a fixed vector a)",
           "the competitor": "any a^T y with E[a^T y] = c^T beta for all beta"},
  tcn="An exact, finite-sample optimality result within the linear-unbiased class, requiring only first and second moments of eps. Proof: write any linear unbiased estimator as c^T beta_hat + d^T y with d^T X = 0 (unbiasedness); then Var = Var(c^T beta_hat) + sigma^2 ||d||^2 + 2 sigma^2 c^T (X^T X)^{-1} X^T d, and the cross term is 0 because X^T d = 0; so Var = Var(c^T beta_hat) + sigma^2 ||d||^2 >= Var(c^T beta_hat).",
  regime="exact",
  hyps=["unbiased_estimator"],
  proof=("decompose a competing linear unbiased estimator as OLS plus d^T y with X^T d = 0; the variance is Var(OLS) + sigma^2 ||d||^2, cross term killed by X^T d = 0",
         "linear_algebra_background", "instance",
         "validation/proof-checks.lean Stat.gauss_markov_cross_term -- the cross term c^T (X^T X)^{-1} X^T d = 0 given X^T d = 0, and Var = Var_OLS + sigma^2 ||d||^2 >= Var_OLS, for a fixed integer design via decide"),
  spec=["OLS is BLUE even for heavy-tailed (finite-variance) errors -- normality is NOT needed for this",
        "with normal errors, OLS is additionally the MVUE among ALL unbiased estimators (not just linear), and is the MLE",
        "if Cov(eps) = sigma^2 V (known V != I): GLS beta_hat_GLS = (X^T V^{-1} X)^{-1} X^T V^{-1} y is BLUE, and OLS is no longer efficient"],
  cxd={"Cov(eps) = sigma^2 I": "heteroskedasticity (Cov(eps) = diag(sigma_i^2)): OLS is still unbiased but NOT BLUE -- weighted least squares with weights 1/sigma_i^2 beats it; and OLS's usual SEs are wrong",
       "autocorrelated errors (time series)": "OLS inefficient; GLS / Cochrane-Orcutt or Newey-West SEs needed",
       "linearity_and_correct_mean": "if E[y] != X beta, OLS is biased and 'BLUE' is moot"},
  misuse=["citing Gauss-Markov to claim OLS is 'optimal' when errors are heteroskedastic or correlated -- it is not, and the theorem's hypotheses are exactly what fails",
          "believing Gauss-Markov requires normal errors (it does not) or delivers optimality among all (not just linear) estimators (it does not, without normality)"],
  apps=["the theoretical warrant for defaulting to OLS in applied regression -- and, by its failure cases, the motivation for WLS, GLS, FGLS, and robust standard errors throughout econometrics"],
  related={"uses": ["ordinary_least_squares", "linear_algebra_background", "prob_covariance"],
           "strengthened_by": ["ols_distribution_under_normal_errors"]},
  sources=["rao_linear_statistical_inference", "seber_lee_linear_regression"])

N("ols_distribution_under_normal_errors", "theorem", "regression",
  "In the linear model with eps ~ N(0, sigma^2 I): beta_hat ~ N(beta, sigma^2 (X^T X)^{-1}), RSS/sigma^2 ~ chi^2_{n-p}, and beta_hat is independent of RSS. Hence every coefficient contrast is exactly normal and studentizes to an exact t.",
  parseable="eps ~ N(0, sigma^2 I)   =>   beta_hat ~ N(beta, sigma^2 (X^T X)^{-1}),   RSS/sigma^2 ~ chi^2_{n-p},   beta_hat _||_ RSS",
  symbols={"sigma^2 (X^T X)^{-1}": "the exact covariance of beta_hat; its j-th diagonal is Var(beta_hat_j)",
           "beta_hat _||_ RSS": "the multivariate analogue of Xbar _||_ S^2, from H and I - H being orthogonal projectors"},
  tcn="An exact finite-sample distributional result. beta_hat = (X^T X)^{-1} X^T y is a fixed linear map of the Gaussian y, hence Gaussian; RSS is a quadratic form in (I - H) y; H y (which determines beta_hat) and (I - H) y are orthogonal linear images of a spherical Gaussian, hence independent (Cochran).",
  regime="exact",
  hyps=[],
  proof=("beta_hat linear-in-Gaussian => Gaussian with the stated moments; (Hy, (I-H)y) orthogonal projections of N(mu, sigma^2 I) => independent, and (I-H)y-part gives RSS/sigma^2 ~ chi^2_{n-p} (Cochran)",
         "cochran_theorem", "cited",
         "the independence-of-orthogonal-Gaussian-projections step is proof-checks.lean Stat.gaussian_orthogonal_independent, reused from the normal_sample_mean_variance_independence node"),
  spec=["p = 1, X = 1: beta_hat = Xbar ~ N(mu, sigma^2/n), RSS/sigma^2 ~ chi^2_{n-1} -- exactly the one-sample normal results",
        "the coefficient t-statistic (beta_hat_j - beta_j)/(sigma_hat sqrt((X^TX)^{-1}_jj)) ~ t_{n-p} exactly (coefficient_t_test)",
        "a linear-hypothesis F-statistic ~ F_{q, n-p} exactly (overall_f_test)"],
  cxd={"gaussian_errors": "non-normal errors: beta_hat is only ASYMPTOTICALLY normal (CLT for (X^T X)^{-1} X^T eps, valid if no single row of X dominates -- max leverage h_ii -> 0); RSS/sigma^2 is not chi^2; the t/F p-values are approximate",
       "homoskedastic_uncorrelated": "Cov(eps) = sigma^2 V != sigma^2 I: beta_hat ~ N(beta, sigma^2 (X^TX)^{-1} X^T V X (X^TX)^{-1}) -- the 'sandwich' covariance; the naive (X^TX)^{-1} SEs are wrong"},
  misuse=["trusting exact t/F inference with small n and heavy-tailed or heteroskedastic residuals",
          "using the model-based covariance when a few points have very high leverage (the CLT for beta_hat then converges slowly)"],
  related={"uses": ["normal_equations", "cochran_theorem", "prob_normal_affine_closure"],
           "required_by": ["coefficient_t_test", "overall_f_test"], "strengthens": ["gauss_markov_theorem"]},
  sources=["seber_lee_linear_regression", "rao_linear_statistical_inference"])

N("coefficient_t_test", "proposition", "regression",
  "In the Gaussian linear model, (beta_hat_j - beta_j^0) / se(beta_hat_j) ~ t_{n-p} exactly, where se(beta_hat_j) = sigma_hat sqrt( ((X^T X)^{-1})_jj ); the test of H0: beta_j = 0 rejects when |t_j| > t_{n-p, 1-alpha/2}, and Xbar +- t_{n-p,1-alpha/2} se is an exact CI for beta_j.",
  parseable="t_j = (beta_hat_j - beta_j^0) / se(beta_hat_j)  ~  t_{n-p}   under H0",
  symbols={"se(beta_hat_j) = sigma_hat sqrt((X^T X)^{-1}_jj)": "the reported standard error",
           "t_{n-p}": "n - p residual degrees of freedom"},
  tcn="Exact under the Gaussian linear model. The numerator ~ N(0, sigma^2 (X^TX)^{-1}_jj) is independent of sigma_hat^2, and (n-p)sigma_hat^2/sigma^2 ~ chi^2_{n-p}, so the ratio is exactly t_{n-p} (assemble the t definition).",
  regime="exact",
  hyps=[],
  proof=("ols_distribution_under_normal_errors supplies numerator normality, denominator chi^2_{n-p}, and their independence; assemble t_{n-p}",
         "t_statistic_distribution", "core", None),
  spec=["the 't value' and 'Pr(>|t|)' columns of every regression summary table",
        "one predictor of interest, others as controls: t_j tests the PARTIAL effect (holding the other X fixed) -- Frisch-Waugh (partitioned_regression)",
        "as n - p grows, t_{n-p} -> N(0,1) and the t-test approaches the Wald z-test"],
  cxd={"gaussian_errors": "with non-normal errors the exact t_{n-p} calibration is lost; for large n - p and bounded leverage it is asymptotically valid (t -> N(0,1) via the CLT for beta_hat)",
       "homoskedasticity": "heteroskedastic errors make se(beta_hat_j) wrong -- use HC (White) robust SEs; the point estimate beta_hat_j stays unbiased",
       "no_collinearity": "near-collinear predictors inflate ((X^TX)^{-1})_jj (the variance inflation factor), giving large SEs and unstable, individually-non-significant coefficients even when the group is jointly significant"},
  misuse=["reading a non-significant coefficient as 'no effect' when collinearity has inflated its SE (check the joint F-test and the VIFs)",
          "interpreting beta_hat_j in isolation when the other predictors would necessarily change with x_j",
          "multiple coefficient tests without multiplicity adjustment"],
  apps=["the near-universal way effects are judged 'significant' in observational research and covariate-adjusted experiments"],
  related={"uses": ["ols_distribution_under_normal_errors", "students_t_distribution", "unbiased_error_variance_estimator"],
           "special_case_of": ["overall_f_test"]},
  sources=["seber_lee_linear_regression", "weisberg_applied_linear_regression"])

N("overall_f_test", "proposition", "regression",
  "To test H0: the reduced model (q linear restrictions R beta = r) against the full model, the statistic F = [ (RSS_0 - RSS) / q ] / [ RSS / (n - p) ] ~ F_{q, n-p} exactly under the Gaussian linear model and H0. The 'overall F' is the special case H0: all non-intercept coefficients are 0.",
  parseable="F = [ (RSS_0 - RSS) / q ] / [ RSS / (n - p) ]  ~  F_{q, n-p}   under H0",
  symbols={"RSS_0": "residual SS of the reduced (restricted) model", "RSS": "residual SS of the full model",
           "q": "number of restrictions (numerator df)", "n - p": "full-model error df (denominator df)"},
  tcn="Exact under normality. RSS_0 - RSS and RSS are, after dividing by sigma^2, independent chi^2_q and chi^2_{n-p} (Cochran: the restriction subspace and the full residual space are orthogonal), so their scaled ratio is F_{q, n-p}. It is the LRT for the linear restriction (special_case_of likelihood_ratio_test).",
  regime="exact",
  hyps=[],
  proof=("Cochran decomposition: chi^2_q (extra SS explained by relaxing the restriction) _||_ chi^2_{n-p} (full residual); ratio of independent scaled chi-squares is F",
         "cochran_theorem", "cited", None),
  spec=["q = 1: F_{1, n-p} = (t_{n-p})^2 -- the F-test of one coefficient is the square of its t-test",
        "overall F (all slopes 0): F = [ (TSS - RSS)/(p-1) ] / [ RSS/(n-p) ] = [R^2/(p-1)] / [(1-R^2)/(n-p)]",
        "nested model comparison / analysis of variance / ANCOVA all use this statistic"],
  cxd={"gaussian_errors": "non-normal errors: F is only asymptotically q^{-1} chi^2_q (i.e. approximately F); robust or bootstrap versions exist",
       "the_restriction_is_linear_and_pre-specified": "a data-driven restriction (testing the predictors that looked promising) invalidates the F null distribution -- selective inference",
       "homoskedasticity": "heteroskedasticity distorts the F null distribution; use a heteroskedasticity-robust Wald test of R beta = r"},
  misuse=["reading a significant overall F as 'the model is good' -- it only says at least one predictor has non-zero coefficient; R^2 and residual diagnostics speak to fit",
          "running an F-test after stepwise selection with the naive null distribution",
          "an overall F that is significant while no individual t is -- a collinearity signature, not a contradiction"],
  apps=["the 'F-statistic' line at the bottom of every regression summary; the ANOVA table's F column; the standard test for dropping a block of predictors"],
  related={"uses": ["ols_distribution_under_normal_errors", "f_distribution", "residual_sum_of_squares", "cochran_theorem"],
           "special_case_of": ["likelihood_ratio_test"], "generalizes_from": ["one_way_anova_identity"]},
  sources=["seber_lee_linear_regression", "casella_berger_2e"])

N("r_squared", "definition", "regression",
  "The coefficient of determination is R^2 = 1 - RSS/TSS, where TSS = sum_i (y_i - ybar)^2; it is the fraction of the response's variance 'explained' by the fitted model, and equals the squared correlation between y and y_hat.",
  parseable="R^2 = 1 - RSS/TSS = 1 - sum e_i^2 / sum (y_i - ybar)^2  in [0, 1]",
  symbols={"TSS": "total sum of squares (RSS of the intercept-only model)",
           "adjusted R^2 = 1 - (RSS/(n-p)) / (TSS/(n-1))": "penalizes added predictors"},
  tcn="A descriptive statistic in [0,1] (for a model with an intercept). NOT a goodness-of-fit test, NOT a measure of correct specification, and NOT comparable across different response variables or transformations. R^2 never decreases when a predictor is added -- hence adjusted R^2.",
  regime="exact",
  hyps=[],
  proof=("Pythagorean identity TSS = (explained SS) + RSS (from ols_is_projection, since y - ybar 1 decomposes orthogonally); R^2 = explained/total",
         "sample_variance", "core", None),
  spec=["simple regression: R^2 = (sample correlation of x and y)^2",
        "R^2 = 0: the model does no better than ybar; R^2 = 1: exact fit (residuals all 0)",
        "in-sample R^2 vs cross-validated / out-of-sample R^2 -- the gap measures overfitting"],
  cxd={"model_has_an_intercept": "without an intercept the identity TSS = explained + RSS fails and 'R^2' can be negative or > 1 depending on the software's convention -- it is not interpretable",
       "same_response_and_no_transformation": "R^2 for a model of y and a model of log y are not comparable; a high R^2 can accompany a badly misspecified model and a low R^2 a correct one (just noisy)"},
  misuse=["treating a high R^2 as evidence the model is correct or useful for prediction (could be overfitting; could be a spurious trend)",
          "comparing R^2 across models with different y transformations or different samples",
          "adding predictors to chase R^2 without adjusted R^2 or out-of-sample validation"],
  apps=["ubiquitous as a headline fit summary -- and correspondingly misused; econometrics emphasizes that a low R^2 does not invalidate a well-identified causal estimate"],
  related={"uses": ["residual_sum_of_squares", "sample_variance"], "related_to": ["overall_f_test"]},
  sources=["weisberg_applied_linear_regression", "seber_lee_linear_regression"])

N("partitioned_regression", "theorem", "regression",
  "Frisch-Waugh-Lovell: partition X = [X_1 | X_2]. The OLS coefficient beta_hat_2 on X_2 in the full regression of y on [X_1, X_2] equals the OLS coefficient from regressing the X_1-residuals of y on the X_1-residuals of X_2: beta_hat_2 = ( X_2_tilde^T X_2_tilde )^{-1} X_2_tilde^T y_tilde, where tilde denotes residuals from projecting out X_1 (i.e. M_1 = I - H_1 applied).",
  parseable="beta_hat_2  from  y ~ [X_1, X_2]   equals   beta_hat_2  from  (M_1 y) ~ (M_1 X_2)",
  symbols={"M_1 = I - X_1(X_1^T X_1)^{-1} X_1^T": "the residual-maker for X_1",
           "'partialling out' X_1": "regressing both y and X_2 on X_1 and taking residuals"},
  tcn="An exact algebraic identity. It formalizes 'controlling for X_1': a multiple-regression coefficient is a SIMPLE regression coefficient on the parts of y and x_2 that X_1 cannot explain. Proof: block normal equations, solve the X_1 block, substitute.",
  regime="exact",
  hyps=[],
  proof=("block form of the normal equations [X_1^T X_1, X_1^T X_2; X_2^T X_1, X_2^T X_2][b_1; b_2] = [X_1^T y; X_2^T y]; eliminate b_1 to get (X_2^T M_1 X_2) b_2 = X_2^T M_1 y",
         "ols_is_projection", "cited",
         "the block-elimination identity is proof-checks.lean Stat.fwl_block_elimination for a fixed integer design via decide"),
  spec=["time-series regression with a trend: 'detrend y and x, then regress' gives the same slope as including the trend",
        "fixed-effects panel estimator = regress the within-group-demeaned y on the within-group-demeaned x (X_1 = the group dummies)",
        "the residuals from the full regression equal the residuals from the partialled-out simple regression"],
  cxd={"X_1_and_X_2_not_collinear": "if X_2 lies in the span of X_1 then M_1 X_2 = 0 and beta_hat_2 is not identified -- 'controlling for X_1' has removed all variation in X_2"},
  misuse=["interpreting a partialled-out coefficient as causal without an identification argument (FWL is algebra, not identification)",
          "expecting the SE of beta_hat_2 to come out right from the two-step regression without the correct residual df (n - p, not n - dim(X_2))"],
  apps=["the workhorse identity behind fixed-effects / within estimators, added-variable (partial-regression) plots, and double/debiased machine learning (regress out nuisance predictions, then regress residuals)"],
  related={"uses": ["ordinary_least_squares", "ols_is_projection"], "historically_precedes": []},
  sources=["frisch_waugh_1933", "lovell_1963", "seber_lee_linear_regression"])
