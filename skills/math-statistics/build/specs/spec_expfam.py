"""Exponential families."""
from nodespec import N

N("exponential_family", "structure", "exponential_family",
  "A family is a k-parameter exponential family if its densities can be written f(x; eta) = h(x) exp( eta . T(x) - A(eta) ) for a natural parameter eta in R^k, sufficient statistic T(x) in R^k, carrier h >= 0, and log-partition A(eta).",
  parseable="f(x; eta) = h(x) exp( sum_{j<=k} eta_j T_j(x) - A(eta) )",
  symbols={"eta": "the natural (canonical) parameter, type: point of the natural parameter space in R^k",
           "T(x)": "the natural sufficient statistic, type: R^k-valued function of the data",
           "h(x)": "the carrier / base density, type: nonnegative, theta-free",
           "A(eta)": "the log-partition (cumulant) function, type: real, convex"},
  tcn="The representation is not unique (affine reparametrizations of (eta, T) and rescalings of h). 'k-parameter' means the minimal such k (T has no a.s. affine dependency). A curved exponential family restricts eta to a lower-dimensional manifold and is NOT a k-parameter exponential family.",
  welldef="Well defined wherever A(eta) < inf; the family is the set of such densities. The MINIMAL representation (linearly independent T_j, 1) is unique up to affine change.",
  spec=["Bernoulli(p): eta = log(p/(1-p)), T(x) = x, A(eta) = log(1 + e^eta), h = 1",
        "N(mu, sigma^2): eta = (mu/sigma^2, -1/(2 sigma^2)), T(x) = (x, x^2) -- a 2-parameter family",
        "Poisson, exponential, gamma, beta, binomial(n fixed), multinomial, chi-squared -- all exponential families",
        "uniform(0, theta), Cauchy, Student's t (df unknown) -- NOT exponential families (support moves / no finite-dim sufficient statistic)"],
  cxd={"finite_k": "the location-scale Cauchy family has no sufficient statistic of dimension < n (Pitman-Koopman-Darmois: among families with fixed support, exactly the exponential families admit a fixed-dimension sufficient statistic). All the clean exp-family results below then fail."},
  misuse=["assuming a curved exponential family (eta constrained to a curve) inherits completeness or the clean CRLB-attainment result -- it does not",
          "forgetting the carrier h(x) when it encodes a support constraint"],
  apps=["generalized linear models (logistic, Poisson, gamma regression) ARE exponential-family response models -- the IRLS fitting algorithm and the deviance are exp-family constructs",
        "the maximum-entropy distribution under moment constraints is always an exponential family with T = the constrained functions (statistical mechanics, NLP max-ent models)"],
  related={"illustrated_by": ["prob_normal", "prob_bernoulli", "prob_poisson"],
           "required_by": ["natural_parameter_space", "exponential_family_sufficient_statistic", "crlb_attainment"]},
  sources=["brown_exponential_families", "lehmann_casella_tpe"])

N("natural_parameter_space", "definition", "exponential_family",
  "The natural parameter space is H = { eta in R^k : int h(x) exp(eta . T(x)) dmu(x) < inf }; it is convex, and the family is 'full' if H is used in its entirety.",
  parseable="H = { eta : int h e^{eta.T} dmu < inf },  a convex set",
  symbols={"H": "the natural parameter space, type: convex subset of R^k",
           "int(H)": "its interior, where A is analytic and all moments of T exist"},
  tcn="Convexity is by Holder's inequality. On int(H), A is C^infinity (indeed analytic) and E_eta|T|^m < inf for all m -- this is why exponential families are 'regular' with no fuss.",
  welldef="H is determined by h, T, mu; convex and possibly all of R^k (Gaussian, Bernoulli) or a half-line (gamma with one parameter) or open (some heavy-carrier families).",
  spec=["Bernoulli: H = R (all eta give a finite sum)",
        "N(mu, sigma^2): H = R x (-inf, 0) -- the second natural parameter must be negative",
        "gamma(shape a, rate b), a known: H = (-inf, 0) in the rate's natural parameter -eta"],
  cxd={"eta_in_interior": "at a boundary point of H (if the family is defined there) A may be finite but not differentiable, or E[T] infinite -- the moment identities and PD information fail. 'Full-rank' results need int(H)."},
  misuse=["assuming H = R^k -- for the normal-variance and gamma-rate parameters it is a half-space",
          "evaluating A' or A'' at a boundary eta"],
  related={"required_by": ["cumulant_function", "exponential_family_completeness"]},
  sources=["brown_exponential_families"])

N("cumulant_function", "definition", "exponential_family",
  "A(eta) = log int h(x) exp(eta . T(x)) dmu(x): the log of the normalizing constant, a convex function on H, analytic on int(H).",
  parseable="A(eta) = log int h(x) e^{eta . T(x)} dmu(x)",
  symbols={"A(eta)": "the cumulant / log-partition function, type: convex real function on H",
           "grad A(eta)": "= E_eta[T(X)], the mean-value map",
           "Hess A(eta)": "= Cov_eta(T(X)), positive semidefinite (definite in the minimal representation)"},
  tcn="Convex (Holder); strictly convex in a minimal representation. Its gradient and Hessian are the first two cumulants of T -- see exponential_family_moment_identities.",
  welldef="A(eta) is finite exactly on H and determines the family given (h, T, mu).",
  spec=["Bernoulli: A(eta) = log(1 + e^eta); A'(eta) = e^eta/(1+e^eta) = p; A''(eta) = p(1-p)",
        "N(mu, sigma^2), eta_1 = mu/sigma^2: partial in eta_1 recovers mu",
        "Poisson: A(eta) = e^eta = lambda; A' = A'' = lambda (mean = variance)"],
  misuse=["confusing A(eta) (the log-partition) with the cumulant GENERATING function of X",
          "differentiating A outside int(H)"],
  related={"required_by": ["exponential_family_moment_identities", "mean_value_parametrization"]},
  sources=["brown_exponential_families", "wainwright_jordan_graphical_models"])

N("exponential_family_moment_identities", "identity", "exponential_family",
  "For an exponential family, grad A(eta) = E_eta[T(X)] and Hess A(eta) = Cov_eta(T(X)); in particular the Fisher information in eta is I(eta) = Hess A(eta).",
  parseable="grad A(eta) = E_eta[T(X)];  Hess A(eta) = Cov_eta(T(X)) = I(eta)",
  symbols={"grad A, Hess A": "gradient and Hessian of the cumulant function",
           "E_eta[T], Cov_eta(T)": "mean vector and covariance matrix of the sufficient statistic"},
  tcn="Identities on int(H). Proof: differentiate A(eta) = log int h e^{eta.T} under the integral sign (legal on int(H) by the analyticity of A); first derivative gives the mean, second gives the covariance.",
  status_label="mathematical_identity",
  hyps=["natural_parameter_space"],
  proof=("differentiate A(eta) = log int h e^{eta.T} dmu twice on int(H)",
         "cumulant_function", "core",
         "validation/proof-checks.lean Stat.expfam_grad_A -- the quotient-rule algebra d/deta log(int h e^{eta T}) = E[T]"),
  regime="exact",
  spec=["Bernoulli: A'(eta) = p (mean of T = X), A''(eta) = p(1-p) (variance)",
        "the score U(eta) = sum_i T(x_i) - n grad A(eta) = 'observed minus expected sufficient statistic' -- zero exactly at the MLE",
        "gives the information equality for free: I(eta) = Var(U_1) = Hess A(eta) = -E[Hess log f]"],
  cxd={"natural_parameter_space": "at a boundary eta the interchange of d/deta and int fails; grad A need not equal E[T] (which may be infinite)"},
  misuse=["using E[T] = grad A when eta is a curved / constrained parameter -- the chain rule adds a Jacobian",
          "assuming Hess A is positive DEFINITE without the minimal-representation (full-rank) assumption"],
  apps=["variational inference: the mean parameters mu = grad A(eta) and the Legendre dual A*(mu) are the whole computational backbone of exponential-family graphical models (Wainwright-Jordan)",
        "GLM fitting: the mean-variance relationship Var = A''(A'^{-1}(mu)) is the GLM variance function"],
  related={"equivalent_to": ["information_equality"], "required_by": ["mean_value_parametrization", "exponential_family_completeness"]},
  sources=["brown_exponential_families", "wainwright_jordan_graphical_models"])

N("mean_value_parametrization", "definition", "exponential_family",
  "The mean-value (expectation) parametrization replaces eta by mu = E_eta[T(X)] = grad A(eta); the map eta -> mu is a diffeomorphism from int(H) onto the interior of the convex hull of T's support.",
  parseable="mu = grad A(eta);  eta = (grad A)^{-1}(mu)  on int(H)",
  symbols={"mu": "the mean parameter, type: point of int(conv(supp T))",
           "grad A": "the mean map, a bijection on the interiors by strict convexity of A"},
  tcn="A reparametrization, well-posed because grad A is injective (Hess A = Cov(T) PD in the minimal representation). The Fisher information transforms as I(mu) = (Hess A)^{-1} = Cov_eta(T)^{-1}.",
  welldef="Bijective on the interiors; the inverse is the gradient of the Legendre dual A*(mu) = sup_eta (eta.mu - A(eta)) (negative entropy).",
  spec=["Bernoulli: mu = p (the natural sufficient statistic's mean); eta = logit(p)",
        "N: mu = (E X, E X^2); the moment parametrization",
        "the MLE in the mean parametrization is simply mu_hat = Tbar = (1/n) sum T(x_i) -- always unbiased for mu"],
  misuse=["forgetting the Jacobian when moving a variance or an interval between the eta and mu parametrizations",
          "assuming mu_hat = Tbar is the MLE of a FUNCTION of mu -- it is, by invariance, only for the identity"],
  related={"dual_of": ["cumulant_function"], "required_by": []},
  sources=["brown_exponential_families", "wainwright_jordan_graphical_models"])

N("exponential_family_sufficient_statistic", "theorem", "exponential_family",
  "In an exponential family f(x; eta) = h(x) exp(eta . T(x) - A(eta)), for an iid sample the statistic sum_{i=1}^n T(X_i) is sufficient for eta (and minimal sufficient in the minimal representation).",
  parseable="X_1..X_n iid ~ exp-family  =>  sum_i T(X_i)  is sufficient for eta",
  symbols={"sum_i T(X_i)": "the natural sufficient statistic of the sample, type: R^k, dimension k regardless of n",
           "n": "sample size -- the KEY point: the sufficient statistic's dimension does not grow with n"},
  tcn="A sufficiency claim; it follows immediately from Neyman-Fisher factorization: the joint density is [prod h(x_i)] exp(eta . sum T(x_i) - n A(eta)), which is g(sum T(x_i); eta) times h-part.",
  hyps=[],
  proof=("Neyman-Fisher factorization applied to the product density",
         "neyman_fisher_factorization", "cited",
         "the discrete factorization identity is in proof-checks.lean Stat.factorization_discrete; the exp-family instance is immediate"),
  regime="exact",
  spec=["Bernoulli: sum X_i (the count) is sufficient -- the individual outcomes are not needed",
        "N(mu, sigma^2): (sum X_i, sum X_i^2), equivalently (Xbar, S^2), is sufficient",
        "Poisson: sum X_i; exponential: sum X_i; gamma(a known, rate): sum X_i"],
  cxd={"exponential_family": "outside exponential families the minimal sufficient statistic generally has dimension growing with n -- e.g. for iid Cauchy or uniform-location the full order statistic is minimal sufficient. Pitman-Koopman-Darmois makes this an iff (fixed support)."},
  misuse=["reducing to sum T(X_i) and then using a method that needs the full data (a goodness-of-fit test against the exp-family itself)",
          "assuming (Xbar, S^2) is sufficient for a non-normal location-scale family"],
  related={"special_case_of": ["neyman_fisher_factorization"], "required_by": ["crlb_attainment"]},
  sources=["lehmann_casella_tpe", "brown_exponential_families"])

N("exponential_family_completeness", "theorem", "exponential_family",
  "If the natural parameter space H contains a nonempty open set (the family is full-rank), then the natural sufficient statistic sum_i T(X_i) is complete.",
  parseable="H has nonempty interior  =>  sum_i T(X_i)  is a complete statistic for eta",
  symbols={"complete": "E_eta[g(sum T)] = 0 for all eta implies g = 0 a.s.",
           "full-rank / regular": "int(H) nonempty and T has no a.s. affine dependency"},
  tcn="A completeness claim. Proof: E_eta[g(S)] = 0 for all eta in an open set makes the two-sided Laplace transform of g times the S-density vanish on an open set; by uniqueness of the Laplace transform g = 0 a.e.",
  hyps=["natural_parameter_space"],
  proof=("uniqueness of the (two-sided) Laplace transform on an open set",
         "natural_parameter_space", "cited",
         "Laplace-transform uniqueness is the analytic input; CITED to Lehmann-Romano TSH Thm 4.3.1"),
  regime="exact",
  spec=["N(mu, sigma^2) full family: (sum X_i, sum X_i^2) is complete -- hence (Xbar, S^2) is, so any unbiased function of it is UMVUE (Lehmann-Scheffe)",
        "Bernoulli, Poisson, exponential: sum X_i is complete",
        "used to get Xbar _||_ S^2 for a normal sample via Basu"],
  cxd={"full_rank": "the CURVED family N(theta, theta^2) (one parameter, T two-dimensional constrained to a parabola): the sufficient statistic is NOT complete -- there exist nonzero unbiased-of-zero functions. UMVUE claims via Lehmann-Scheffe then do not go through.",
       "H_open": "N(mu, sigma^2) with sigma^2 KNOWN and mu restricted to integers: H is not open, sum X_i is sufficient but not complete"},
  misuse=["assuming completeness for a curved exponential family or a family with a constrained parameter",
          "concluding an estimator is UMVUE from sufficiency alone, without completeness"],
  related={"required_by": ["basu_theorem", "lehmann_scheffe_theorem"],
           "commonly_confused_with": ["minimal_sufficiency"]},
  sources=["lehmann_romano_tsh", "lehmann_casella_tpe"])
