"""The statistical model, the likelihood, and the regularity conditions."""
from nodespec import N

N("statistical_model", "structure", "model",
  "A statistical model is a family P = { P_theta : theta in Theta } of candidate probability laws for the observed data on a common sample space.",
  parseable="P = { P_theta : theta in Theta }, each P_theta a law on (X, A)",
  symbols={"P": "the model (a set of probability laws)",
           "P_theta": "the law indexed by theta, type: probability measure on the sample space",
           "Theta": "the index set, type: an arbitrary set (parametric if a subset of R^d)",
           "(X, A)": "the sample space and its sigma-algebra"},
  tcn="P_theta is a probability measure for each theta; Theta is a set; the map theta -> P_theta need not be injective (that is identifiability) or continuous.",
  welldef="Nothing to check beyond: each P_theta is a genuine probability measure. The model is an assumption about the data-generating mechanism, not a derived object.",
  spec=["Theta a single point: the model is fully specified, no inference to do",
        "Theta = all laws on X: the fully nonparametric model",
        "Theta a subset of R^d: a parametric model (next node)"],
  misuse=["conflating the model with the truth -- 'all models are wrong'; the true law need not lie in P (misspecification)",
          "letting a statistic or the data enter the definition of P_theta"],
  related={"specializes_to": ["parametric_model"], "commonly_confused_with": ["identifiability"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("parametric_model", "definition", "model",
  "A parametric model is a statistical model whose index set Theta is a subset of R^d for a fixed finite d; theta is the parameter.",
  parseable="statistical_model with Theta subset R^d, d fixed and finite",
  symbols={"d": "the (fixed, finite) dimension of the parameter, type: positive integer",
           "theta": "a parameter value, type: point of Theta subset R^d"},
  tcn="Theta must be a subset of a fixed R^d; 'finite-dimensional' is the whole content. Models with d growing with n, or infinite-dimensional nuisance parts, are semi-/non-parametric and out of scope.",
  welldef="d is fixed in advance and does not depend on the sample size n.",
  spec=["d = 1: a scalar parameter (Bernoulli p, Poisson lambda, exponential rate)",
        "d = 2: N(mu, sigma^2), Gamma(shape, rate), the simple linear model's (slope, intercept) with sigma^2 known",
        "Theta open: needed for the interior-point regularity condition later"],
  cxd={"finite_fixed_dimension": "a location model with an unknown error density f is nonparametric in f; the CRLB and MLE-normality theory below does not apply to f without extra structure"},
  misuse=["counting the sample size or the number of observed categories as part of d",
          "treating a nonparametric functional (a density value, a quantile) as a 'parameter' with a Fisher information"],
  related={"generalizes_from": ["statistical_model"], "historically_precedes": ["kernel_density_estimator"]},
  sources=["casella_berger_2e", "van_der_vaart_asymptotic"])

N("parameter_space", "definition", "model",
  "The parameter space Theta is the set of admissible parameter values; the model is indexed by it.",
  parseable="Theta: the set of admissible theta",
  symbols={"Theta": "the parameter space, type: subset of R^d",
           "int(Theta)": "its interior; theta_0 in int(Theta) is a standard regularity condition"},
  tcn="Theta is a set; 'admissible' means the modeller asserts every theta in Theta is a priori possible and no theta outside it is.",
  welldef="Constraints defining Theta (sigma^2 > 0, p in [0,1], a correlation in [-1,1], a covariance matrix positive definite) are part of the model specification.",
  spec=["Theta = (0, inf) for a variance or a rate",
        "Theta = [0, 1] for a probability -- note the boundary points, where interior-point regularity fails",
        "Theta = R^d for a location / regression-coefficient vector",
        "Theta = { positive definite d x d matrices } for a covariance parameter"],
  cxd={"admissibility_of_the_constraint": "if the true p is 0 or 1 (on the boundary of [0,1]) the MLE is still consistent but sqrt(n)-asymptotic normality fails -- the limit is one-sided"},
  misuse=["forgetting that a boundary true value breaks the interior-point regularity condition",
          "using an unconstrained optimizer that leaves Theta (a negative variance estimate)"],
  related={"commonly_confused_with": ["null_hypothesis"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("identifiability", "hypothesis", "model",
  "The model is identifiable if theta != theta' implies P_theta != P_theta': the map theta -> P_theta is injective.",
  parseable="for all theta, theta' in Theta: theta != theta' => P_theta != P_theta'",
  symbols={"theta, theta'": "distinct candidate parameter values",
           "P_theta": "the law they index"},
  tcn="An injectivity statement about theta -> P_theta. Equivalent (dominated case): f(x; theta) = f(x; theta') for a.e. x implies theta = theta'.",
  welldef="Identifiability is a property of the PARAMETRIZATION, not the data. A non-identifiable model can be repaired by quotienting Theta or adding a constraint.",
  spec=["a full-rank exponential family in its natural parametrization is identifiable",
        "N(mu, sigma^2) is identifiable in (mu, sigma^2); it is NOT identifiable in (mu, sigma) vs (mu, -sigma) if sigma is allowed negative",
        "a two-component mixture is identifiable only up to permutation of the labels ('label switching')"],
  cxd={"identifiability": "the model X ~ N(alpha + beta, 1) with theta = (alpha, beta): alpha and beta are not separately identifiable -- only their sum is. The MLE of (alpha, beta) is not consistent (it wanders along the ridge alpha + beta = const); the Fisher information matrix is singular. This is exactly why fisher_information_positive_definite is a separate hypothesis."},
  misuse=["assuming the MLE is consistent without checking identifiability (mle_consistency requires it)",
          "reading a tight confidence interval for a non-identified parameter as informative -- it reflects the prior / penalty, not the data"],
  related={"commonly_confused_with": ["fisher_information_positive_definite"], "required_by": ["mle_consistency"]},
  sources=["van_der_vaart_asymptotic", "lehmann_casella_tpe"])

N("dominated_family", "hypothesis", "model",
  "The model is dominated if there is one sigma-finite measure mu such that every P_theta has a density f(x; theta) = dP_theta / dmu.",
  parseable="exists sigma-finite mu: for all theta, P_theta << mu, with density f(.; theta)",
  symbols={"mu": "the dominating measure, type: sigma-finite measure on the sample space (usually Lebesgue or counting)",
           "f(x; theta)": "the density / likelihood contribution, type: measurable function >= 0 with int f dmu = 1"},
  tcn="An absolute-continuity statement P_theta << mu holding for ALL theta with ONE mu. Gives a common reference so likelihoods L(theta) = prod f(x_i; theta) are comparable across theta.",
  welldef="mu is not unique but the model being dominated is a genuine property; the Radon-Nikodym derivative f(.; theta) is mu-a.e. unique.",
  spec=["discrete model: mu = counting measure, f = the pmf",
        "continuous model on R^k: mu = Lebesgue, f = the pdf",
        "a mixed discrete-continuous model: mu = Lebesgue + counting on the atoms"],
  cxd={"dominated_family": "Theta = R, P_theta = the point mass delta_theta. No single sigma-finite mu dominates all of them (an uncountable family of mutually singular measures). The likelihood is not well defined and the whole density-based apparatus (score, Fisher information, factorization) is unavailable."},
  misuse=["writing down a 'likelihood' for a model that is not dominated (e.g. uniform(0, theta) IS dominated by Lebesgue -- fine -- but comparing densities of singular laws is not)",
          "forgetting the support may still depend on theta even in a dominated family (uniform(0, theta)); that is the separate support_independent_of_theta condition"],
  related={"required_by": ["likelihood_function", "neyman_fisher_factorization"]},
  sources=["lehmann_romano_tsh", "schervish_theory_of_statistics"])

N("iid_sample", "definition", "model",
  "An i.i.d. sample of size n from the model is X_1, ..., X_n drawn independently, all from one common P_theta in P.",
  parseable="X_1,...,X_n iid ~ P_theta for a single unknown theta in Theta",
  symbols={"n": "the sample size, type: positive integer",
           "X_i": "the i-th observation, type: random element of the sample space",
           "X": "the whole sample (X_1,...,X_n), type: random element of X^n"},
  tcn="Joint law is the n-fold product P_theta^{(n)}. One theta governs all n observations (not one per observation).",
  welldef="The observations share the sample space and the sigma-algebra; 'identically distributed' fixes one theta.",
  spec=["n = 1: a single observation; the likelihood is f(x_1; theta)",
        "the joint density is prod_{i=1}^n f(x_i; theta) -- the product structure is what makes the log-likelihood a SUM",
        "non-iid extensions (regression with fixed covariates, time series) replace the product by the actual joint density"],
  cxd={"independence": "if the X_i are positively correlated, the effective sample size is below n; a variance estimate treating them as independent is too small and confidence intervals undercover",
       "identical_distribution": "if each X_i has its own theta_i there is nothing to estimate without a link; the model is not a fixed-d parametric model"},
  misuse=["applying iid-sample theory to clustered or longitudinal data (students within schools) without a random-effects or cluster-robust adjustment",
          "treating a convenience sample as a random sample from the target population"],
  related={"required_by": ["likelihood_function", "sample_mean", "empirical_cdf"]},
  sources=["casella_berger_2e"])

N("statistic", "definition", "model",
  "A statistic is a measurable function T = T(X_1, ..., X_n) of the sample that does not depend on theta.",
  parseable="T: X^n -> Y measurable, no theta in its definition",
  symbols={"T": "the statistic, type: measurable map X^n -> Y (some measurable space)",
           "T(X)": "its value on the sample, type: random element of Y"},
  tcn="Measurability is the only formal requirement. The 'no theta' clause is what makes T computable from data alone -- an estimator, a test statistic, a sufficient statistic are all statistics.",
  welldef="T(X) is a random variable because T is measurable and X is; its law depends on theta through the law of X.",
  spec=["the sample mean, sample variance, order statistics, the maximum -- all statistics",
        "the sample itself, X = (X_1,...,X_n), is the trivial (always sufficient) statistic",
        "a constant map is a (useless, always ancillary) statistic"],
  cxd={"no_theta_dependence": "sum_i (X_i - mu_0)^2 with a KNOWN mu_0 is a statistic; sum_i (X_i - mu)^2 with the unknown true mu is NOT a statistic -- it cannot be computed"},
  misuse=["calling a quantity a statistic when it secretly uses the unknown parameter (e.g. 'standardize by the true sigma')",
          "forgetting that the LAW of a statistic still depends on theta -- 'statistic' does not mean 'pivotal'"],
  related={"specializes_to": ["estimator", "test_function", "sufficiency", "ancillary_statistic"]},
  sources=["casella_berger_2e"])

N("likelihood_function", "definition", "model",
  "The likelihood function is L(theta) = L(theta; x) = prod_{i=1}^n f(x_i; theta), the joint density evaluated at the observed data and read as a function of theta for fixed x.",
  parseable="L(theta) = prod_{i=1}^n f(x_i; theta), argument theta, data x fixed",
  symbols={"L(theta)": "the likelihood, type: nonnegative function on Theta (NOT a density in theta -- it need not integrate to 1)",
           "f(x_i; theta)": "the density of one observation",
           "x = (x_1,...,x_n)": "the observed (now fixed) data"},
  tcn="L: Theta -> [0, inf). It is a function of theta, not a probability distribution over theta. Two data sets with proportional likelihoods (same L up to a theta-free constant) carry the same likelihood information.",
  welldef="Requires the dominated_family condition so the f(.; theta) are densities against one mu. L is defined up to the theta-free multiplicative constant coming from the choice of mu.",
  spec=["n iid Bernoulli(p): L(p) = p^{sum x_i} (1-p)^{n - sum x_i}",
        "n iid N(mu, sigma^2): L(mu, sigma^2) = (2 pi sigma^2)^{-n/2} exp(- sum (x_i - mu)^2 / 2 sigma^2)",
        "uniform(0, theta): L(theta) = theta^{-n} 1{ theta >= max x_i } -- not differentiable at the MLE"],
  cxd={"dominated_family": "without a common dominating measure the products f(x_i; theta) for different theta are not comparable and 'maximize the likelihood' is meaningless"},
  misuse=["reading L(theta) as a posterior density -- it becomes one only after multiplying by a prior and normalizing (Bayes)",
          "reading L(theta_1)/L(theta_2) as odds on theta without a prior",
          "dropping the indicator / support factor (uniform, shifted exponential) and getting the wrong MLE"],
  apps=["the single object the likelihood principle says all inference should depend on",
        "every MLE, LRT, Wald and score procedure, and every Bayesian posterior is built from it"],
  related={"required_by": ["log_likelihood", "maximum_likelihood_estimator", "neyman_pearson_lemma", "likelihood_ratio_test"],
           "commonly_confused_with": ["prob_pdf"]},
  sources=["casella_berger_2e", "cox_hinkley_theoretical_statistics"])

N("log_likelihood", "definition", "model",
  "The log-likelihood is ell(theta) = log L(theta) = sum_{i=1}^n log f(x_i; theta).",
  parseable="ell(theta) = sum_{i=1}^n log f(x_i; theta)",
  symbols={"ell(theta)": "the log-likelihood, type: function Theta -> [-inf, inf)",
           "ell_i(theta) = log f(x_i; theta)": "the i-th contribution"},
  tcn="ell is well defined (possibly -inf) wherever L >= 0; the product becoming a SUM is the point -- it makes the score a sum of iid terms and hands the CLT its hypothesis.",
  welldef="Same theta-free additive constant ambiguity as L (from the choice of mu). Differences ell(theta_1) - ell(theta_2) are unambiguous.",
  spec=["Bernoulli: ell(p) = (sum x_i) log p + (n - sum x_i) log(1 - p)",
        "the maximizer of ell is the maximizer of L (log is increasing) -- this is why one always works with ell",
        "at a data point with f(x_i; theta) = 0, ell(theta) = -inf: theta is excluded"],
  misuse=["differentiating L instead of ell and drowning in product rule",
          "forgetting ell can be -inf on part of Theta (support constraints)"],
  related={"required_by": ["score_function", "likelihood_ratio_test"]},
  sources=["casella_berger_2e"])

N("score_function", "definition", "model",
  "The score is U(theta) = d ell / d theta = sum_{i=1}^n d/dtheta log f(x_i; theta); in R^d it is the gradient of ell.",
  parseable="U(theta) = grad_theta ell(theta) = sum_i grad_theta log f(X_i; theta)",
  symbols={"U(theta)": "the score, type: R^d-valued random function of theta (random through the data)",
           "U_1(theta) = grad log f(X_1; theta)": "the single-observation score",
           "U(theta) = sum_i U_i(theta)": "additivity over an iid sample"},
  tcn="U(theta) in R^d for each theta; as a function of the data it is a random vector. Requires log f(x; .) differentiable in theta (part of log_likelihood_smooth). The MLE, when interior and smooth, is a root of U.",
  welldef="Defined wherever ell is differentiable in theta. For an iid sample, U(theta) is a sum of n iid random vectors -- the structure the CLT needs.",
  spec=["Bernoulli: U(p) = (sum x_i)/p - (n - sum x_i)/(1 - p); setting U(p) = 0 gives p_hat = xbar",
        "N(mu, sigma^2 known): U(mu) = sum (x_i - mu)/sigma^2; root at mu_hat = xbar",
        "exponential family f = h exp(eta T - A): U(eta) = sum T(x_i) - n A'(eta) -- the score is 'observed minus expected sufficient statistic'"],
  cxd={"log_likelihood_smooth": "uniform(0, theta): ell(theta) = -n log theta on theta >= max x_i, so U(theta) = -n/theta is never 0 -- the MLE is at the boundary theta_hat = max x_i and the score equation has no solution. Score-based asymptotics (Fisher information, mle_asymptotic_normality) do not apply."},
  misuse=["solving U(theta) = 0 and declaring victory without checking it is a MAXIMUM and INTERIOR",
          "using the score identity / information equality when differentiation under the integral sign fails"],
  apps=["the score test uses U(theta_0) evaluated only under the null -- no need to fit the alternative",
        "score / estimating equations generalize to GLMs, GMM, and quasi-likelihood"],
  related={"required_by": ["fisher_information", "score_identity", "mle_score_equation", "score_test"],
           "generalizes_to": ["estimating_equation"]},
  sources=["casella_berger_2e", "van_der_vaart_asymptotic"])

N("fisher_information", "definition", "model",
  "The (per-observation) Fisher information is I(theta) = Var_theta(U_1(theta)) = E_theta[(d/dtheta log f(X_1; theta))^2]; the sample information is n I(theta).",
  parseable="I(theta) = Var_theta( d/dtheta log f(X_1; theta) )",
  symbols={"I(theta)": "Fisher information, type: nonnegative real (scalar theta)",
           "U_1(theta)": "the single-observation score",
           "n I(theta)": "the information in an iid sample of size n (scores add, variances of independent sums add)"},
  tcn="I(theta) >= 0 real; equals E[U_1^2] because E[U_1] = 0 (score identity). Additivity n I(theta) over an iid sample is Var of a sum of iid. Under regularity also I(theta) = -E[d^2/dtheta^2 log f] (information_equality).",
  welldef="Finite and positive under regularity; can be 0 (parameter locally invisible) or +inf.",
  spec=["Bernoulli(p): I(p) = 1 / (p(1-p)) -- largest near p = 0 or 1, smallest at p = 1/2",
        "Poisson(lambda): I(lambda) = 1/lambda",
        "N(mu, sigma^2): I(mu) = 1/sigma^2 (with sigma^2 known); the information about mu does not depend on mu",
        "location family f(x - theta): I(theta) = int (f'/f)^2 f dx does not depend on theta"],
  cxd={"interchange_derivative_integral": "if int f can't be differentiated under the integral sign, E[U_1] != 0 and I(theta) = Var(U_1) != E[U_1^2]; the CRLB denominator and the information equality both fail",
       "log_likelihood_smooth": "uniform(0, theta): the naive I(theta) = 1/theta^2 gives the WRONG rate -- the MLE converges at rate n, not sqrt(n), so 'Var >= 1/(n I)' is not the operative bound"},
  misuse=["quoting 1/(n I(theta)) as the variance of an estimator that is biased or not the MLE",
          "using per-observation I where sample information n I is meant (or vice versa) -- a factor-of-n error in every standard error"],
  apps=["the standard error of a fitted parameter in every statistics package is sqrt of a diagonal entry of the inverse observed information",
        "optimal experimental design maximizes a scalar function of the information matrix (D-, A-, E-optimality)"],
  related={"required_by": ["cramer_rao_lower_bound", "mle_asymptotic_normality", "wald_test", "score_test", "wald_interval"],
           "equivalent_to": ["information_equality"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("fisher_information_matrix", "definition", "model",
  "For a d-dimensional parameter, I(theta) = Cov_theta(grad log f(X_1; theta)) = E_theta[grad log f (grad log f)^T], a symmetric positive-semidefinite d x d matrix; = -E_theta[Hessian of log f] under regularity.",
  parseable="I(theta) = E_theta[ (grad log f(X_1;theta)) (grad log f(X_1;theta))^T ]",
  symbols={"I(theta)": "the d x d Fisher information matrix, type: symmetric PSD matrix",
           "grad log f": "the score vector, type: R^d",
           "I(theta)^{-1}": "the asymptotic covariance of the MLE (when I is positive definite)"},
  tcn="Symmetric PSD by construction (a covariance matrix). Requires the vector score to exist (log_likelihood_smooth). Positive-DEFINITE is the separate hypothesis fisher_information_positive_definite. Uses linear_algebra_background for 'PSD', 'inverse', 'eigenvalues'.",
  welldef="A covariance matrix, hence symmetric PSD; the matrix analogue of the scalar I(theta).",
  spec=["N(mu, sigma^2), theta = (mu, sigma^2): I = diag(1/sigma^2, 1/(2 sigma^4)) -- mu and sigma^2 are 'information-orthogonal'",
        "a full-rank exponential family: I(eta) = Hess A(eta), the Hessian of the cumulant function (always PD on the interior)",
        "block structure: a zero off-diagonal block means the blocks' MLEs are asymptotically independent"],
  cxd={"fisher_information_positive_definite": "the non-identified model N(alpha + beta, 1): I(alpha, beta) = [[1,1],[1,1]], singular -- not invertible, so I^{-1} does not exist and MLE-normality fails along the ridge"},
  misuse=["inverting a near-singular information matrix (collinear covariates) and reporting the resulting huge, unstable standard errors as if reliable",
          "ignoring off-diagonal terms and treating each coordinate's information as 1 / I_jj instead of (I^{-1})_jj"],
  related={"required_by": ["fisher_information_positive_definite", "mle_asymptotic_normality"],
           "specializes_from": ["fisher_information"]},
  sources=["lehmann_casella_tpe", "van_der_vaart_asymptotic"])

N("score_identity", "identity", "model",
  "Under regularity, E_theta[U(theta)] = 0: the score has mean zero at the true parameter.",
  parseable="E_theta[ grad_theta log f(X; theta) ] = 0  for every theta in int(Theta)",
  symbols={"U(theta)": "the score at theta", "E_theta": "expectation under P_theta (the SAME theta as in the score)"},
  tcn="An identity in theta, not an approximation. Proof: 1 = int f(x; theta) dx; differentiate both sides; if d/dtheta passes inside, 0 = int (d/dtheta f) dx = int (d/dtheta log f) f dx = E_theta[U_1(theta)]. Sum over i for the full score.",
  status_label="mathematical_identity",
  hyps=["interchange_derivative_integral", "support_independent_of_theta"],
  proof=("differentiate the identity int f(x;theta) dx = 1 under the integral sign",
         "interchange_derivative_integral", "core",
         "validation/proof-checks.lean Stat.score_mean_zero -- the algebra int (d log f) f = int df given int f = 1"),
  regime="exact",
  spec=["Bernoulli: E_p[(X - p)/(p(1-p))] = 0 since E[X] = p",
        "N(mu, sigma^2 known): E_mu[(X - mu)/sigma^2] = 0",
        "gives the moment condition behind method-of-moments and estimating-equation estimators"],
  cxd={"support_independent_of_theta": "uniform(0, theta): the support {0 <= x <= theta} moves with theta, so d/dtheta int_0^theta theta^{-1} dx has a boundary term; E_theta[U(theta)] = -1/theta != 0. The score is biased and every downstream result (CRLB, information equality) fails.",
       "interchange_derivative_integral": "a family where the tails are too heavy for the dominated-derivative condition: the swap of d/dtheta and int is invalid and E[U] can be nonzero"},
  misuse=["assuming E[score] = 0 for a MISSPECIFIED model -- then the score has mean zero only at the pseudo-true parameter, not the truth",
          "applying it at a boundary theta"],
  related={"required_by": ["information_equality", "cramer_rao_lower_bound"],
           "generalizes_to": ["estimating_equation"]},
  sources=["casella_berger_2e", "van_der_vaart_asymptotic"])

N("information_equality", "identity", "model",
  "Under regularity, I(theta) = Var_theta(U_1(theta)) = -E_theta[ d^2/dtheta^2 log f(X_1; theta) ]: the two information formulas agree.",
  parseable="E_theta[ (d log f)^2 ] = - E_theta[ d^2 log f ]  (per observation)",
  symbols={"I(theta)": "Fisher information", "d^2/dtheta^2 log f": "the observation's log-likelihood curvature",
           "-E[d^2 ell]": "the 'expected information'; its sample analogue -d^2 ell(theta_hat) is the OBSERVED information"},
  tcn="An identity, per observation. Proof: differentiate the score identity int (d log f) f dx = 0 once more; product rule gives int (d^2 log f) f + int (d log f)^2 f = 0, i.e. E[d^2 log f] + E[(d log f)^2] = 0.",
  status_label="mathematical_identity",
  hyps=["interchange_derivative_integral", "log_likelihood_smooth"],
  proof=("differentiate the score identity a second time under the integral sign; product rule",
         "score_identity", "core",
         "validation/proof-checks.lean Stat.information_equality -- given int f = 1 and the two interchanges, E[d2 log f] + E[(d log f)^2] = 0"),
  regime="exact",
  spec=["Bernoulli: -E[d^2/dp^2 ell_1] = -E[-X/p^2 - (1-X)/(1-p)^2] = 1/(p(1-p)) = Var((X-p)/(p(1-p)))",
        "exponential family: both sides equal A''(eta) -- the cumulant function's curvature",
        "lets you estimate I(theta) either by the sample variance of the scores or by the negative average Hessian (the latter is what optimizers return for free)"],
  cxd={"interchange_derivative_integral": "same failure as score_identity: without the second interchange the two formulas disagree and the 'sandwich' variance A^{-1} B A^{-1} (with A != B) is the correct one -- this is exactly the misspecification-robust case",
       "log_likelihood_smooth": "a non-twice-differentiable log-density (Laplace / double-exponential at its median) -- the second-derivative form does not exist though the variance form may"},
  misuse=["reporting the negative-Hessian (observed information) standard errors when the model is misspecified -- use the sandwich estimator",
          "assuming the equality to justify dropping the score-variance computation in a quasi-likelihood setting where it does NOT hold"],
  apps=["every optimizer that reports standard errors from the inverse Hessian at the optimum is using this identity (Hessian = -observed information ~ -n I)",
        "the score test statistic U(theta_0)^2 / (n I(theta_0)) uses the expected-information form under the null"],
  related={"equivalent_to": ["fisher_information"], "required_by": ["score_test", "mle_asymptotic_normality"]},
  sources=["casella_berger_2e", "cox_hinkley_theoretical_statistics"])

N("support_independent_of_theta", "hypothesis", "model",
  "The set { x : f(x; theta) > 0 } is the same for every theta in Theta.",
  parseable="supp f(.; theta) does not depend on theta",
  symbols={"supp f(.; theta)": "the support of the density, type: measurable subset of the sample space"},
  tcn="A statement that a theta-indexed family of sets is constant. It is what lets d/dtheta and int_{supp} commute without a boundary (Leibniz) term.",
  welldef="A property of the model as parametrized.",
  spec=["N(mu, sigma^2): support is all of R for every (mu, sigma^2) -- holds",
        "Poisson(lambda): support is {0,1,2,...} for every lambda -- holds",
        "exponential(rate): support (0, inf) for every rate -- holds"],
  cxd={"support_independent_of_theta": "uniform(0, theta): support (0, theta) depends on theta. Consequences: the score identity fails, the CRLB does not bind (the MLE max X_i beats it), the MLE converges at rate n and its limit is Exponential-not-Normal. The whole 'regular' theory is replaced by 'non-regular' (boundary) theory.",
       "also fails for": "shifted exponential f(x; theta) = e^{-(x - theta)} 1{x >= theta}; and for uniform(theta, theta + 1)"},
  misuse=["computing a Fisher information for uniform(0, theta) and quoting sqrt(n)-rate confidence intervals",
          "assuming it holds because the density formula 'looks smooth' -- check the indicator"],
  related={"required_by": ["score_identity", "cramer_rao_lower_bound"], "commonly_confused_with": ["log_likelihood_smooth"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("interchange_derivative_integral", "hypothesis", "model",
  "d/dtheta int f(x; theta) dmu(x) = int d/dtheta f(x; theta) dmu(x), and likewise for int g(x) f(x; theta): the theta-derivative passes under the integral sign.",
  parseable="d/dtheta int f(.;theta) = int d/dtheta f(.;theta)  (and the same with a test function g)",
  symbols={"f(x; theta)": "the density", "the dominating condition": "|d/dtheta f(x; theta)| <= h(x) locally, with int h dmu < inf"},
  tcn="The hypothesis of the measure-theoretic 'differentiation under the integral' theorem (ra_interchange_limit_integral supplies the sufficient condition). Needed twice: once for the score identity, once more for the information equality.",
  welldef="A regularity condition on the model; typically verified once via a dominating function.",
  spec=["exponential families on their natural parameter's interior: holds automatically (analyticity of A)",
        "N, Poisson, Gamma, Beta in their standard parametrizations: holds",
        "the sufficient condition (ra_interchange_limit_integral): a local integrable envelope for the theta-derivative"],
  cxd={"interchange_derivative_integral": "combine with a moving support (uniform(0, theta)) or heavy tails and the swap introduces a boundary term or diverges: E[score] != 0, so score_identity, information_equality, cramer_rao_lower_bound all fail. The robust replacement is the sandwich variance."},
  misuse=["treating it as automatic -- it is the single most-skipped hypothesis in applied derivations",
          "assuming that because the MLE 'worked' the interchange was valid"],
  related={"required_by": ["score_identity", "information_equality", "cramer_rao_lower_bound"],
           "licensed_by": ["ra_interchange_limit_integral"]},
  sources=["lehmann_casella_tpe", "schervish_theory_of_statistics"])

N("true_parameter_interior", "hypothesis", "model",
  "The true parameter theta_0 lies in the interior of Theta, not on its boundary.",
  parseable="theta_0 in int(Theta)",
  symbols={"theta_0": "the (unknown) data-generating parameter", "int(Theta)": "the topological interior of the parameter space"},
  tcn="A location statement about theta_0 relative to Theta. It is what allows a two-sided Taylor expansion of the score around theta_0 and guarantees the MLE is eventually an interior stationary point.",
  welldef="An assumption about the truth; unverifiable from data but often defensible (a variance is not exactly 0, a probability is not exactly 1).",
  spec=["theta_0 = 1/2 for a Bernoulli p in (0,1): interior, holds",
        "theta_0 on the boundary: p = 0, a variance component = 0, a correlation = 1"],
  cxd={"true_parameter_interior": "testing H0: sigma^2_between = 0 in a random-effects model -- the null is on the boundary of [0, inf). The LRT statistic -2 log Lambda is NOT chi^2_1 under H0; it is a 50:50 mixture of chi^2_0 and chi^2_1 (Self-Liang / Chernoff). Using the chi^2_1 critical value makes the test conservative.",
       "also": "p_hat for a truly-degenerate coin (p = 0): sqrt(n)(p_hat - 0) does not converge to a two-sided normal"},
  misuse=["using standard chi^2 critical values for a variance-component or a one-sided boundary test",
          "reporting a symmetric Wald interval for a parameter whose true value is at the edge of its range"],
  related={"required_by": ["mle_score_equation", "mle_asymptotic_normality", "wilks_theorem"]},
  sources=["van_der_vaart_asymptotic", "self_liang_1987"])

N("fisher_information_positive_definite", "hypothesis", "model",
  "I(theta_0) is finite and positive definite (positive and finite in the scalar case).",
  parseable="0 < I(theta_0) < inf  (scalar);  I(theta_0) symmetric PD  (vector)",
  symbols={"I(theta_0)": "the Fisher information at the truth", "PD": "positive definite: x^T I x > 0 for all x != 0"},
  tcn="A nondegeneracy statement. PD => invertible => I(theta_0)^{-1} exists and is the MLE's asymptotic covariance. Uses linear_algebra_background for 'positive definite' and 'invertible'.",
  welldef="Finiteness rules out heavy-tailed scores; positive-definiteness rules out a locally flat / non-identified direction.",
  spec=["any full-rank exponential family on the interior: I(eta) = Hess A(eta) is PD (A strictly convex)",
        "N(mu, sigma^2): I = diag(1/sigma^2, 1/(2 sigma^4)) PD for every sigma^2 > 0"],
  cxd={"fisher_information_positive_definite": "the non-identified N(alpha + beta, 1): I = [[1,1],[1,1]] has eigenvalue 0 along (1, -1). The MLE is not consistent for (alpha, beta) and I^{-1} does not exist. This hypothesis is the local (2nd-order) counterpart of the global identifiability hypothesis.",
       "singular in the limit": "a model at a parameter where a component's information -> 0 (e.g. a mixture weight at 0 or 1): near-singular I, unstable inversion, wide/unreliable intervals"},
  misuse=["inverting a singular or near-singular information matrix from collinear regressors and trusting the standard errors",
          "conflating this with global identifiability -- a model can be identified yet have singular information at one point (a 'non-regular' point)"],
  related={"required_by": ["mle_asymptotic_normality", "wilks_theorem"], "commonly_confused_with": ["identifiability"]},
  sources=["van_der_vaart_asymptotic", "lehmann_casella_tpe"])

N("log_likelihood_smooth", "hypothesis", "model",
  "theta -> log f(x; theta) is three-times continuously differentiable in a neighbourhood of theta_0, with the third derivative dominated by a fixed integrable function uniformly in that neighbourhood.",
  parseable="log f(x; .) in C^3 near theta_0, with |d^3 log f(x;theta)| <= M(x), E[M(X)] < inf",
  symbols={"C^3": "three continuous theta-derivatives", "M(x)": "a fixed integrable envelope for the third derivative"},
  tcn="The classical (Cramer) smoothness package. C^2 gives the information equality; the dominated third derivative controls the Taylor remainder in the MLE expansion so the o_p(1) terms really are negligible.",
  welldef="A regularity condition on the model.",
  spec=["exponential families: log f = eta.T(x) - A(eta) + log h(x) is real-analytic in eta -- holds on the interior",
        "N, Gamma, Beta, Poisson in standard parametrizations: hold",
        "weaker 'differentiability in quadratic mean' (van der Vaart) replaces C^3 for the modern proof of mle_asymptotic_normality -- but C^3 is what the classical proof and this capsule use"],
  cxd={"log_likelihood_smooth": "Laplace(theta) = (1/2) e^{-|x - theta|}: log f = -|x - theta| + const is not differentiable at x = theta. The MLE is the sample median; it IS sqrt(n)-asymptotically normal (variance 1), but via a different argument (the score is a sum of sign(X_i - theta), not a smooth function) -- the C^3 route does not apply.",
       "uniform(0, theta)": "not differentiable in theta at all on the relevant set -- non-regular, rate n not sqrt(n)"},
  misuse=["applying the -d^2 ell(theta_hat) 'observed information' standard error to a non-smooth model (quantile regression, Laplace)",
          "assuming a neural-net / spline log-likelihood satisfies this near a boundary of the parameter region"],
  related={"required_by": ["information_equality", "mle_asymptotic_normality", "wilks_theorem"],
           "commonly_confused_with": ["support_independent_of_theta"]},
  sources=["lehmann_casella_tpe", "van_der_vaart_asymptotic"])
