"""Sufficiency, ancillarity, completeness."""
from nodespec import N

N("sufficiency", "definition", "sufficiency",
  "A statistic T is sufficient for theta if the conditional distribution of the sample X given T(X) = t does not depend on theta.",
  parseable="P_theta( X in . | T(X) = t )  does not depend on theta",
  symbols={"T": "the statistic, type: measurable map of the sample",
           "P_theta(X in . | T = t)": "the conditional law of the data given the statistic"},
  tcn="A conditional-independence statement: given T, the data carry no further information about theta. Equivalent operational form: one could simulate a fresh sample with the same distribution from T alone.",
  welldef="Requires a regular conditional distribution to exist (it does for the models in scope). The full sample is trivially sufficient; the constant statistic is sufficient only if theta is known.",
  spec=["Bernoulli: the count sum X_i is sufficient; the order of the 0s and 1s is not informative",
        "N(mu, sigma^2): (Xbar, S^2) is sufficient",
        "uniform(0, theta): the maximum X_(n) is sufficient"],
  cxd={"conditional_law_theta_free": "Xbar alone is NOT sufficient for (mu, sigma^2) in a normal sample: given Xbar, the spread of the residuals still depends on sigma^2. Discarding S^2 loses information about sigma^2."},
  misuse=["reducing data to an insufficient statistic and then claiming no information was lost",
          "confusing 'sufficient' (loses nothing) with 'minimal sufficient' (the maximal reduction) or 'complete'"],
  apps=["the Rao-Blackwell recipe -- condition any estimator on a sufficient statistic to improve it -- is the basis of every UMVUE derivation",
        "in privacy / compression, a sufficient statistic is the minimal release that preserves inference about theta"],
  related={"required_by": ["neyman_fisher_factorization", "rao_blackwell_theorem", "basu_theorem"],
           "specializes_to": ["minimal_sufficiency"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("neyman_fisher_factorization", "theorem", "sufficiency",
  "T is sufficient for theta iff the density factors as f(x; theta) = g(T(x); theta) h(x) for some functions g >= 0 and h >= 0 with h not depending on theta.",
  parseable="T sufficient  <=>  f(x; theta) = g(T(x); theta) . h(x),  h theta-free",
  symbols={"g(t; theta)": "the theta-dependent factor, a function of the data ONLY through T",
           "h(x)": "the theta-free residual factor"},
  tcn="An iff. The practical test for sufficiency: write the joint density and see whether theta interacts with the data only through T(x). In the dominated case this is rigorous (Halmos-Savage); the discrete case is a direct conditional-probability computation.",
  hyps=["dominated_family"],
  proof=("discrete: direct computation of P(X = x | T = t); general: Halmos-Savage / Radon-Nikodym factorization",
         "sufficiency", "core",
         "validation/proof-checks.lean Stat.factorization_discrete -- the identity P(X=x|T=t) = g(t)h(x) / sum_{x': T(x')=t} g(t)h(x') with the g(t) cancelling"),
  regime="exact",
  spec=["Bernoulli: f = p^{sum x_i}(1-p)^{n - sum x_i} = [p/(1-p)]^{sum x_i} (1-p)^n . 1  => T = sum x_i sufficient",
        "N(mu, sigma^2): the exponent expands to a function of (sum x_i, sum x_i^2) plus a theta-free term",
        "uniform(0, theta): f = theta^{-n} 1{x_(n) <= theta} . 1{x_(1) >= 0}  => T = x_(n) sufficient"],
  cxd={"dominated_family": "for a family of mutually singular laws there is no common density to factor; sufficiency must be checked directly from the conditional law"},
  misuse=["forgetting the theta-free indicator 1{x_(1) >= 0} type factors, which belong in h",
          "reading a factorization through T and T' as making BOTH minimal sufficient -- it makes (T, T') sufficient"],
  apps=["the one-line route to 'sum T(X_i) is sufficient' for every exponential family",
        "in GLMs, shows the sufficient statistic for beta is X^T y (design matrix times response)"],
  related={"proof_route_for": ["sufficiency"], "required_by": ["exponential_family_sufficient_statistic", "sufficiency_principle"]},
  sources=["casella_berger_2e", "lehmann_casella_tpe"])

N("minimal_sufficiency", "definition", "sufficiency",
  "A sufficient statistic T is minimal sufficient if it is a function of every other sufficient statistic: the coarsest sufficient reduction of the data.",
  parseable="T sufficient, and for every sufficient T' there is a function phi with T = phi(T')",
  symbols={"T": "the minimal sufficient statistic (unique up to bijection)"},
  tcn="A minimality statement. Lehmann-Scheffe test: T(x) = T(y) iff the likelihood ratio f(x; theta)/f(y; theta) is constant in theta.",
  welldef="Exists for the dominated models in scope; unique up to a one-to-one transformation.",
  spec=["exponential family (minimal representation): sum_i T(X_i) is minimal sufficient",
        "iid Cauchy(theta, 1): the full order statistic is minimal sufficient -- no finite-dimensional reduction",
        "uniform(theta, theta+1): (X_(1), X_(n)) is minimal sufficient, 2-dimensional for a 1-dim parameter"],
  cxd={"minimality": "sum_i T(X_i) for a CURVED exponential family: still minimal sufficient but its dimension exceeds dim(theta) -- 'minimal' does not mean 'dimension = dim theta'"},
  misuse=["assuming minimal sufficient has the same dimension as the parameter",
          "confusing minimal sufficiency (coarsest sufficient) with completeness (a different property entirely)"],
  related={"commonly_confused_with": ["completeness_statistic"],
           "generalizes_from": ["sufficiency"]},
  sources=["lehmann_casella_tpe", "casella_berger_2e"])

N("ancillary_statistic", "definition", "sufficiency",
  "A statistic A is ancillary for theta if its distribution does not depend on theta.",
  parseable="the law of A(X) is the same for every theta in Theta",
  symbols={"A(X)": "the ancillary statistic, type: statistic with a theta-free law"},
  tcn="A statistic carrying no MARGINAL information about theta. It can still carry information about the PRECISION of an estimate (an ancillary configuration statistic) -- the basis of conditional inference.",
  welldef="A property of the statistic's marginal law under the model.",
  spec=["location family f(x - theta): the residuals (X_i - Xbar), or the range X_(n) - X_(1), are ancillary",
        "N(mu, 1): S^2 is ancillary for mu",
        "the sample size n itself, if random but theta-free, is ancillary"],
  cxd={"theta_free_law": "S^2 is NOT ancillary for sigma^2 in N(mu, sigma^2) -- its law (scaled chi-squared) depends on sigma^2. Ancillarity is always relative to a specified parameter."},
  misuse=["conditioning on a statistic that is only APPROXIMATELY ancillary and ignoring the approximation error",
          "treating an ancillary statistic as ignorable -- it should often be conditioned on (conditionality principle)"],
  related={"required_by": ["basu_theorem", "conditionality_principle"],
           "dual_of": ["sufficiency"]},
  sources=["cox_hinkley_theoretical_statistics", "casella_berger_2e"])

N("completeness_statistic", "definition", "sufficiency",
  "A statistic T is complete if E_theta[g(T)] = 0 for every theta implies g(T) = 0 almost surely (for every theta); bounded completeness restricts to bounded g.",
  parseable="( E_theta[g(T)] = 0  for all theta )  =>  g(T) = 0 a.s.  for all theta",
  symbols={"g": "an arbitrary (measurable, or bounded) function on the range of T",
           "the family of laws of T": "must be 'rich enough' to kill every unbiased-of-zero g"},
  tcn="A property of the FAMILY of distributions of T, not of a single distribution. It says the only unbiased estimator of 0 based on T is 0 -- which forces uniqueness of unbiased estimators that are functions of T.",
  welldef="A property of { law of T under P_theta : theta }.",
  spec=["full-rank exponential family: the natural sufficient statistic is complete (Laplace-transform uniqueness)",
        "uniform(0, theta): X_(n) is complete -- E_theta[g(X_(n))] = 0 for all theta forces g = 0 by differentiating in theta",
        "Bernoulli count sum X_i in {0,...,n}: complete (a degree-n polynomial with n+1 roots is zero)"],
  cxd={"completeness": "N(theta, theta^2) curved family: T = (sum X_i, sum X_i^2) is sufficient but E_theta[2(sum X_i)^2 - (n+1) sum X_i^2] = 0 for all theta with the bracket not identically 0 -- not complete. Lehmann-Scheffe then cannot certify a UMVUE."},
  misuse=["asserting completeness for a curved or parameter-restricted family",
          "confusing completeness (about the family of laws of T) with sufficiency (about conditional laws given T)"],
  related={"required_by": ["basu_theorem", "lehmann_scheffe_theorem", "exponential_family_completeness"],
           "commonly_confused_with": ["minimal_sufficiency"]},
  sources=["lehmann_casella_tpe", "lehmann_romano_tsh"])

N("bounded_completeness", "definition", "sufficiency",
  "T is boundedly complete if E_theta[g(T)] = 0 for all theta implies g(T) = 0 a.s. whenever g is bounded.",
  parseable="same as completeness but quantified only over BOUNDED g",
  symbols={"g": "a bounded measurable function"},
  tcn="Weaker than completeness. It is exactly what is needed for the identifiability of a structural function in some econometric models, and it suffices for one direction of Basu's theorem.",
  welldef="A property of the family of laws of T.",
  spec=["complete => boundedly complete (trivially)",
        "there exist boundedly-complete-but-not-complete families (Lehmann-Scheffe example)",
        "used in nonparametric IV: bounded completeness of the instrument given the endogenous regressor identifies the structural function"],
  cxd={"bounded_completeness": "a family rich enough to kill bounded unbiased-of-zero g but not unbounded ones -- rare in the parametric models here, central in nonparametric identification"},
  misuse=["treating bounded completeness as equivalent to completeness in general -- it is strictly weaker"],
  related={"generalizes_from": ["completeness_statistic"]},
  sources=["lehmann_romano_tsh", "newey_powell_2003"])

N("basu_theorem", "theorem", "sufficiency",
  "If T is a complete sufficient statistic for theta and A is an ancillary statistic, then T and A are independent (under every P_theta).",
  parseable="T complete sufficient,  A ancillary  =>  T _||_ A  under every P_theta",
  symbols={"T": "complete sufficient statistic", "A": "ancillary statistic",
           "T _||_ A": "statistical independence, for every theta"},
  tcn="An independence conclusion. Proof: for any event B for A, P_theta(A in B) is a constant c (ancillarity); E_theta[ P(A in B | T) - c ] = 0 for all theta; completeness forces P(A in B | T) = c a.s., i.e. A _||_ T.",
  hyps=["completeness_statistic", "sufficiency"],
  proof=("ancillarity gives a constant marginal; completeness upgrades 'constant in mean' to 'constant a.s.', i.e. independence",
         "completeness_statistic", "core",
         "validation/proof-checks.lean Stat.basu_step -- E[h(T)] = 0 for all theta and h = P(A in B|T) - c forces h = 0, hence independence"),
  regime="exact",
  spec=["N(mu, sigma^2) sigma^2 known: Xbar (complete sufficient for mu) _||_ S^2 (ancillary for mu). Gives Xbar _||_ S^2 with NO Jacobian computation.",
        "uniform(0, theta): X_(n) (complete sufficient) _||_ (X_(1)/X_(n), ..., X_(n-1)/X_(n)) (ancillary ratios)",
        "exponential(rate): sum X_i _||_ (X_1/sum X_i, ..., X_n/sum X_i) -- the normalized spacings"],
  cxd={"completeness_statistic": "drop completeness (curved family, or restricted parameter): T and A can be dependent even though A is ancillary and T is sufficient -- the standard Xbar _||_ S^2 shortcut is not available",
       "sufficiency": "if T is complete but not sufficient the argument's first step (constant conditional given T) has no reason to hold"},
  misuse=["using Basu to claim independence of two statistics neither of which is ancillary (S^2 and Xbar for UNKNOWN sigma^2 -- S^2 is not ancillary for sigma^2, so Basu does not apply to that pair; independence there is a separate fact)",
          "forgetting the conclusion is independence for EVERY theta, a strong statement"],
  apps=["the cleanest proof that the t-statistic's numerator and denominator are independent, hence that it has a t distribution",
        "deriving moments of ratios (E[X_(1)/X_(n)] etc.) by factoring the joint expectation"],
  related={"proof_route_for": ["normal_sample_mean_variance_independence"],
           "requires": ["completeness_statistic", "ancillary_statistic"]},
  sources=["basu_1955", "lehmann_casella_tpe"])
