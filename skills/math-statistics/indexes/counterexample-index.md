# Counterexample index (generated from results/*.yaml)

`dropped hypothesis`  ->  `witnessing counterexample`  (from which result)

- **admissibility** — dimension: 'Xbar is INADMISSIBLE for a normal mean in dimension d >= 3 -- the James-Stein estimator dominates it everywhere (james_stein). This is the famous Stein paradox.'
- **alternative_hypothesis** — chosen_in_advance: 'switching from two-sided to one-sided after seeing a positive Xbar doubles the effective alpha'
- **ancillary_statistic** — theta_free_law: 'S^2 is NOT ancillary for sigma^2 in N(mu, sigma^2) -- its law (scaled chi-squared) depends on sigma^2. Ancillarity is always relative to a specified parameter.'
- **asymptotic_efficiency** — for_all_theta: 'Hodges'' estimator (shrink Xbar to 0 when |Xbar| < n^{-1/4}) has asymptotic variance 0 at theta = 0 and sigma^2 elsewhere -- ''superefficient'' at one point, but its finite-sample maximum risk blows up near 0. Superefficiency on a Lebesgue-null set is the only way to beat I^{-1}, and it is not a free lunch.'
- **asymptotic_normality_estimator** — sqrt_n_rate: 'uniform(0, theta): n(theta - X_(n)) -> Exponential(1/theta), NOT a sqrt(n) normal limit -- ''asymptotic normality'' as defined here simply does not hold'
- **asymptotic_variance** — limit_of_scaled_variance: 'an estimator can be asymptotically N(0, Sigma) yet have Var(theta_hat_n) = inf for every n (a small-probability heavy tail that vanishes in the limit) -- report the asymptotic variance, not the (infinite) exact one'
- **basu_theorem** — completeness_statistic: 'drop completeness (curved family, or restricted parameter): T and A can be dependent even though A is ancillary and T is sufficient -- the standard Xbar _||_ S^2 shortcut is not available'
- **basu_theorem** — sufficiency: 'if T is complete but not sufficient the argument''s first step (constant conditional given T) has no reason to hold'
- **bayes_estimator** — proper_posterior: 'an improper prior can yield an improper posterior (e.g. pi(sigma) = 1/sigma with n = 0, or the Bayes/Neyman-Scott hierarchical models) -- then no Bayes estimator exists and reported ''posterior summaries'' are meaningless'
- **bayes_estimator** — finite_posterior_loss: 'squared-error loss with a Cauchy posterior: E[theta | x] does not exist; use the posterior median'
- **bayes_risk** — prior_proper_or_r_finite: 'an improper prior with infinite Bayes risk gives no minimizer; ''generalized Bayes'' rules (formal posterior mean) may still be admissible but are not Bayes in this sense'
- **bayes_rule_minimizes_bayes_risk** — inner_min_attained: 'if argmin_a E[L(theta,a)|x] is empty (loss not lower-semicontinuous, or infinite everywhere) there is no Bayes rule -- e.g. squared loss with a Cauchy posterior'
- **benjamini_hochberg_fdr** — many_true_nulls: 'if m_0 = m (all null) FDR control coincides with weak FWER control -- BH does not waste power there'
- **bernstein_von_mises** — true_parameter_interior: 'boundary parameter (a variance component at 0): the posterior piles up against the boundary and is NOT asymptotically normal; credible intervals do NOT have frequentist coverage'
- **bernstein_von_mises** — fixed_finite_dimension: 'in nonparametric / high-dimensional models Bernstein-von Mises can FAIL -- credible bands can badly under- or over-cover (Cox 1993, Freedman 1999); this is an active research area'
- **bernstein_von_mises** — identifiability: 'non-identified parameter: the posterior does not concentrate and coverage is prior-driven'
- **bias** — finite_mean: 'the MLE of the Cauchy scale from n=1 observation, or any estimator with heavy-tailed sampling distribution, may have no mean and hence no bias -- ''unbiased'' is then not even a well-posed question'
- **bias_of_sample_variance** — finite_variance: 'if sigma^2 = inf (Cauchy, t_2) the left side is infinite and ''unbiased for sigma^2'' is vacuous -- S^2 estimates nothing'
- **bonferroni_correction** — none_all_hypotheses_essential: 'the bound holds unconditionally; its weakness is conservatism (low power) when m is large or tests are positively dependent, not invalidity'
- **bootstrap** — smooth_functional: 'the maximum of a uniform(0, theta) sample: the bootstrap distribution of n(theta_hat - theta_hat*) does NOT converge to the true Exponential limit -- P(X_(n)* = X_(n)) -> 1 - 1/e, an atom that should not be there. The m-out-of-n bootstrap or subsampling fixes it.'
- **bootstrap** — limit_free_of_nuisance: 'bootstrapping a non-studentized statistic when the limit variance is unknown gives poor coverage -- bootstrap the STUDENTIZED (pivotal) statistic (bootstrap-t)'
- **bootstrap_consistency** — nondegenerate_influence_function: 'at a point where IF = 0 (e.g. g(theta_hat) with g''(theta) = 0) the sqrt(n) scaling is wrong and the bootstrap distribution degenerates'
- **bootstrap_consistency** — iid: 'the ordinary bootstrap is inconsistent for dependent data -- block bootstrap under mixing'
- **bounded_completeness** — bounded_completeness: 'a family rich enough to kill bounded unbiased-of-zero g but not unbounded ones -- rare in the parametric models here, central in nonparametric identification'
- **chi_squared_additivity** — independence: 'U and V = c U dependent: U + V = (1+c) U ~ (1+c) chi^2_a, NOT chi^2_{2a} -- df do not add without independence'
- **chi_squared_distribution** — standard_normal_summands: 'sum of squares of N(mu_i, 1) is NONCENTRAL chi^2_k(lambda), lambda = sum mu_i^2 -- the distribution under a testing alternative'
- **cochran_theorem** — sum_of_ranks_equals_n: 'if sum r_j < n the components need not be chi^2 or independent (there is ''leftover'' variation); if the A_j are not idempotent (a general quadratic form) the law is a weighted sum of chi^2_1''s'
- **coefficient_t_test** — gaussian_errors: 'with non-normal errors the exact t_{n-p} calibration is lost; for large n - p and bounded leverage it is asymptotically valid (t -> N(0,1) via the CLT for beta_hat)'
- **coefficient_t_test** — homoskedasticity: 'heteroskedastic errors make se(beta_hat_j) wrong -- use HC (White) robust SEs; the point estimate beta_hat_j stays unbiased'
- **coefficient_t_test** — no_collinearity: 'near-collinear predictors inflate ((X^TX)^{-1})_jj (the variance inflation factor), giving large SEs and unstable, individually-non-significant coefficients even when the group is jointly significant'
- **completeness_statistic** — completeness: 'N(theta, theta^2) curved family: T = (sum X_i, sum X_i^2) is sufficient but E_theta[2(sum X_i)^2 - (n+1) sum X_i^2] = 0 for all theta with the bracket not identically 0 -- not complete. Lehmann-Scheffe then cannot certify a UMVUE.'
- **conditionality_principle** — the_selector_is_ancillary: 'if which ''experiment'' is run depends on theta (informative sampling, response-adaptive designs), conditioning on it can bias the analysis -- the principle applies only to theta-ancillary selection'
- **confidence_set** — coverage_for_every_theta: 'the Wald interval for a binomial p has coverage that DIPS well below 1 - alpha for p near 0 or 1 and small n (down to ~0.8 for a nominal 0.95) -- the Clopper-Pearson or Wilson interval fixes it'
- **confidence_set_test_duality** — none_all_hypotheses_essential: 'the duality is a definitional equivalence; the only requirement is that the test have level alpha (or the CI have coverage 1 - alpha)'
- **conjugate_prior** — exists_and_is_flexible_enough: 'conjugate priors can be too rigid -- e.g. the Beta cannot be bimodal, the Gamma cannot have a heavy left tail; a genuinely different prior belief forces non-conjugate computation (MCMC)'
- **consistency** — finite_mean: 'Xbar for iid Cauchy: NOT consistent for the median theta -- Xbar is again Cauchy(theta, 1) for every n and never concentrates. The sample median IS consistent.'
- **consistency** — for_every_theta: 'an estimator hard-coded to return theta_0 is consistent AT theta_0 only; it is inconsistent everywhere else'
- **coverage_probability** — none_all_hypotheses_essential: 'coverage is defined for any procedure; the content is whether inf_theta beta_C >= 1 - alpha'
- **cramer_rao_lower_bound** — support_independent_of_theta: 'uniform(0, theta): the naive CRLB gives Var >= theta^2/n, but (n+1)/n X_(n) has variance ~ theta^2/n^2 -- it BEATS the ''bound'' by a factor n. The regularity failure makes the CRLB inapplicable, not merely loose.'
- **cramer_rao_lower_bound** — interchange_derivative_integral: 'if d/dtheta and int do not commute, Cov(theta_hat, U) != 1 + b'' and the bound is simply wrong'
- **cramer_rao_lower_bound** — unbiased_estimator: 'for biased estimators the bound has the (1 + b'')^2 numerator; quoting 1/(nI) for a biased estimator is a category error'
- **credible_interval** — proper_posterior: 'an improper posterior (from an improper prior with no finite normalizer) has no credible interval -- reported intervals are then meaningless'
- **credible_interval** — prior_is_defensible: 'a 95% credible interval inherits the prior; with a strong prior and little data it mostly reflects the prior, which must be disclosed'
- **crlb_attainment** — natural_parameter: 'estimating sigma (SD, not variance) in a normal model: even the UMVUE has variance strictly above 1/(nI) for every n, because sigma is a nonlinear function of the natural parameter -- the CRLB is unattainable, though approached as n -> inf'
- **cumulant_function** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **decision_rule** — measurable: 'a non-measurable rule has no well-defined risk -- excluded'
- **delta_method_standard_error** — g_prime_nonzero: 'g(theta) = theta^2 at theta = 0: g''(0) = 0, so the first-order delta method gives se = 0. The truth is n theta_hat^2 -> sigma^2 chi^2_1 -- a completely different (one-sided, non-normal) limit.'
- **delta_method_standard_error** — g_differentiable: 'g(theta) = |theta| at 0, or a max/min functional -- not differentiable, delta method inapplicable'
- **dominated_family** — dominated_family: 'Theta = R, P_theta = the point mass delta_theta. No single sigma-finite mu dominates all of them (an uncountable family of mutually singular measures). The likelihood is not well defined and the whole density-based apparatus (score, Fisher information, factorization) is unavailable.'
- **donsker_theorem** — iid: 'dependent data need mixing conditions and give a different (long-range vs short-range) limit'
- **dvoretzky_kiefer_wolfowitz** — iid: 'for dependent data the exponential rate degrades; block or spectral corrections are needed'
- **empirical_cdf** — none_all_hypotheses_essential: 'the ECDF is defined for any sample; its consistency (Glivenko-Cantelli) needs only iid -- no moment or smoothness assumption'
- **estimator** — valued_in_the_parameter_space: 'an ''estimator'' of a variance that can return negative values (a naive ANOVA variance-component estimate) is ill-posed as a point in Theta = (0, inf); it must be truncated or the model rethought'
- **exponential_family** — finite_k: 'the location-scale Cauchy family has no sufficient statistic of dimension < n (Pitman-Koopman-Darmois: among families with fixed support, exactly the exponential families admit a fixed-dimension sufficient statistic). All the clean exp-family results below then fail.'
- **exponential_family_completeness** — full_rank: 'the CURVED family N(theta, theta^2) (one parameter, T two-dimensional constrained to a parabola): the sufficient statistic is NOT complete -- there exist nonzero unbiased-of-zero functions. UMVUE claims via Lehmann-Scheffe then do not go through.'
- **exponential_family_moment_identities** — natural_parameter_space: 'at a boundary eta the interchange of d/deta and int fails; grad A need not equal E[T] (which may be infinite)'
- **exponential_family_sufficient_statistic** — exponential_family: 'outside exponential families the minimal sufficient statistic generally has dimension growing with n -- e.g. for iid Cauchy or uniform-location the full order statistic is minimal sufficient. Pitman-Koopman-Darmois makes this an iff (fixed support).'
- **f_distribution** — both_chi_squared: 'under the alternative, U is NONCENTRAL chi^2 -- the test statistic has a noncentral F distribution, which is how power is computed'
- **f_test_equality_of_variances** — normality: 'with heavy-tailed data (kurtosis > 3) the F-test for variances has true size FAR above nominal -- e.g. 0.20 at nominal 0.05 for t_5 data. This is the textbook example of a procedure that must not be used without near-exact normality.'
- **f_test_equality_of_variances** — independence: 'paired or correlated samples invalidate the chi^2 factorization'
- **fisher_information** — interchange_derivative_integral: 'if int f can''t be differentiated under the integral sign, E[U_1] != 0 and I(theta) = Var(U_1) != E[U_1^2]; the CRLB denominator and the information equality both fail'
- **fisher_information** — log_likelihood_smooth: 'uniform(0, theta): the naive I(theta) = 1/theta^2 gives the WRONG rate -- the MLE converges at rate n, not sqrt(n), so ''Var >= 1/(n I)'' is not the operative bound'
- **fisher_information_matrix** — fisher_information_positive_definite: 'the non-identified model N(alpha + beta, 1): I(alpha, beta) = [[1,1],[1,1]], singular -- not invertible, so I^{-1} does not exist and MLE-normality fails along the ridge'
- **fisher_information_positive_definite** — fisher_information_positive_definite: 'the non-identified N(alpha + beta, 1): I = [[1,1],[1,1]] has eigenvalue 0 along (1, -1). The MLE is not consistent for (alpha, beta) and I^{-1} does not exist. This hypothesis is the local (2nd-order) counterpart of the global identifiability hypothesis.'
- **gauss_markov_theorem** — linearity_and_correct_mean: 'if E[y] != X beta, OLS is biased and ''BLUE'' is moot'
- **glivenko_cantelli** — iid: 'for dependent data (a time series) F_hat_n still converges pointwise under ergodicity but the uniform / rate results need mixing conditions'
- **glivenko_cantelli** — the_class_is_not_too_rich: 'uniform convergence FAILS over an arbitrary class of sets -- e.g. over all finite sets, sup |P_hat_n(A) - P(A)| = 1 always (A = the sample). Half-lines (VC dim 1) are simple enough; this is why VC dimension matters.'
- **hajek_convolution_theorem** — regularity_of_the_estimator: 'drop it and Hodges'' estimator formally ''beats'' I^{-1} at a point -- but only by being non-regular (badly behaved just off that point), which the theorem''s hypothesis excludes'
- **identifiability** — identifiability: 'the model X ~ N(alpha + beta, 1) with theta = (alpha, beta): alpha and beta are not separately identifiable -- only their sum is. The MLE of (alpha, beta) is not consistent (it wanders along the ridge alpha + beta = const); the Fisher information matrix is singular. This is exactly why fisher_information_positive_definite is a separate hypothesis.'
- **iid_sample** — independence: 'if the X_i are positively correlated, the effective sample size is below n; a variance estimate treating them as independent is too small and confidence intervals undercover'
- **iid_sample** — identical_distribution: 'if each X_i has its own theta_i there is nothing to estimate without a link; the model is not a fixed-d parametric model'
- **information_equality** — interchange_derivative_integral: 'same failure as score_identity: without the second interchange the two formulas disagree and the ''sandwich'' variance A^{-1} B A^{-1} (with A != B) is the correct one -- this is exactly the misspecification-robust case'
- **information_equality** — log_likelihood_smooth: 'a non-twice-differentiable log-density (Laplace / double-exponential at its median) -- the second-derivative form does not exist though the variance form may'
- **interchange_derivative_integral** — interchange_derivative_integral: 'combine with a moving support (uniform(0, theta)) or heavy tails and the swap introduces a boundary term or diverges: E[score] != 0, so score_identity, information_equality, cramer_rao_lower_bound all fail. The robust replacement is the sandwich variance.'
- **karlin_rubin_theorem** — monotone_likelihood_ratio: 'Cauchy location, or any family without MLR: no UMP one-sided test exists -- different alternatives are best detected by different regions'
- **karlin_rubin_theorem** — one_sided: 'for H0: theta = theta_0 vs H1: theta != theta_0 (two-sided) there is no UMP test even under MLR; one restricts to unbiased tests (UMPU) and gets the equal-tailed two-sided test'
- **kde_bias_variance_tradeoff** — f_twice_differentiable: 'if f is only Lipschitz (one derivative) the best rate drops to n^{-2/3}; if f has a jump, to n^{-1/2} away from the jump and O(1) at it'
- **kde_bias_variance_tradeoff** — interior_x: 'at the boundary of the support the bias is O(h) not O(h^2) unless a boundary kernel is used -- boundary bias dominates the MISE if untreated'
- **kernel_density_estimator** — h_chosen_well: 'h too small: f_hat is a spiky mess (n bumps); h too large: f_hat is oversmoothed and hides real features (bimodality). There is no h that is right everywhere if f has varying curvature -- adaptive bandwidths address this.'
- **kernel_density_estimator** — f_smooth: 'if f has a discontinuity or a kink (uniform density''s edges, an exponential at 0) the KDE smears it and the bias there is O(1), not O(h^2) -- boundary bias'
- **large_sample_wald_interval** — true_parameter_interior: 'near a boundary the interval extends past the parameter space and undercovers -- use the LR interval, which respects the likelihood''s shape'
- **large_sample_wald_interval** — log_likelihood_smooth: 'non-regular model: the interval is invalid (wrong rate / wrong shape)'
- **le_cam_lan_theory** — regular_model: 'non-LAN models: uniform(0, theta) is locally asymptotically EXPONENTIAL (LAE, rate n); some models are locally asymptotically MIXED normal (LAMN, e.g. explosive AR, some diffusions). The Gaussian-shift reduction and its optimality conclusions do not apply.'
- **le_cam_lan_theory** — fixed_dimension: 'increasing-dimension / nonparametric models need the infinite-dimensional LAN theory and have their own (slower) efficiency bounds'
- **lehmann_scheffe_theorem** — completeness_statistic: 'curved family N(theta, theta^2): sufficient statistic not complete; there is no UMVUE -- two unbiased estimators of theta exist with crossing variance functions'
- **lehmann_scheffe_theorem** — sufficiency: 'an unbiased function of a complete-but-not-sufficient statistic is not improvable to the UMVUE by this route'
- **likelihood_function** — dominated_family: 'without a common dominating measure the products f(x_i; theta) for different theta are not comparable and ''maximize the likelihood'' is meaningless'
- **likelihood_principle** — accepting_both_premises_unrestrictedly: 'the principle is CONTESTED. Frequentists reject it (or reject one of Birnbaum''s premises) precisely because it outlaws pre-data error-rate control; Mayo (2014) and others dispute the theorem''s premises. This node states the principle and the theorem; it does not adjudicate.'
- **likelihood_ratio_test** — nested_models: 'Lambda requires Theta_0 subset Theta (nested); comparing NON-nested models needs AIC / BIC / Vuong''s test, not the LRT'
- **linear_algebra_background** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **linear_model** — full_column_rank: 'exact collinearity (a redundant dummy, x_2 = 2 x_1): X^T X is singular, beta is not identified, OLS has no unique solution'
- **linear_model** — errors_uncorrelated_homoskedastic: 'heteroskedasticity or autocorrelation: OLS stays unbiased but is no longer BLUE and the usual SEs are wrong (need GLS or robust SEs)'
- **linear_model** — gaussian_errors: 'only needed for EXACT t / F inference; drop it and the t/F statistics are asymptotically valid by the CLT (if n - p is large and the design is not too leveraged)'
- **local_asymptotic_minimax** — bowl_shaped_loss: 'for non-bowl-shaped (asymmetric) loss the bound changes and Anderson''s lemma does not apply directly'
- **log_likelihood** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **log_likelihood_smooth** — log_likelihood_smooth: 'Laplace(theta) = (1/2) e^{-|x - theta|}: log f = -|x - theta| + const is not differentiable at x = theta. The MLE is the sample median; it IS sqrt(n)-asymptotically normal (variance 1), but via a different argument (the score is a sum of sign(X_i - theta), not a smooth function) -- the C^3 route does not apply.'
- **loss_function** — loss_reflects_real_consequences: 'using squared-error loss when the real cost is a step function (a dam either overtops or not) gives a Bayes rule optimizing the wrong thing'
- **m_estimator** — estimating_equation_unbiased: 'if E_theta[psi(X; theta)] != 0 (a misspecified mean model, or Huber''s psi under an asymmetric error) the M-estimator converges to the wrong value theta* -- the solution of E[psi(X; theta*)] = 0, not theta'
- **maximum_likelihood_estimator** — argmax_exists: 'a two-component normal mixture with a free component variance: ell -> +inf as one component''s variance -> 0 around a single data point -- the global MLE does not exist (only a consistent local maximizer does)'
- **maximum_likelihood_estimator** — argmax_unique: 'a periodic likelihood (circular / phase parameter) can have multiple equal maxima'
- **mean_squared_error** — finite_second_moment: 'estimators with infinite variance (Cauchy-scale MLE at small n, ratio estimators near a zero denominator) have infinite MSE though they may be perfectly usable (finite median absolute error)'
- **mean_value_parametrization** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **method_of_moments** — moments_exist: 'Cauchy or t_2: no finite mean / variance, so mbar_1, mbar_2 do not converge and the method produces nonsense (prob_cauchy_no_mean). Estimate the median / IQR instead.'
- **method_of_moments** — moment_map_invertible: 'if two parameter values give the same first d moments the system is not solvable for theta'
- **minimal_sufficiency** — minimality: 'sum_i T(X_i) for a CURVED exponential family: still minimal sufficient but its dimension exceeds dim(theta) -- ''minimal'' does not mean ''dimension = dim theta'''
- **minimax_rate** — the_smoothness_class_is_correct: 'if f is actually rougher than assumed (fewer derivatives) the tuned estimator is inconsistent at the claimed rate; adaptive estimators (Lepski, wavelet thresholding) achieve the right rate WITHOUT knowing beta, at a log-factor cost'
- **minimax_rate** — no_extra_structure: 'under sparsity or a low-dimensional structure the effective d is much smaller and near-parametric rates return -- the entire premise of high-dimensional statistics'
- **mle_asymptotic_normality** — log_likelihood_smooth: 'Laplace location: the MLE (sample median) IS sqrt(n)-normal but with variance 1 (= 1/(4 f(0)^2)), reached by a non-Taylor argument -- the C^3 route fails though the conclusion''s SHAPE survives'
- **mle_asymptotic_normality** — support_independent_of_theta: 'uniform(0, theta): n(theta - theta_hat) -> Exponential -- rate n, non-normal limit; the theorem''s conclusion is simply false'
- **mle_asymptotic_normality** — true_parameter_interior: 'one-sided boundary (variance component = 0): the limit is a half-normal, not a normal'
- **mle_asymptotic_normality** — fisher_information_positive_definite: 'non-identified direction: I singular, I^{-1} undefined, no sqrt(n)-normal limit along the flat direction'
- **mle_consistency** — identifiability: 'N(alpha + beta, 1): (alpha_hat, beta_hat) is NOT consistent -- it wanders along the ridge alpha + beta = Xbar. Only the identified function alpha + beta is consistently estimated.'
- **mle_invariance** — bias_is_not_preserved: 'invariance is about the argmax, NOT about unbiasedness -- g(theta_hat) is typically biased even if theta_hat was unbiased (Jensen). E.g. sqrt(S^2) is a biased estimator of sigma.'
- **mle_score_equation** — true_parameter_interior: 'boundary MLE (uniform endpoint; a variance component estimated at 0; a probability estimated at exactly 0 or 1 in a small sample) -- U(theta_hat) != 0 and the score equation is the wrong tool'
- **mle_score_equation** — differentiability: 'non-smooth log-likelihood (Laplace location -> sample median): U is a step function, 0 is ''crossed'' not ''hit'''
- **monotone_likelihood_ratio** — two_sided: 'MLR gives UMP tests only for ONE-SIDED hypotheses; for H0: theta = theta_0 vs H1: theta != theta_0 even an MLR family has no UMP test'
- **mse_bias_variance_decomposition** — finite_second_moment: 'if Var(theta_hat) = inf the identity reads inf = inf + b^2 and carries no information'
- **multiple_testing_fwer** — the_family_is_well_defined: 'if the analyst does not fix the family in advance, the ''number of tests'' is ambiguous and any correction is gameable -- the multiplicity problem is really a pre-registration problem'
- **natural_parameter_space** — eta_in_interior: 'at a boundary point of H (if the family is defined there) A may be finite but not differentiable, or E[T] infinite -- the moment identities and PD information fail. ''Full-rank'' results need int(H).'
- **neyman_fisher_factorization** — dominated_family: 'for a family of mutually singular laws there is no common density to factor; sufficiency must be checked directly from the conditional law'
- **neyman_pearson_lemma** — simple_hypotheses: 'for a composite two-sided alternative H1: mu != 0 there is NO uniformly most powerful test -- the MP test against mu_1 > 0 (reject for large Xbar) has power BELOW alpha against mu_1 < 0. One must add an unbiasedness or invariance restriction.'
- **normal_mean_ci_known_variance** — sigma_known: 'sigma is essentially never known; using an estimate S in the z-interval undercovers at small n -- that is exactly why the t-interval exists (normal_mean_ci_unknown_variance)'
- **normal_mean_ci_known_variance** — normality: 'for non-normal data the coverage is approximate (1 - alpha + O(n^{-1/2}) by the CLT), reasonable for large n'
- **normal_mean_ci_unknown_variance** — normality: 'for skewed data at small n coverage departs from 1 - alpha (the t-interval is more robust than the chi^2 variance interval but not immune); a bootstrap-t or BCa interval can do better'
- **normal_mean_ci_unknown_variance** — iid: 'positively correlated observations => S underestimates Var(Xbar) => the interval is too narrow => undercoverage'
- **normal_sample_mean_variance_independence** — normality: 'exponential sample: Xbar and S^2 are POSITIVELY correlated (both driven up by a large observation). The one-sample t-statistic is then not exactly t-distributed and the t-test''s level is off at small n. Independence of Xbar and S^2 for all n is a *characterization* of the normal (Geary 1936, Lukacs 1942).'
- **normal_variance_ci** — normality: 'THIS PROCEDURE HAS NO ROBUSTNESS. For data with kurtosis != 3 (any non-normal shape) the coverage can be far from 1 - alpha even for large n, because (n-1)S^2/sigma^2 is not chi^2_{n-1} and its shape depends on the 4th moment. Use a bootstrap or a kurtosis-corrected interval.'
- **null_hypothesis** — specified_in_advance: 'choosing Theta_0 after seeing the data (testing the hypothesis the data suggest) invalidates the Type I error control -- ''the garden of forking paths'''
- **ols_distribution_under_normal_errors** — gaussian_errors: 'non-normal errors: beta_hat is only ASYMPTOTICALLY normal (CLT for (X^T X)^{-1} X^T eps, valid if no single row of X dominates -- max leverage h_ii -> 0); RSS/sigma^2 is not chi^2; the t/F p-values are approximate'
- **ols_distribution_under_normal_errors** — homoskedastic_uncorrelated: 'Cov(eps) = sigma^2 V != sigma^2 I: beta_hat ~ N(beta, sigma^2 (X^TX)^{-1} X^T V X (X^TX)^{-1}) -- the ''sandwich'' covariance; the naive (X^TX)^{-1} SEs are wrong'
- **one_sample_t_test** — normality: 'small skewed samples: the actual size deviates from alpha (up to ~0.08-0.10 for strongly skewed n ~ 10); large n rescues the LEVEL via the CLT (t -> N(0,1) regardless of parent) but not necessarily the power comparison'
- **one_sample_t_test** — iid: 'correlated data (repeated measures analysed as independent): the true SE of Xbar is underestimated, t is inflated, the test over-rejects badly'
- **one_way_anova_identity** — equal_group_variances: 'if the groups have different sigma_i^2 the F-statistic is not F-distributed under H0 (the Behrens-Fisher problem); Welch''s ANOVA adjusts the df'
- **one_way_anova_identity** — normality: 'the identity is algebraic and always holds; the chi^2 / F distribution of the pieces needs normal errors (or large n_i via the CLT)'
- **overall_f_test** — gaussian_errors: 'non-normal errors: F is only asymptotically q^{-1} chi^2_q (i.e. approximately F); robust or bootstrap versions exist'
- **overall_f_test** — homoskedasticity: 'heteroskedasticity distorts the F null distribution; use a heteroskedasticity-robust Wald test of R beta = r'
- **p_value** — simple_null_or_sup_taken: 'with a composite null and a non-pivotal statistic the sup is intractable; one uses a plug-in or a similar test, and the p-value is only approximately calibrated'
- **parameter_space** — admissibility_of_the_constraint: 'if the true p is 0 or 1 (on the boundary of [0,1]) the MLE is still consistent but sqrt(n)-asymptotic normality fails -- the limit is one-sided'
- **parametric_model** — finite_fixed_dimension: 'a location model with an unknown error density f is nonparametric in f; the CRLB and MLE-normality theory below does not apply to f without extra structure'
- **pearson_chi_squared_gof** — large_expected_counts: 'sparse tables (many E_j < 5): the chi^2 approximation fails; the statistic is not chi^2 and can be wildly miscalibrated'
- **pivot_method** — pivotal_quantity: 'with only an asymptotic pivot the coverage is 1 - alpha + O(n^{-1/2}) -- the method still ''works'' but is no longer exact'
- **pivot_method** — monotone_in_theta: 'if Q is not monotone in theta the inverted set can be a union of intervals or empty'
- **pivotal_quantity** — exactly_theta_free_law: 'an ASYMPTOTIC pivot (theta_hat - theta)/SE -> N(0,1) is only approximately pivotal at finite n -- the resulting Wald interval has approximate coverage'
- **posterior_mean_rule** — posterior_mean_exists: 'heavy-tailed posterior (Cauchy prior + one observation): E[theta | x] is undefined; the posterior-median rule (absolute loss) still works'
- **posterior_mean_rule** — squared_loss_appropriate: 'if over- and under-estimation have different costs (inventory, dosing) the posterior mean is the wrong summary -- use the rule for the actual asymmetric loss'
- **posterior_median_rule** — none_all_hypotheses_essential: 'the rule needs only that the posterior has a median (always true) and finite E|theta| for the risk to be finite -- weaker than the posterior-mean rule''s requirement'
- **power_function** — none_all_hypotheses_essential: 'the power function is defined for any test; the content is its shape'
- **prob_bernoulli** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_beta** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_binomial** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_cauchy_no_mean** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_cauchy_schwarz** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_cdf** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_characteristic_function** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_chebyshev_ineq** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_clt** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_conditional_distribution** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_conditional_expectation** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_continuous_mapping** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_conv_as** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_conv_d** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_conv_p** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_correlation** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_covariance** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_delta_method** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_expectation** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_expectation_linearity** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_exponential** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_gamma** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_hoeffding** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_iid** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_independence_factorization** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_independence_rv** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_jensen_ineq** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_joint_distribution** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_law_total_variance** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_levy_continuity** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_lotus** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_marginal_distribution** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_markov_ineq** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_mgf** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_moment** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_normal** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_normal_affine_closure** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_pdf** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_pmf** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_poisson** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_probability_space** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_random_variable** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_random_vector** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_slln** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_slutsky** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_standard_normal** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_tower_property** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_uniform_continuous** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_variance** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **prob_wlln** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **r_squared** — model_has_an_intercept: 'without an intercept the identity TSS = explained + RSS fails and ''R^2'' can be negative or > 1 depending on the software''s convention -- it is not interpretable'
- **r_squared** — same_response_and_no_transformation: 'R^2 for a model of y and a model of log y are not comparable; a high R^2 can accompany a badly misspecified model and a low R^2 a correct one (just noisy)'
- **ra_compactness** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_continuity** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_convex_function** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_differentiability** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_interchange_limit_integral** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_mean_value_theorem** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **ra_taylor_theorem** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **rao_blackwell_theorem** — sufficiency: 'condition on a NON-sufficient statistic T and E[theta_hat | T] depends on theta -- it is not a usable estimator. The variance would still drop, but you cannot compute the thing.'
- **rao_blackwell_theorem** — finite_variance: 'if Var(theta_hat) = inf the inequality is vacuous'
- **relative_efficiency** — same_rate: 'comparing a sqrt(n) estimator to an n-rate estimator (uniform endpoint) by an ARE is meaningless -- the ratio degenerates to 0 or inf'
- **residual_sum_of_squares** — correct_mean_model: 'if E[y] is not X beta (omitted nonlinearity) then E[RSS] > (n-p) sigma^2 -- RSS is inflated by the model bias (the basis of the lack-of-fit F-test)'
- **risk_function** — expectation_exists: 'a rule whose loss has infinite expectation under some theta (an estimator with infinite MSE) has undefined risk there and cannot be compared by risk'
- **sample_mean** — finite_mean: 'Xbar of iid Cauchy is again Cauchy(mu, 1) -- it does not concentrate and is a useless estimator of the center (use the median)'
- **sample_quantile** — positive_density_at_the_quantile: 'if f(Q(p)) = 0 (a gap in the support, or estimating an extreme quantile p -> 0) the sqrt(n)-normal limit fails -- the rate and shape change'
- **sample_quantile** — unique_quantile: 'a flat stretch of F at level p makes Q(p) an interval; the sample quantile is unstable there'
- **sample_variance** — finite_fourth_moment: 'Var(S^2) = (1/n)(mu_4 - (n-3)/(n-1) sigma^4); if E X^4 = inf then S^2 has infinite variance and is an unstable estimator (t_3, t_4 data)'
- **sandwich_variance** — finite_meat_and_bread: 'if B = E[psi psi^T] is infinite (heavy-tailed influence) the sandwich variance is infinite and no sqrt(n) normal limit holds'
- **scaled_sample_variance_chi_squared** — normality: 'for non-normal data (n-1)S^2/sigma^2 is not chi^2_{n-1}; its mean is still n-1 (bias_of_sample_variance holds generally) but its variance and shape depend on the kurtosis. The chi^2 variance interval is then badly miscalibrated -- far worse than the t interval for the mean is under non-normality.'
- **score_function** — log_likelihood_smooth: 'uniform(0, theta): ell(theta) = -n log theta on theta >= max x_i, so U(theta) = -n/theta is never 0 -- the MLE is at the boundary theta_hat = max x_i and the score equation has no solution. Score-based asymptotics (Fisher information, mle_asymptotic_normality) do not apply.'
- **score_identity** — support_independent_of_theta: 'uniform(0, theta): the support {0 <= x <= theta} moves with theta, so d/dtheta int_0^theta theta^{-1} dx has a boundary term; E_theta[U(theta)] = -1/theta != 0. The score is biased and every downstream result (CRLB, information equality) fails.'
- **score_identity** — interchange_derivative_integral: 'a family where the tails are too heavy for the dominated-derivative condition: the swap of d/dtheta and int is invalid and E[U] can be nonzero'
- **score_test** — fisher_information_positive_definite: 'singular I(theta_0) (a redundant restriction) makes the inverse undefined'
- **size_of_test** — sup_attained_or_controlled: 'if the null contains a nuisance parameter over which the rejection probability is not maximized at a computable point (Behrens-Fisher), the exact size is unknown and one uses an approximately-similar test'
- **statistic** — no_theta_dependence: 'sum_i (X_i - mu_0)^2 with a KNOWN mu_0 is a statistic; sum_i (X_i - mu)^2 with the unknown true mu is NOT a statistic -- it cannot be computed'
- **statistical_model** — none_all_hypotheses_essential: 'every listed hypothesis is used; see the block above'
- **sufficiency** — conditional_law_theta_free: 'Xbar alone is NOT sufficient for (mu, sigma^2) in a normal sample: given Xbar, the spread of the residuals still depends on sigma^2. Discarding S^2 loses information about sigma^2.'
- **sufficiency_principle** — none_all_hypotheses_essential: 'the principle is a stance; there is nothing to ''drop''. A method violating it (e.g. one depending on the order in which iid data arrived) would be regarded as defective.'
- **support_independent_of_theta** — support_independent_of_theta: 'uniform(0, theta): support (0, theta) depends on theta. Consequences: the score identity fails, the CRLB does not bind (the MLE max X_i beats it), the MLE converges at rate n and its limit is Exponential-not-Normal. The whole ''regular'' theory is replaced by ''non-regular'' (boundary) theory.'
- **t_statistic_distribution** — normality: 'for skewed data at small n the statistic is not t-distributed (both Xbar/S independence and the chi^2 denominator fail); the t-test''s actual level can be well off 5%. For large n the CLT + Slutsky rescue it (-> N(0,1)) REGARDLESS of the parent, which is why the t-test is ''robust in level'' asymptotically but not for small skewed samples.'
- **t_statistic_distribution** — iid: 'correlated observations inflate the true variance of Xbar while S^2 underestimates it -- the t-statistic is then too large and the test over-rejects'
- **test_function** — measurable: 'a non-measurable rejection region has no size -- excluded'
- **three_tests_asymptotically_equivalent** — regularity_all_three: 'at a boundary or in a non-regular model the three can have DIFFERENT (non-chi^2) limits and are no longer equivalent'
- **three_tests_asymptotically_equivalent** — far_from_local: 'against a FIXED (non-local) alternative all three have power -> 1 but at different rates; ''equivalent'' is a local statement'
- **true_parameter_interior** — true_parameter_interior: 'testing H0: sigma^2_between = 0 in a random-effects model -- the null is on the boundary of [0, inf). The LRT statistic -2 log Lambda is NOT chi^2_1 under H0; it is a 50:50 mixture of chi^2_0 and chi^2_1 (Self-Liang / Chernoff). Using the chi^2_1 critical value makes the test conservative.'
- **true_parameter_interior** — also: 'p_hat for a truly-degenerate coin (p = 0): sqrt(n)(p_hat - 0) does not converge to a two-sided normal'
- **two_sample_t_test** — equal_variances_for_the_pooled_version: 'n=5 with sigma_X = 1 and m=50 with sigma_Y = 4: the pooled t-test''s true size can be 0.15+ at nominal 0.05 -- pooling a large-sample large-variance group drags S_p^2 wrongly. Use Welch.'
- **two_sample_t_test** — normality: 'same small-sample skew caveat as the one-sample test'
- **two_sample_t_test** — independence_between_samples: 'matched/paired data must use the one-sample test on differences -- treating pairs as independent throws away the pairing and usually loses power'
- **umvue** — exists: 'for a curved exponential family the sufficient statistic is not complete and a UMVUE may fail to exist -- there can be two unbiased estimators neither dominating the other'
- **umvue** — unbiased_class_is_the_right_one: 'the UMVUE can be beaten in MSE by a biased estimator (James-Stein beats the UMVUE Xbar for a >=3-dim normal mean)'
- **unbiased_error_variance_estimator** — correct_mean_model: 'omitted nonlinearity inflates RSS, so sigma_hat^2 is biased UP -- the lack-of-fit test exploits this'
- **unbiased_estimator** — exists_at_all: 'e^{-2 lambda} Poisson: the (-1)^X estimator shows unbiasedness can pick out a ridiculous rule; the MLE e^{-2 Xbar} is biased but sensible'
- **wald_interval** — fisher_information_positive_definite: 'near a boundary (p near 0/1, a variance component near 0) the sampling distribution is skewed and one-sided; the symmetric Wald interval can extend outside the parameter space and badly undercover -- the score (Wilson) or LR interval is the fix'
- **wald_interval** — sqrt_n_normality: 'for a non-sqrt(n) estimator (uniform endpoint) the Wald recipe is simply invalid'
- **wald_test** — reparametrization: 'testing H0: theta = 1 vs H0: log theta = 0 gives DIFFERENT Wald statistics and p-values though the hypotheses are identical -- the Wald test is not invariant. The LRT and score test are.'
- **wald_test** — true_parameter_interior: 'the ''Hauck-Donner effect'': for a logistic coefficient far from 0 with a large SE, W can DECREASE as the effect grows, so the Wald test loses power exactly when the effect is large. Use the LRT.'
- **wald_test** — fisher_information_positive_definite: 'near-singular information (collinearity) makes W numerically unstable'
- **wilks_theorem** — true_parameter_interior: 'H0 on the boundary (sigma^2_component = 0, testing k vs k+1 mixture components): -2 log Lambda is NOT chi^2_r -- it is a mixture (e.g. 0.5 chi^2_0 + 0.5 chi^2_1) or has an even more complex limit. Using chi^2_r is conservative here.'
- **wilks_theorem** — log_likelihood_smooth: 'non-regular models (uniform endpoint): -2 log Lambda has a non-chi^2 limit (often exponential-related)'

