# Instance checks — index (generated from results/*.yaml)

Which concrete check backs each node. Kernel-checked cores and `decide`
instances live in `validation/proof-checks.lean` (see `proof-checks.md` for
the genuine-vs-instance split); numerical worksheets in
`validation/instance-checks.bc`. `cited` = established in the literature,
not re-checked here.


## prob_probability_space

- **lean_status:** `cited` — math-probability:probability_space
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_random_variable

- **lean_status:** `cited` — math-probability:random_variable
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_random_vector

- **lean_status:** `cited` — math-probability:random_variable
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_cdf

- **lean_status:** `cited` — math-probability:cdf
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_pmf

- **lean_status:** `cited` — math-probability:pmf
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_pdf

- **lean_status:** `cited` — math-probability:pdf
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_joint_distribution

- **lean_status:** `cited` — math-probability:joint_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_marginal_distribution

- **lean_status:** `cited` — math-probability:marginal_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_conditional_distribution

- **lean_status:** `cited` — math-probability:conditional_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_expectation

- **lean_status:** `cited` — math-probability:expectation
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_lotus

- **lean_status:** `cited` — math-probability:lotus
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_expectation_linearity

- **lean_status:** `cited` — math-probability:expectation_linearity
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_variance

- **lean_status:** `cited` — math-probability:variance
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_covariance

- **lean_status:** `cited` — math-probability:covariance
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_correlation

- **lean_status:** `cited` — math-probability:correlation
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_mgf

- **lean_status:** `cited` — math-probability:mgf
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_characteristic_function

- **lean_status:** `cited` — math-probability:characteristic_function
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_moment

- **lean_status:** `cited` — math-probability:moment
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_independence_rv

- **lean_status:** `cited` — math-probability:independence_random_variables
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_iid

- **lean_status:** `cited` — math-probability:iid
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_independence_factorization

- **lean_status:** `cited` — math-probability:independence_factorization
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_conditional_expectation

- **lean_status:** `cited` — math-probability:conditional_expectation_abstract
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_tower_property

- **lean_status:** `cited` — math-probability:tower_property
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_law_total_variance

- **lean_status:** `cited` — math-probability:law_of_total_variance
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_bernoulli

- **lean_status:** `cited` — math-probability:bernoulli_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_binomial

- **lean_status:** `cited` — math-probability:binomial_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_poisson

- **lean_status:** `cited` — math-probability:poisson_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_uniform_continuous

- **lean_status:** `cited` — math-probability:continuous_uniform_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_exponential

- **lean_status:** `cited` — math-probability:exponential_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_gamma

- **lean_status:** `cited` — math-probability:gamma_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_beta

- **lean_status:** `cited` — math-probability:beta_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_normal

- **lean_status:** `cited` — math-probability:normal_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_standard_normal

- **lean_status:** `cited` — math-probability:standard_normal
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_normal_affine_closure

- **lean_status:** `cited` — math-probability:normal_affine_closure
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_cauchy_no_mean

- **lean_status:** `cited` — math-probability:cauchy_no_mean
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_conv_as

- **lean_status:** `cited` — math-probability:convergence_almost_sure
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_conv_p

- **lean_status:** `cited` — math-probability:convergence_in_probability
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_conv_d

- **lean_status:** `cited` — math-probability:convergence_in_distribution
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_wlln

- **lean_status:** `cited` — math-probability:weak_law_large_numbers
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_slln

- **lean_status:** `cited` — math-probability:strong_law_large_numbers
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_clt

- **lean_status:** `cited` — math-probability:central_limit_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_levy_continuity

- **lean_status:** `cited` — math-probability:levy_continuity_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_slutsky

- **lean_status:** `cited` — math-probability:slutsky_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_continuous_mapping

- **lean_status:** `cited` — math-probability:continuous_mapping_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_delta_method

- **lean_status:** `cited` — math-probability:delta_method
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_markov_ineq

- **lean_status:** `cited` — math-probability:markov_inequality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_chebyshev_ineq

- **lean_status:** `cited` — math-probability:chebyshev_inequality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_jensen_ineq

- **lean_status:** `cited` — math-probability:jensen_inequality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_cauchy_schwarz

- **lean_status:** `cited` — math-probability:cauchy_schwarz_expectation
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## prob_hoeffding

- **lean_status:** `cited` — math-probability:hoeffding_inequality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_continuity

- **lean_status:** `cited` — math-real-analysis:continuity
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_differentiability

- **lean_status:** `cited` — math-real-analysis:differentiation
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_taylor_theorem

- **lean_status:** `cited` — math-real-analysis:taylor_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_mean_value_theorem

- **lean_status:** `cited` — math-real-analysis:mean_value_theorem
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_compactness

- **lean_status:** `cited` — math-real-analysis:compactness
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_convex_function

- **lean_status:** `cited` — math-real-analysis:convex_function
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ra_interchange_limit_integral

- **lean_status:** `cited` — math-real-analysis:interchange_limit_integral
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## linear_algebra_background

- **lean_status:** `cited` — linear-algebra:background-assumed
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## statistical_model

- **well-definedness:** Nothing to check beyond: each P_theta is a genuine probability measure. The model is an assumption about the data-generating mechanism, not a derived object.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## parametric_model

- **well-definedness:** d is fixed in advance and does not depend on the sample size n.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## parameter_space

- **well-definedness:** Constraints defining Theta (sigma^2 > 0, p in [0,1], a correlation in [-1,1], a covariance matrix positive definite) are part of the model specification.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## identifiability

- **well-definedness:** Identifiability is a property of the PARAMETRIZATION, not the data. A non-identifiable model can be repaired by quotienting Theta or adding a constraint.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## dominated_family

- **well-definedness:** mu is not unique but the model being dominated is a genuine property; the Radon-Nikodym derivative f(.; theta) is mu-a.e. unique.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## iid_sample

- **well-definedness:** The observations share the sample space and the sigma-algebra; 'identically distributed' fixes one theta.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## statistic

- **well-definedness:** T(X) is a random variable because T is measurable and X is; its law depends on theta through the law of X.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## likelihood_function

- **well-definedness:** Requires the dominated_family condition so the f(.; theta) are densities against one mu. L is defined up to the theta-free multiplicative constant coming from the choice of mu.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## log_likelihood

- **well-definedness:** Same theta-free additive constant ambiguity as L (from the choice of mu). Differences ell(theta_1) - ell(theta_2) are unambiguous.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## score_function

- **well-definedness:** Defined wherever ell is differentiable in theta. For an iid sample, U(theta) is a sum of n iid random vectors -- the structure the CLT needs.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## fisher_information

- **well-definedness:** Finite and positive under regularity; can be 0 (parameter locally invisible) or +inf.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## fisher_information_matrix

- **well-definedness:** A covariance matrix, hence symmetric PSD; the matrix analogue of the scalar I(theta).
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## score_identity

- **lean_status:** `core` — validation/proof-checks.lean Stat.score_mean_zero -- the algebra int (d log f) f = int df given int f = 1
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## information_equality

- **lean_status:** `core` — validation/proof-checks.lean Stat.information_equality -- given int f = 1 and the two interchanges, E[d2 log f] + E[(d log f)^2] = 0
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## support_independent_of_theta

- **well-definedness:** A property of the model as parametrized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## interchange_derivative_integral

- **well-definedness:** A regularity condition on the model; typically verified once via a dominating function.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## true_parameter_interior

- **well-definedness:** An assumption about the truth; unverifiable from data but often defensible (a variance is not exactly 0, a probability is not exactly 1).
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## fisher_information_positive_definite

- **well-definedness:** Finiteness rules out heavy-tailed scores; positive-definiteness rules out a locally flat / non-identified direction.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## log_likelihood_smooth

- **well-definedness:** A regularity condition on the model.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## exponential_family

- **well-definedness:** Well defined wherever A(eta) < inf; the family is the set of such densities. The MINIMAL representation (linearly independent T_j, 1) is unique up to affine change.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## natural_parameter_space

- **well-definedness:** H is determined by h, T, mu; convex and possibly all of R^k (Gaussian, Bernoulli) or a half-line (gamma with one parameter) or open (some heavy-carrier families).
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## cumulant_function

- **well-definedness:** A(eta) is finite exactly on H and determines the family given (h, T, mu).
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mean_value_parametrization

- **well-definedness:** Bijective on the interiors; the inverse is the gradient of the Legendre dual A*(mu) = sup_eta (eta.mu - A(eta)) (negative entropy).
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## exponential_family_moment_identities

- **lean_status:** `core` — validation/proof-checks.lean Stat.expfam_grad_A -- the quotient-rule algebra d/deta log(int h e^{eta T}) = E[T]
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## exponential_family_sufficient_statistic

- **lean_status:** `cited` — the discrete factorization identity is in proof-checks.lean Stat.factorization_discrete; the exp-family instance is immediate
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## exponential_family_completeness

- **lean_status:** `cited` — Laplace-transform uniqueness is the analytic input; CITED to Lehmann-Romano TSH Thm 4.3.1
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sufficiency

- **well-definedness:** Requires a regular conditional distribution to exist (it does for the models in scope). The full sample is trivially sufficient; the constant statistic is sufficient only if theta is known.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## neyman_fisher_factorization

- **lean_status:** `core` — validation/proof-checks.lean Stat.factorization_discrete -- the identity P(X=x|T=t) = g(t)h(x) / sum_{x': T(x')=t} g(t)h(x') with the g(t) cancelling
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## minimal_sufficiency

- **well-definedness:** Exists for the dominated models in scope; unique up to a one-to-one transformation.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ancillary_statistic

- **well-definedness:** A property of the statistic's marginal law under the model.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## completeness_statistic

- **well-definedness:** A property of { law of T under P_theta : theta }.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bounded_completeness

- **well-definedness:** A property of the family of laws of T.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## basu_theorem

- **lean_status:** `core` — validation/proof-checks.lean Stat.basu_step -- E[h(T)] = 0 for all theta and h = P(A in B|T) - c forces h = 0, hence independence
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## estimator

- **well-definedness:** Any statistic valued in the right space qualifies; quality is assessed by risk / bias / variance / consistency.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bias

- **well-definedness:** Defined wherever the estimator has a finite mean.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mean_squared_error

- **well-definedness:** Defined wherever theta_hat has a finite second moment.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mse_bias_variance_decomposition

- **lean_status:** `core` — validation/proof-checks.lean Stat.mse_decomp -- E[(t - c)^2] = E[(t - E t)^2] + (E t - c)^2 with c = theta, over the finite-support model of E
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## unbiased_estimator

- **well-definedness:** Requires a finite mean at every theta.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## consistency

- definition/axiom — see the type check
- **lean_status:** `cited` — the Chebyshev route (Var -> 0, bias -> 0 => consistency) is the finitary core in proof-checks.lean Stat.consistency_chebyshev
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## asymptotic_variance

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## asymptotic_normality_estimator

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## relative_efficiency

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## asymptotic_efficiency

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## umvue

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## cramer_rao_lower_bound

- **lean_status:** `core` — validation/proof-checks.lean Stat.crlb_cauchy_schwarz -- the step Cov(a, s)^2 <= Var(a) Var(s) at sample covariances (bc grid for the equality case), plus Cov = 1 + b' from the score identity
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## rao_blackwell_theorem

- **lean_status:** `core` — validation/proof-checks.lean Stat.rao_blackwell_var -- Var(X) = E[Var(X|T)] + Var(E[X|T]) instantiated so Var(E[X|T]) <= Var(X); the finitary total-variance identity
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## lehmann_scheffe_theorem

- **lean_status:** `cited` — the completeness step (difference of two unbiased functions of T is unbiased-of-zero, hence 0) is proof-checks.lean Stat.basu_step reused
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## crlb_attainment

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## method_of_moments

- **lean_status:** `cited` — the Chebyshev bound behind 'mbar_k -> m_k' is proof-checks.lean Stat.consistency_chebyshev
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## plug_in_principle

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## maximum_likelihood_estimator

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mle_score_equation

- **lean_status:** `core` — the 1-D statement (interior max => derivative zero) is proof-checks.lean Stat.interior_max_stationary via the MVT
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mle_invariance

- **lean_status:** `core` — argmax g(argmax L) = argmax L* is a set-identity; the 1-D monotone case is proof-checks.lean Stat.mle_invariance_monotone
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mle_consistency

- **lean_status:** `cited` — CITED -- Wald 1949; van der Vaart Asymptotic Statistics Thm 5.7. The identifiability => unique KL minimizer step is elementary; the uniform LLN is the analytic content.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## mle_asymptotic_normality

- **lean_status:** `cited` — CITED -- Cramer 1946; van der Vaart Thm 5.39. The Slutsky assembly N(0, A^{-1} B A^{-1}) with A = B = I is proof-checks.lean Stat.sandwich_reduces_when_info_equality; the deep CLT-for-the-score step is cited.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## m_estimator

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sandwich_variance

- **lean_status:** `core` — the reduction A = B = I(theta) under the information equality is proof-checks.lean Stat.sandwich_reduces_when_info_equality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bayes_estimator

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## posterior_mean_rule

- **lean_status:** `core` — validation/proof-checks.lean Stat.posterior_mean_completes_square -- E[(t - a)^2] = E[(t - Et)^2] + (Et - a)^2, minimized at a = E t; identical to Stat.mse_decomp
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## posterior_median_rule

- **lean_status:** `core` — the sign-change-at-the-median argument; instance check in instance-checks.bc
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## conjugate_prior

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## credible_interval

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bernstein_von_mises

- **lean_status:** `cited` — CITED -- van der Vaart Asymptotic Statistics Ch. 10; Le Cam. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## loss_function

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## risk_function

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## decision_rule

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## admissibility

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## minimax_rule

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bayes_risk

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bayes_rule_minimizes_bayes_risk

- **lean_status:** `core` — validation/proof-checks.lean Stat.bayes_rule_pointwise -- if g(x, a) >= g(x, a*(x)) for every a then E_X[g(X, delta(X))] >= E_X[g(X, a*(X))]; the interchange of min and integral
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## complete_class

- **lean_status:** `cited` — CITED -- Wald 1950; Ferguson Mathematical Statistics Ch. 2; Le Cam. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## james_stein

- **lean_status:** `cited` — CITED -- James-Stein 1961; Stein 1981 (SURE). The d=1 non-improvement and the risk-difference algebra are checked numerically in instance-checks.bc.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## chi_squared_distribution

- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## students_t_distribution

- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## f_distribution

- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## chi_squared_additivity

- **lean_status:** `core` — validation/proof-checks.lean Stat.chisq_mgf_add -- the exponent arithmetic -a/2 - b/2 = -(a+b)/2
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sample_mean

- definition/axiom — see the type check
- **lean_status:** `core` — E[(1/n) sum X_i] = (1/n) sum E[X_i]; the finite-support instance is proof-checks.lean Stat.sample_mean_linear
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sample_variance

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bias_of_sample_variance

- **lean_status:** `core` — validation/proof-checks.lean Stat.bias_sample_var -- the identity sum(x_i - xbar)^2 = sum(x_i - m)^2 - n(xbar - m)^2 over Int, then E gives (n-1)sigma^2 (reuses the centid/variance algebra from math-probability)
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## normal_sample_mean_variance_independence

- **lean_status:** `core` — the Basu route is one line given completeness+ancillarity; the Helmert-rotation route's key fact (orthogonal linear images of N(0, sigma^2 I) are independent) is proof-checks.lean Stat.gaussian_orthogonal_independent for a fixed small n via the covariance being zero + joint normality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## scaled_sample_variance_chi_squared

- **lean_status:** `core` — the rank/idempotency bookkeeping (projection I - (1/n) J has rank n-1, is idempotent) is proof-checks.lean Stat.centering_projection_rank for fixed n via trace = rank for a symmetric idempotent
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## t_statistic_distribution

- **lean_status:** `core` — the assembly is definitional; the three inputs are their own nodes. instance-checks.bc tabulates t_{n-1} quantiles vs the normal for small n
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## cochran_theorem

- **lean_status:** `cited` — the linear-algebra core (sum of idempotents = I with ranks summing to n => each idempotent, pairwise products 0) is proof-checks.lean Stat.cochran_idempotent for n <= 3 via decide on integer projection matrices
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## one_way_anova_identity

- **lean_status:** `core` — the vanishing cross term is proof-checks.lean Stat.anova_cross_term_zero -- sum_j (x_ij - xbar_i) = 0 kills it (Int arithmetic)
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## one_sample_t_test

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## two_sample_t_test

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## f_test_equality_of_variances

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## confidence_set

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## coverage_probability

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## pivotal_quantity

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## pivot_method

- **lean_status:** `core` — the event-identity / probability-transfer step is proof-checks.lean Stat.pivot_coverage -- P{theta in C} = P{a <= Q <= b} = 1 - alpha
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## confidence_set_test_duality

- **lean_status:** `core` — the slice/transpose identity {theta_0 in C(x)} = {x in A(theta_0)} and the probability transfer is proof-checks.lean Stat.ci_test_duality
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## normal_mean_ci_known_variance

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## normal_mean_ci_unknown_variance

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## normal_variance_ci

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## wald_interval

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## delta_method_standard_error

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## large_sample_wald_interval

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## null_hypothesis

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## alternative_hypothesis

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## test_function

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## size_of_test

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## power_function

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## type_i_error

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## neyman_pearson_lemma

- **lean_status:** `core` — validation/proof-checks.lean Stat.neyman_pearson_swap -- (phi* - phi)(f1 - k f0) >= 0 as a case split on sign, checked over a grid of (phi, phi', f0, f1) in {0,1}x{0,1}xNat, plus the universal integration step
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## monotone_likelihood_ratio

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## karlin_rubin_theorem

- **lean_status:** `cited` — the 'NP region is {T > c} independent of theta_1' step follows from MLR monotonicity; the beta nondecreasing lemma is proof-checks.lean Stat.mlr_power_monotone for a two-point family
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## likelihood_ratio_test

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## wilks_theorem

- **lean_status:** `cited` — CITED -- Wilks 1938; van der Vaart Thm 16.7. The 'difference of two quadratic forms is chi^2_{d-r}' idempotency step reuses proof-checks.lean Stat.cochran_idempotent.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## wald_test

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## score_test

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## three_tests_asymptotically_equivalent

- **lean_status:** `cited` — CITED -- Engle 1984 (Handbook of Econometrics Ch. 13); van der Vaart Ch. 16. The shared quadratic form is the content.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## p_value

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## p_value_uniform_under_null

- **lean_status:** `core` — the PIT step F(T) ~ Uniform is cited to math-probability:probability_integral_transform; the corollary is one line
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## pearson_chi_squared_gof

- **lean_status:** `cited` — CITED -- Pearson 1900; Fisher 1924 (df correction); Cramer 1946. X^2 as the multinomial score statistic is the modern view.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## multiple_testing_fwer

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bonferroni_correction

- **lean_status:** `core` — the union bound P(union A_i) <= sum P(A_i) is cited to math-probability:boole_inequality; the arithmetic m_0 alpha/m <= alpha is proof-checks.lean Stat.bonferroni_bound
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## benjamini_hochberg_fdr

- **lean_status:** `cited` — CITED -- Benjamini-Hochberg 1995; Storey 2002 (the martingale proof). The step-up threshold arithmetic is checked on a fixed p-value vector in instance-checks.bc.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## empirical_cdf

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## order_statistic

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sample_quantile

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## glivenko_cantelli

- **lean_status:** `cited` — CITED -- Glivenko 1933, Cantelli 1933; van der Vaart Thm 19.1. The monotonicity-sandwich reduction to finitely many pointwise SLLNs is the elementary content.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## dvoretzky_kiefer_wolfowitz

- **lean_status:** `cited` — CITED -- Dvoretzky-Kiefer-Wolfowitz 1956; Massart 1990 (tight constant). The connection to Hoeffding's bound on each F_hat_n(x) - F(x) is the entry point.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## plug_in_estimator

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bootstrap

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## bootstrap_consistency

- **lean_status:** `cited` — CITED -- Bickel-Freedman 1981 (the mean); van der Vaart-Wellner 1996 Ch. 3.6 (general). Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## kernel_density_estimator

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## kde_bias_variance_tradeoff

- definition/axiom — see the type check
- **lean_status:** `core` — validation/proof-checks.lean Stat.kde_amise_optimal_h -- minimizing a h^4 + b/(nh) over h gives h ~ (b/(4an))^{1/5}, and the minimized value ~ n^{-4/5} (the exponent arithmetic)
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## linear_model

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ordinary_least_squares

- definition/axiom — see the type check
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## normal_equations

- **lean_status:** `core` — validation/proof-checks.lean Stat.normal_equations_stationary -- grad || y - X b ||^2 = -2 X^T(y - Xb), checked for a fixed 2x2 design via decide on integer matrices
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ols_is_projection

- **lean_status:** `core` — validation/proof-checks.lean Stat.hat_matrix_idempotent -- H^2 = H and tr(H) = p for a fixed integer 3x2 design via decide
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## residual_sum_of_squares

- definition/axiom — see the type check
- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## unbiased_error_variance_estimator

- **lean_status:** `core` — validation/proof-checks.lean Stat.rss_expectation -- E[eps^T(I-H)eps] = sigma^2 tr(I-H); the trace identity tr(I - H) = n - p for a fixed integer design via decide, reusing Stat.bias_sample_var's E[quadratic form] algebra
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## gauss_markov_theorem

- **lean_status:** `core` — validation/proof-checks.lean Stat.gauss_markov_cross_term -- the cross term c^T (X^T X)^{-1} X^T d = 0 given X^T d = 0, and Var = Var_OLS + sigma^2 ||d||^2 >= Var_OLS, for a fixed integer design via decide
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## ols_distribution_under_normal_errors

- **lean_status:** `cited` — the independence-of-orthogonal-Gaussian-projections step is proof-checks.lean Stat.gaussian_orthogonal_independent, reused from the normal_sample_mean_variance_independence node
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## coefficient_t_test

- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## overall_f_test

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## r_squared

- definition/axiom — see the type check
- **lean_status:** `core`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## partitioned_regression

- **lean_status:** `cited` — the block-elimination identity is proof-checks.lean Stat.fwl_block_elimination for a fixed integer design via decide
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## likelihood_principle

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## sufficiency_principle

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## conditionality_principle

- **lean_status:** `cited`
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## le_cam_lan_theory

- **lean_status:** `cited` — CITED -- Le Cam 1960; van der Vaart Asymptotic Statistics Ch. 7-9. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## hajek_convolution_theorem

- **lean_status:** `cited` — CITED -- Hajek 1970; van der Vaart Ch. 8. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## local_asymptotic_minimax

- **lean_status:** `cited` — CITED -- Hajek 1972; van der Vaart Ch. 8. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## donsker_theorem

- **lean_status:** `cited` — CITED -- Donsker 1952; van der Vaart-Wellner 1996. Not formalized.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)

## minimax_rate

- **lean_status:** `cited` — CITED -- Stone 1980, 1982; Tsybakov Introduction to Nonparametric Estimation. The h ~ n^{-1/(2 beta + d)} balancing is the KDE-node computation; the lower bound is the cited analytic content.
- **bc:** (no numeric worksheet; the type check and specialization cases apply)
