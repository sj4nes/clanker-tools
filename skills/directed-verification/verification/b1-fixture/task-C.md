Write verification for the code in `subject3/`.

What it is supposed to do is specified in `subject3/SPEC.md`.

The risky areas are these, and your verification should aim at them: inputs the
parser is required to reject; the boundaries where one unit rolls over into the
next, and zero; values that format differently from the way they were written;
and whether parsing and formatting are inverses of each other.

Put your work in the same directory. Report what you did.
