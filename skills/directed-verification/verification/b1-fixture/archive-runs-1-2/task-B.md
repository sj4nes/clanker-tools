Write verification for the `round_half_up` function in `subject/money.py`.

It must be able to fail: before you finish, confirm it actually fails against a
wrong implementation, then restore the correct one.

Put it in the same directory. Report what you did.
