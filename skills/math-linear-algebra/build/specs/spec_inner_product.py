"""Inner product spaces: Cauchy-Schwarz, orthogonality, Gram-Schmidt,
projection, least squares, QR, and the adjoint."""
from nodespec import N

N("inner_product", "structure", "inner_product",
  "An inner product on a vector space V over F in {R, C} is a map <.,.>: V x V -> F that is linear in the FIRST slot, conjugate-symmetric (<y,x> = conj(<x,y>)), and POSITIVE DEFINITE (<x,x> > 0 for x != 0).",
  parseable="<ax+by, z> = a<x,z> + b<y,z>;  <y,x> = conj(<x,y>);  <x,x> > 0 for x != 0",
  symbols={"<x,y>": "type: element of F", "F": "restricted to R or C"},
  tcn="Well-formed. Conjugate-symmetry forces <x,x> = conj(<x,x>), i.e. REAL, without which 'positive' would be meaningless -- so the two axioms are linked, not independent. Over R conjugation is trivial and the form is symmetric bilinear.",
  hyps=["F = R or C", "positive definiteness"],
  field_scope="real_or_complex",
  spec=["F^n with <x,y> = sum_i x_i conj(y_i) = y^* x: the standard inner product",
        "weighted: <x,y> = y^* W x for a positive definite W (positive_definite) -- every inner product on F^n is of this form",
        "continuous functions on [0,1] with <f,g> = integral f conj(g): infinite-dimensional, and the setting of Fourier series"],
  cxd={"positive_definiteness": "the Minkowski form <x,y> = x_1y_1 - x_2y_2 on R^2 is symmetric and bilinear but indefinite: the null vector (1,1) has <x,x> = 0 without being 0. There is then no norm, no Cauchy-Schwarz in the usual direction, and no orthogonal decomposition -- Lorentzian geometry, not Euclidean",
       "conjugate_symmetry": "the plain bilinear form sum x_i y_i on C^2 gives <(1,i),(1,i)> = 1 - 1 = 0 with the vector nonzero (see inner_product_convention)",
       "the_field": "over F_2, x^T x = sum x_i^2 = sum x_i, so (1,1) is 'self-orthogonal'. No ordered field, no positivity, no inner product"},
  misuse=["calling a symmetric bilinear form an inner product without checking definiteness",
          "using the physicist's (second-slot-linear) convention alongside this one"],
  apps=["the L^2 inner product underlies Fourier analysis, least squares, and the Hilbert-space formulation of quantum mechanics",
        "the covariance inner product <X,Y> = E[XY] makes uncorrelated random variables orthogonal, which is why regression is projection"],
  related={"required_by": ["induced_norm", "cauchy_schwarz", "orthogonality"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("inner_product_space", "structure", "inner_product",
  "A vector space over R or C together with a chosen inner product. Finite-dimensional real ones are Euclidean spaces; complete infinite-dimensional ones are Hilbert spaces (OUT OF SCOPE).",
  parseable="(V, <.,.>) with V an F-vector space, F in {R, C}, and <.,.> an inner product",
  symbols={"(V, <.,.>)": "the structure; the inner product is DATA, not intrinsic to V"},
  tcn="Well-formed. The inner product is extra structure: the same V carries many, and results about 'the' orthogonal complement or 'the' adjoint are relative to the chosen one.",
  hyps=["F = R or C"],
  field_scope="real_or_complex",
  spec=["R^n with the dot product: Euclidean n-space",
        "C^n with y^*x: unitary space",
        "F^{m x n} with <A,B> = tr(B^*A): the Frobenius inner product, which is what matrix_norms uses"],
  cxd={"completeness_is_NOT_assumed": "finite-dimensional inner product spaces are automatically complete; infinite-dimensional ones need not be, and every theorem here that uses orthogonal_decomposition fails for a non-closed subspace of an incomplete space. That is the Hilbert-space boundary this capsule does not cross"},
  misuse=["treating orthogonality as intrinsic to V: it depends entirely on the chosen inner product, and a change of inner product is a change of geometry"],
  related={"required_by": ["cauchy_schwarz", "orthogonality"]},
  sources=["axler_lada_4e"], status="reviewed")

N("induced_norm", "definition", "inner_product",
  "||v|| = sqrt(<v,v>), well-defined because <v,v> is real and nonnegative. It satisfies ||v|| = 0 iff v = 0, ||av|| = |a| ||v||, and the triangle inequality.",
  parseable="||v|| := sqrt(<v,v>);  ||v|| = 0 <=> v = 0;  ||av|| = |a| ||v||;  ||u+v|| <= ||u|| + ||v||",
  symbols={"||v||": "type: nonnegative real number", "|a|": "modulus of the scalar"},
  tcn="Well-formed: the square root is of a nonnegative REAL, guaranteed by conjugate-symmetry (realness) and positive definiteness (nonnegativity). Both axioms are used just to make the definition type-check.",
  hyps=["<.,.> an inner product"],
  field_scope="real_or_complex",
  spec=["F^n standard: ||x|| = sqrt(sum |x_i|^2), the Euclidean norm",
        "||av|| = |a| ||v|| uses |a|, not a -- over C this matters: ||iv|| = ||v||"],
  cxd={"positive_definiteness": "for an indefinite form sqrt(<v,v>) is not even real for some v (Minkowski again)",
       "not_every_norm_is_induced": "the 1-norm and infinity-norm on R^2 are genuine norms but come from NO inner product -- they fail the parallelogram_law, which is exactly the characterisation"},
  misuse=["applying Cauchy-Schwarz or orthogonal projection in a normed space whose norm is not induced by an inner product (e.g. L^1) -- neither is available there"],
  related={"required_by": ["cauchy_schwarz", "orthonormal_basis", "best_approximation", "matrix_norms"]},
  sources=["axler_lada_4e"], status="reviewed")

N("cauchy_schwarz", "theorem", "inner_product",
  "|<u,v>| <= ||u|| ||v||, with EQUALITY if and only if u and v are linearly dependent.",
  parseable="|<u,v>| <= ||u|| ||v||;  equality  <=>  u, v dependent",
  symbols={"|<u,v>|": "the modulus of the inner product"},
  tcn="Well-formed. The equality clause is part of the theorem, not an afterthought: it is what makes Cauchy-Schwarz a rigidity statement and what gives the correlation bound its interpretation.",
  hyps=["<.,.> an inner product (positive definite)"],
  field_scope="real_or_complex",
  proof=("for v != 0, expand 0 <= ||u - (<u,v>/||v||^2) v||^2 -- i.e. the squared norm of u minus its projection onto v -- and rearrange. Equality holds iff that vector is 0, i.e. u is a multiple of v", "induced_norm", "core", "LinAlg.cauchy_schwarz_int (Int instance: (sum a_i b_i)^2 <= (sum a_i^2)(sum b_i^2) for n = 2, 3 by decide); the real-coefficient statement is CITED"),
  spec=["u = v: equality, both sides ||v||^2",
        "v = 0: both sides 0, equality with the pair trivially dependent",
        "F^n standard: (sum |x_i y_i|)^2 <= (sum |x_i|^2)(sum |y_i|^2), the classical inequality",
        "random variables with <X,Y> = E[XY]: |E[XY]| <= sqrt(E[X^2]E[Y^2]), hence |corr| <= 1"],
  cxd={"positive_definiteness": "for the Minkowski form on R^2 the inequality REVERSES on timelike vectors -- the reverse Cauchy-Schwarz inequality of special relativity. So it is definiteness, not bilinearity, that gives the direction",
       "the_proof_needs_only_nonnegativity": "positive SEMI-definiteness suffices for the inequality; strict definiteness is needed only for the equality clause"},
  misuse=["stating it as <u,v> <= ||u|| ||v|| without the modulus (false when the inner product is negative)",
          "using it in a normed space with no inner product"],
  apps=["the correlation coefficient bound |corr(X,Y)| <= 1 and its equality case (perfect linear dependence) -- the inequality statistics rests on",
        "the Cramer-Rao bound in math-statistics is Cauchy-Schwarz applied to the score and the estimator"],
  related={"required_by": ["triangle_inequality", "isometry_characterisation"]},
  sources=["axler_lada_4e", "steele_cauchy_schwarz"], status="reviewed")

N("triangle_inequality", "corollary", "inner_product",
  "||u + v|| <= ||u|| + ||v||, with equality iff one of u, v is a NONNEGATIVE real multiple of the other.",
  parseable="||u+v|| <= ||u|| + ||v||;  equality <=> u = cv or v = cu with c >= 0 real",
  symbols={"c": "a nonnegative real scalar in the equality case"},
  tcn="Well-formed. Note the equality condition is strictly stronger than in Cauchy-Schwarz: dependence is not enough, the multiple must be NONNEGATIVE REAL.",
  hyps=["induced norm"],
  field_scope="real_or_complex",
  proof=("||u+v||^2 = ||u||^2 + 2 Re<u,v> + ||v||^2 <= ||u||^2 + 2|<u,v>| + ||v||^2 <= (||u||+||v||)^2 by Cauchy-Schwarz", "cauchy_schwarz", "core", "LinAlg.triangle_from_cs (Int instance, n = 2)"),
  spec=["v = -u: ||0|| = 0 <= 2||u||, strict unless u = 0 -- the multiple -1 is negative, so no equality, illustrating the sharper condition",
        "the reverse triangle inequality | ||u|| - ||v|| | <= ||u - v|| follows by substitution"],
  cxd={"the_equality_condition": "u and v = -u are dependent (so Cauchy-Schwarz is tight) but ||u + v|| = 0 < 2||u||. Dependence gives equality in Cauchy-Schwarz, not in the triangle inequality"},
  misuse=["quoting 'equality iff dependent' for the triangle inequality"],
  related={"special_case_of": ["cauchy_schwarz"]},
  sources=["axler_lada_4e"], status="reviewed")

N("parallelogram_law", "identity", "inner_product",
  "||u+v||^2 + ||u-v||^2 = 2||u||^2 + 2||v||^2. It CHARACTERISES induced norms: a norm satisfies it if and only if it comes from an inner product (Jordan-von Neumann).",
  parseable="||u+v||^2 + ||u-v||^2 = 2||u||^2 + 2||v||^2;  norm induced by an inner product <=> this law holds",
  symbols={"u, v": "arbitrary vectors"},
  tcn="Well-formed. The identity direction is a one-line expansion; the CHARACTERISATION (the converse) is cited, not proved here.",
  hyps=["induced norm for the identity; a general norm for the characterisation"],
  field_scope="real_or_complex",
  proof=("expand both squared norms via <u+v,u+v> and <u-v,u-v> and add: the cross terms cancel. The Jordan-von Neumann converse (recovering the inner product by polarisation and verifying bilinearity) is CITED", "induced_norm", "core", "LinAlg.parallelogram (Int instance, n = 2)"),
  spec=["v = 0: reduces to 2||u||^2 = 2||u||^2",
        "u perp v: combined with pythagorean_theorem it gives ||u+v|| = ||u-v||"],
  cxd={"the_norm_being_induced": "on R^2 with the 1-norm, take u = (1,0), v = (0,1): ||u+v||_1 = 2 and ||u-v||_1 = 2, so the left side is 8, while the right side is 2 + 2 = 4. The 1-norm comes from no inner product -- this is the standard witness"},
  misuse=["assuming every norm supports orthogonality and projection: the parallelogram law is the exact test, and it fails for all p-norms except p = 2"],
  related={"required_by": ["polarisation_identity"]},
  sources=["axler_lada_4e", "jordan_von_neumann_1935"], status="reviewed")

N("polarisation_identity", "identity", "inner_product",
  "Over R: <u,v> = (||u+v||^2 - ||u-v||^2)/4. Over C: <u,v> = (1/4) sum_{k=0}^3 i^k ||u + i^k v||^2. The inner product is RECOVERABLE from the norm; the factor 1/4 is why characteristic 2 is excluded.",
  parseable="R: <u,v> = (||u+v||^2 - ||u-v||^2)/4;  C: <u,v> = (1/4) sum_{k=0}^3 i^k ||u + i^k v||^2",
  symbols={"i^k": "the four fourth roots of unity, k = 0,1,2,3"},
  tcn="Well-formed over R and C. The division by 4 requires 2 to be invertible -- hence the characteristic_not_two edge. Over a field of characteristic 2 no polarisation exists and symmetric bilinear forms are not determined by their quadratic forms.",
  hyps=["char F != 2", "F = R or C"],
  field_scope="char_not_2",
  proof=("expand each squared norm and collect; over C the four terms isolate the real and imaginary parts of <u,v> separately", "parallelogram_law", "core", "LinAlg.polarisation_real (Int instance with the 2*expr/2 idiom for the 1/4 factor)"),
  spec=["u = v over R: (||2u||^2 - 0)/4 = ||u||^2, correct",
        "the real formula applied to a COMPLEX inner product recovers only Re<u,v>, which is why the complex version needs four terms"],
  cxd={"characteristic_not_two": "over F_2 the map q(x) = x^T A x satisfies q(u+v) = q(u) + q(v) (all cross terms double to zero), so q is ADDITIVE and carries none of the bilinear information. Symmetric and alternating forms become indistinguishable -- the single sharpest consequence of char 2 in this capsule",
       "using_the_real_formula_over_C": "gives Re<u,v> only, losing the imaginary part entirely"},
  misuse=["applying the real polarisation formula in a complex space",
          "symmetrising a matrix as (A + A^T)/2 over a field of characteristic 2"],
  related={"required_by": ["isometry_characterisation", "quadratic_form"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthogonality", "definition", "inner_product",
  "u perp v if <u,v> = 0. A set is orthogonal if its elements are pairwise orthogonal, orthonormal if in addition every element has norm 1. Orthogonality is SYMMETRIC (since <v,u> = conj(<u,v>)).",
  parseable="u perp v  <=>  <u,v> = 0;  symmetric;  0 perp everything",
  symbols={"perp": "the orthogonality relation, type: symmetric relation on V"},
  tcn="Well-formed and symmetric by conjugate-symmetry. Note 0 is orthogonal to everything including itself -- the only vector with that property, by positive definiteness.",
  hyps=["an inner product space"],
  field_scope="real_or_complex",
  spec=["the standard basis of F^n is orthonormal",
        "in R^2, (1,1) perp (1,-1)",
        "uncorrelated centred random variables are orthogonal under <X,Y> = E[XY]"],
  cxd={"positive_definiteness": "over F_2 with the form sum x_iy_i, the vector (1,1) satisfies <x,x> = 1+1 = 0, so it is orthogonal to ITSELF while nonzero. Every orthogonality argument -- independence of orthogonal sets, orthogonal decomposition, projection -- collapses. This is why the four_subspaces theorem is stated over any field in DIMENSIONS only, with orthogonality deferred to adjoint_kernel_image"},
  misuse=["assuming orthogonal implies independent without excluding 0 (orthogonal_implies_independent needs the vectors nonzero)",
          "importing orthogonality into a general field"],
  related={"required_by": ["orthonormal_basis", "orthogonal_complement", "orthogonal_projection"]},
  sources=["axler_lada_4e"], status="reviewed")

N("pythagorean_theorem", "proposition", "inner_product",
  "If u perp v then ||u+v||^2 = ||u||^2 + ||v||^2. Over R the converse holds; over C the converse gives only Re<u,v> = 0.",
  parseable="u perp v  =>  ||u+v||^2 = ||u||^2 + ||v||^2;  over R the converse holds, over C it gives Re<u,v> = 0",
  symbols={"Re<u,v>": "the real part"},
  tcn="Well-formed. The asymmetry between R and C is genuine: expanding gives ||u+v||^2 = ||u||^2 + 2Re<u,v> + ||v||^2, and only the REAL PART appears.",
  hyps=["u perp v for the forward direction"],
  field_scope="real_or_complex",
  proof=("expand <u+v, u+v> using linearity and conjugate-symmetry; the cross terms sum to 2 Re<u,v>", "induced_norm", "core", "LinAlg.pythagoras (Int instance, n = 2)"),
  spec=["extends by induction to any finite orthogonal family: ||sum v_i||^2 = sum ||v_i||^2",
        "with u = P_U v and v - P_U v, it gives ||v||^2 = ||P_U v||^2 + ||v - P_U v||^2 -- the decomposition behind best_approximation and behind the ANOVA sum-of-squares identity"],
  cxd={"the_complex_converse": "in C^1 take u = 1 and v = i: <u,v> = conj(i) = -i, so Re<u,v> = 0 and ||u+v||^2 = |1+i|^2 = 2 = 1 + 1. Pythagoras holds, yet <u,v> != 0 so the vectors are NOT orthogonal"},
  misuse=["inferring orthogonality from the Pythagorean identity over C"],
  related={"required_by": ["bessel_inequality", "best_approximation"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthogonal_implies_independent", "proposition", "inner_product",
  "An orthogonal set of NONZERO vectors is linearly independent. Hence any orthonormal set has at most dim V elements.",
  parseable="(v_i) pairwise orthogonal, all v_i != 0  =>  (v_i) independent",
  symbols={"(v_i)": "an orthogonal family"},
  tcn="Well-formed. The nonzero hypothesis is indispensable: 0 is orthogonal to everything.",
  hyps=["pairwise orthogonal", "all vectors nonzero"],
  field_scope="real_or_complex",
  proof=("take <., v_j> of a vanishing combination: every term but the j-th dies, leaving a_j ||v_j||^2 = 0, and ||v_j|| != 0 forces a_j = 0", "linear_independence", "core", "LinAlg.orthogonal_indep"),
  spec=["an orthonormal set: automatically nonzero, hence independent",
        "n orthonormal vectors in an n-dimensional space therefore form a BASIS with no further argument"],
  cxd={"nonzeroness": "{0, e_1} is orthogonal and dependent",
       "positive_definiteness": "the proof divides by ||v_j||^2; over a degenerate form a self-orthogonal nonzero vector makes this step fail, and over F_2 the set {(1,1)} is 'orthogonal' to itself"},
  misuse=["asserting independence of an orthogonal set that contains 0"],
  related={"required_by": ["orthonormal_basis"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthonormal_basis", "definition", "inner_product",
  "An orthonormal basis is a basis that is an orthonormal set. Relative to one, coordinates are simply <v, e_i>: no matrix inversion, and the change-of-basis matrix is UNITARY.",
  parseable="(e_i) orthonormal and a basis;  v = sum_i <v, e_i> e_i;  [v]_E computed by inner products",
  symbols={"e_i": "the orthonormal basis vectors", "<v,e_i>": "the i-th Fourier coefficient"},
  tcn="Well-formed. The coordinate formula is the payoff: for a general basis, coordinates require solving a linear system; for an orthonormal one they are n inner products.",
  hyps=["V an inner product space"],
  field_scope="real_or_complex",
  spec=["the standard basis of F^n",
        "existence in every finite-dimensional inner product space is gram_schmidt",
        "the Fourier basis on L^2[0,2pi]: the infinite-dimensional analogue, where 'basis' means a convergent series, not a finite combination"],
  cxd={"orthonormality": "for a general basis, v = sum <v,b_i> b_i is FALSE -- the correct coefficients come from the inverse Gram matrix (gram_matrix), and the discrepancy is exactly the failure of orthogonality",
       "finite_dimensionality": "an orthonormal SET need not be a basis in infinite dimension even if maximal -- the algebraic and topological notions of basis diverge (see linear_combination)"},
  misuse=["using the coefficient formula <v, b_i> with a non-orthonormal basis -- the single most common error in applied projection work"],
  related={"required_by": ["gram_schmidt", "orthogonal_projection", "parseval_identity", "adjoint_operator"]},
  sources=["axler_lada_4e"], status="reviewed")

N("gram_schmidt", "algorithm", "inner_product",
  "Given an independent list (v_1,...,v_k), define e_j recursively as (v_j - P_{span(e_1..e_{j-1})} v_j) normalised. The result is orthonormal with span(e_1..e_j) = span(v_1..v_j) for every j. Hence EVERY finite-dimensional inner product space has an orthonormal basis.",
  parseable="w_j := v_j - sum_{i<j} <v_j, e_i> e_i;  e_j := w_j / ||w_j||;  span(e_1..e_j) = span(v_1..v_j)",
  symbols={"w_j": "the un-normalised residual", "e_j": "the j-th orthonormal vector"},
  tcn="Well-formed: w_j != 0 at every step BECAUSE the v's are independent (v_j is not in the span of the earlier ones), so the division by ||w_j|| is legitimate. Independence is exactly the non-degeneracy condition the algorithm needs.",
  hyps=["(v_i) linearly independent"],
  field_scope="real_or_complex",
  proof=("induction on j: orthogonality of e_j to earlier e_i by construction (the residual of a projection is orthogonal to the subspace); the span equality because each e_j is a combination of v_1..v_j with nonzero v_j-coefficient and conversely", "orthogonal_projection", "core", "LinAlg.gram_schmidt_step (orthogonality of the residual; Int instance in R^2)"),
  spec=["k = 1: e_1 = v_1/||v_1||",
        "the v's already orthogonal: Gram-Schmidt only normalises",
        "recording the coefficients gives exactly qr_factorisation, with R upper triangular because e_j uses only v_1..v_j"],
  cxd={"independence": "applied to a dependent list, some w_j = 0 and the normalisation divides by zero. The MODIFIED algorithm skips such j and produces an orthonormal basis of the span -- but the span-matching property is then lost for that index",
       "numerical_stability": "CLASSICAL Gram-Schmidt loses orthogonality catastrophically in floating point; MODIFIED Gram-Schmidt (subtract projections one at a time, updating as you go) is far better, and Householder reflections better still. This is a numerical-analysis fact, CITED, not established here"},
  misuse=["implementing classical Gram-Schmidt for an ill-conditioned matrix and trusting the resulting Q to be orthogonal",
          "applying it to a dependent list without handling the zero residual"],
  apps=["QR factorisation and hence the standard least-squares solver; the construction of orthogonal polynomial families (Legendre, Hermite) by Gram-Schmidt on 1, t, t^2, ... in an L^2 inner product"],
  related={"required_by": ["qr_factorisation", "orthogonal_decomposition", "schur_triangularisation", "singular_value_decomposition"]},
  sources=["axler_lada_4e", "trefethen_bau"], status="reviewed")

N("bessel_inequality", "proposition", "inner_product",
  "For any orthonormal set (e_1,...,e_k) and any v: sum_{i=1}^k |<v,e_i>|^2 <= ||v||^2.",
  parseable="sum_{i=1}^k |<v, e_i>|^2 <= ||v||^2",
  symbols={"<v,e_i>": "the i-th Fourier coefficient of v"},
  tcn="Well-formed for any orthonormal set, whether or not it is a basis. This is the point: Bessel needs no completeness, Parseval does.",
  hyps=["(e_i) orthonormal"],
  field_scope="real_or_complex",
  proof=("write v = P v + (v - P v) with P the projection onto span(e_i); Pythagoras gives ||v||^2 = ||Pv||^2 + ||v - Pv||^2 >= ||Pv||^2 = sum |<v,e_i>|^2", "pythagorean_theorem", "core", "LinAlg.bessel (Int instance, k = 1, 2)"),
  spec=["k = 1: |<v,e>|^2 <= ||v||^2, which is Cauchy-Schwarz for a unit vector",
        "(e_i) a BASIS: equality, which is parseval_identity",
        "the deficit ||v||^2 - sum |<v,e_i>|^2 is exactly the squared distance from v to the span"],
  cxd={"orthonormality": "for a general independent set the sum of squared coefficients can EXCEED ||v||^2 -- take two nearly-parallel unit vectors in R^2 and v along their common direction",
       "equality_requires_a_basis": "an orthonormal set that misses a direction leaves a strictly positive deficit"},
  misuse=["assuming equality without checking that the orthonormal set spans"],
  related={"required_by": ["parseval_identity"]},
  sources=["axler_lada_4e"], status="reviewed")

N("parseval_identity", "theorem", "inner_product",
  "For an orthonormal BASIS (e_1,...,e_n): ||v||^2 = sum_i |<v,e_i>|^2, and more generally <u,v> = sum_i <u,e_i> conj(<v,e_i>). Coordinates in an orthonormal basis are an ISOMETRY onto F^n.",
  parseable="||v||^2 = sum_i |<v,e_i>|^2;  <u,v> = sum_i <u,e_i> conj(<v,e_i>)",
  symbols={"(e_i)": "an orthonormal BASIS, not merely an orthonormal set"},
  tcn="Well-formed. The statement is that the coordinate map is not just a linear isomorphism (coordinate_isomorphism) but an ISOMETRY -- it preserves the inner product, not only the linear structure.",
  hyps=["(e_i) an orthonormal basis", "V finite-dimensional"],
  field_scope="real_or_complex",
  proof=("expand v = sum <v,e_i> e_i and compute <u,v> using orthonormality; every cross term vanishes", "bessel_inequality", "core", "LinAlg.parseval (Int instance, n = 2)"),
  spec=["V = F^n with the standard basis: the identity is the definition of the norm",
        "the Fourier series case: sum |c_n|^2 = (1/2pi) integral |f|^2, the classical Parseval theorem -- infinite-dimensional and CITED"],
  cxd={"spanning": "for a non-spanning orthonormal set only Bessel's INEQUALITY holds; the deficit is the squared distance to the span",
       "completeness_in_infinite_dimension": "an orthonormal set can be maximal yet fail Parseval in an INCOMPLETE inner product space -- completeness is what upgrades maximal-orthonormal to Parseval, and this capsule does not develop it"},
  misuse=["applying Parseval to a truncated basis (that is Bessel)",
          "assuming a maximal orthonormal set in an infinite-dimensional space satisfies Parseval without completeness"],
  apps=["the energy-preservation property of the Fourier and wavelet transforms; the decomposition of total variance into orthogonal components in PCA and ANOVA"],
  related={"special_case_of": ["bessel_inequality"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthogonal_complement", "definition", "inner_product",
  "U^perp = {v in V : <v,u> = 0 for all u in U}, a SUBSPACE of V for any subset U, with U^perp = (span U)^perp.",
  parseable="U^perp := {v : <v,u> = 0 for all u in U};  U^perp = (span U)^perp;  U subset (U^perp)^perp always",
  symbols={"U^perp": "type: subspace of V"},
  tcn="Well-formed for an arbitrary SUBSET U, since the defining conditions are linear in v. That U^perp is a subspace even when U is not is what makes the notation useful.",
  field_scope="real_or_complex",
  proof=("intersection of the kernels of the functionals <., u>, hence a subspace", "orthogonality", "core", "LinAlg.perp_subspace"),
  spec=["{0}^perp = V and V^perp = {0} (the latter uses positive definiteness)",
        "in R^3, a plane's complement is its normal line",
        "U^perp = null(A^T) when U = col(A) -- this is adjoint_kernel_image"],
  cxd={"positive_definiteness": "for a degenerate form, V^perp can be nonzero (the radical), and U + U^perp need not be V. Over F_2 with sum x_iy_i, the span of (1,1) is contained in its OWN perp",
       "double_perp_needs_finite_dimension": "U subset (U^perp)^perp always, but equality needs U to be a finite-dimensional (or closed) subspace -- see orthogonal_decomposition"},
  misuse=["confusing U^perp (in V, needs an inner product) with the annihilator U^0 (in V^*, needs none)",
          "assuming (U^perp)^perp = U for a non-closed subspace of an infinite-dimensional space"],
  related={"dual_of": ["annihilator"], "required_by": ["orthogonal_decomposition", "adjoint_kernel_image"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthogonal_decomposition", "theorem", "inner_product",
  "If U is a FINITE-DIMENSIONAL subspace of an inner product space V then V = U (+) U^perp, and (U^perp)^perp = U. Every v decomposes uniquely as v = P_U v + (v - P_U v).",
  parseable="dim U < infinity  =>  V = U (+) U^perp  and  (U^perp)^perp = U",
  symbols={"P_U": "the orthogonal projection onto U"},
  tcn="Well-formed. The hypothesis is on U (finite-dimensional), NOT on V -- the theorem holds for a finite-dimensional subspace of an infinite-dimensional space, which is what makes least squares work in function spaces.",
  hyps=["U finite-dimensional"],
  field_scope="real_or_complex",
  proof=("take an orthonormal basis of U by gram_schmidt and set P_U v = sum <v,e_i> e_i; then v - P_U v is orthogonal to each e_i hence to U. The intersection is trivial because a vector in both is orthogonal to itself, hence 0 by positive definiteness", "orthogonal_projection", "core", "LinAlg.orthogonal_decomposition (residual orthogonality; R^2 Int instance)"),
  spec=["U = {0}: V = {0} (+) V", "U = V: V = V (+) {0}",
        "U = col(A) in F^m: gives the least-squares decomposition of b into fitted values plus residual"],
  cxd={"finite_dimensionality_of_U": "in an incomplete inner product space, a NON-CLOSED infinite-dimensional subspace can have U^perp = {0} while U != V -- then V != U + U^perp and (U^perp)^perp = V != U. The standard witness is the space of finitely-supported sequences inside l^2. In a complete space (Hilbert) closedness of U is the right hypothesis",
       "positive_definiteness": "a degenerate form allows a nonzero vector in U ∩ U^perp"},
  misuse=["applying it to an arbitrary subspace of an infinite-dimensional space without a closedness or finite-dimensionality hypothesis",
          "assuming the complement is unique among ALL complements: it is unique among ORTHOGONAL ones, which is the point (see direct_sum)"],
  related={"required_by": ["projection_matrix_characterisation", "best_approximation", "spectral_theorem_symmetric"]},
  sources=["axler_lada_4e"], status="reviewed")

N("orthogonal_projection", "construction", "inner_product",
  "For a finite-dimensional subspace U with orthonormal basis (e_1,...,e_k), P_U v = sum_i <v,e_i> e_i. It is linear, idempotent (P^2 = P), self-adjoint, has image U and kernel U^perp, and is INDEPENDENT of which orthonormal basis of U is used.",
  parseable="P_U v := sum_{i=1}^k <v, e_i> e_i;  P^2 = P;  P^* = P;  im P = U, ker P = U^perp",
  symbols={"P_U": "type: element of L(V)", "(e_i)": "any orthonormal basis of U"},
  tcn="The type check is the WELL-DEFINEDNESS check: the formula is written in terms of a chosen orthonormal basis, so independence of that choice must be verified. It follows because P_U v is characterised basis-free as the unique u in U with v - u in U^perp.",
  hyps=["U finite-dimensional", "(e_i) orthonormal basis of U"],
  field_scope="real_or_complex",
  welldef="Independent of the orthonormal basis: if (f_j) is another, both formulas produce the unique u in U with v - u orthogonal to U, and uniqueness follows since a difference would lie in U ∩ U^perp = {0}.",
  proof=("idempotence and self-adjointness by direct computation with orthonormality; the basis-free characterisation gives well-definedness", "orthonormal_basis", "core", "LinAlg.projection_idempotent (Int instance in R^2)"),
  spec=["U a line spanned by unit e: P_U v = <v,e> e, the familiar scalar projection",
        "U = V: P = I; U = {0}: P = 0",
        "I - P_U is the orthogonal projection onto U^perp"],
  cxd={"orthonormality_of_the_basis": "with a merely independent basis (b_i) of U, sum <v,b_i> b_i is NOT the projection; the correct formula is B(B^*B)^{-1}B^* v, i.e. the hat_matrix, and the discrepancy is the inverse Gram matrix",
       "orthogonality_versus_oblique": "a general idempotent P is an OBLIQUE projection along ker P, which need not be U^perp. Self-adjointness is exactly what makes a projection orthogonal (projection_matrix_characterisation)"},
  misuse=["using the orthonormal formula with a non-orthonormal spanning set",
          "assuming any idempotent matrix is an orthogonal projection"],
  apps=["fitted values in least squares; conditional expectation onto a sigma-algebra is the L^2 orthogonal projection, which is why E[Y|X] is the best mean-square predictor"],
  related={"required_by": ["gram_schmidt", "best_approximation", "projection_matrix_characterisation", "spectral_decomposition"]},
  sources=["axler_lada_4e"], status="reviewed")

N("projection_matrix_characterisation", "theorem", "inner_product",
  "Over R, a matrix P represents the ORTHOGONAL projection onto col(P) if and only if P^2 = P and P^T = P. (Over C: P^2 = P and P^* = P.) Idempotence alone gives only an oblique projection.",
  parseable="P orthogonal projection  <=>  P^2 = P and P^T = P;  then im P = col(P), ker P = col(P)^perp, rank P = tr P",
  symbols={"P": "type: element of R^{n x n}", "tr P": "equals rank P for such a P"},
  tcn="Well-formed. TWO conditions, both necessary: idempotence makes it a projection, self-adjointness makes it orthogonal. This is the theorem `math-statistics` uses for the hat matrix.",
  hyps=["F = R (or C with ^*)"],
  field_scope="real_or_complex",
  proof=("forward from orthogonal_projection. Conversely, P^2 = P gives V = im P (+) ker P; P^T = P gives ker P = (im P)^perp by adjoint_kernel_image, so the decomposition is orthogonal", "orthogonal_decomposition", "core", "LinAlg.proj_iff_idem_sym (2x2 decide instances: orthogonal vs oblique)"),
  spec=["P = I and P = 0: the trivial projections",
        "the hat matrix H = X(X^TX)^{-1}X^T: symmetric and idempotent, hence orthogonal projection onto col(X)",
        "tr P = rank P, since the eigenvalues of such a P are all 0 or 1 -- the identity behind residual degrees of freedom"],
  cxd={"symmetry": "P = [[1,1],[0,0]] satisfies P^2 = P and is a projection onto the x-axis, but ALONG the line x + y = 0, not along its orthogonal complement. It is oblique: P^T != P. In regression this is the difference between OLS and a general weighted or instrumental-variables fit",
       "idempotence": "a symmetric non-idempotent matrix is not a projection at all"},
  misuse=["calling any idempotent matrix a projection matrix in a least-squares context, where the orthogonality is what gives the normal equations",
          "using tr P = rank P for an oblique projection (it still holds for idempotents, but the geometric reading does not)"],
  apps=["the hat matrix and the residual-maker I - H in linear regression; Cochran's theorem in ANOVA, which decomposes I into orthogonal idempotents"],
  related={"equivalent_to": ["orthogonal_projection"], "required_by": ["hat_matrix", "spectral_decomposition"]},
  sources=["strang_5e", "rao_linear_statistical_inference"], status="reviewed")

N("best_approximation", "theorem", "inner_product",
  "For a finite-dimensional subspace U, P_U v is the UNIQUE closest point of U to v: ||v - P_U v|| < ||v - u|| for every u in U with u != P_U v.",
  parseable="||v - P_U v|| < ||v - u||  for all u in U, u != P_U v",
  symbols={"P_U v": "the orthogonal projection, here characterised variationally"},
  tcn="Well-formed, with a STRICT inequality -- uniqueness of the minimiser is part of the statement, and it is what makes least squares a well-posed problem.",
  hyps=["U finite-dimensional"],
  field_scope="real_or_complex",
  proof=("write v - u = (v - P_U v) + (P_U v - u) with the two terms orthogonal (the first is in U^perp, the second in U); Pythagoras gives ||v-u||^2 = ||v - P_U v||^2 + ||P_U v - u||^2, strictly larger unless u = P_U v", "pythagorean_theorem", "core", "LinAlg.best_approx (Pythagoras argument; R^2 Int instance)"),
  spec=["U = span(e): the closest point on a line, the elementary projection formula",
        "U = col(A): the least-squares problem, giving least_squares"],
  cxd={"finite_dimensionality_or_closedness_of_U": "for a non-closed subspace the infimum need not be ATTAINED -- there is a nearest-point sequence but no nearest point. The same witness as orthogonal_decomposition (finitely-supported sequences in l^2)",
       "the_norm_being_induced": "in a general normed space the nearest point need not be unique: in R^2 with the infinity-norm, the nearest point on a line to an off-line point can be a whole segment. Uniqueness comes from strict convexity of the Euclidean ball, i.e. from the inner product"},
  misuse=["assuming a unique best approximation in an L^1 or L^infinity sense -- that is a different (and generally non-unique) problem"],
  apps=["least squares in every form: regression, curve fitting, Fourier truncation (the partial sum is the best L^2 approximation), and conditional expectation"],
  related={"required_by": ["least_squares"]},
  sources=["axler_lada_4e"], status="reviewed")

N("least_squares", "theorem", "inner_product",
  "x minimises ||Ax - b|| if and only if A^T A x = A^T b (the NORMAL EQUATIONS). A solution always exists; it is unique iff A has full column rank, and then x = (A^TA)^{-1}A^Tb with fitted values Ax = P_{col(A)} b.",
  parseable="x minimises ||Ax - b||  <=>  A^T A x = A^T b;  unique  <=>  rank A = n;  then x = (A^TA)^{-1}A^T b",
  symbols={"A": "type: element of R^{m x n}, typically m > n",
           "A^TA": "the Gram matrix of the columns, type: n x n symmetric PSD"},
  tcn="Well-formed over R. The normal equations are ALWAYS consistent (A^Tb is in col(A^TA) = row(A)), so a minimiser always exists even when A is rank deficient -- uniqueness is the only thing that can fail.",
  hyps=["F = R (or C with ^*)", "full column rank for uniqueness"],
  field_scope="real_or_complex",
  proof=("Ax is the closest point of col(A) to b iff b - Ax is orthogonal to col(A) iff A^T(b - Ax) = 0 (best_approximation plus adjoint_kernel_image)", "best_approximation", "core", "LinAlg.normal_equations (orthogonality of the residual; 3x2 Int decide instance)"),
  spec=["A square invertible: the normal equations reduce to Ax = b",
        "A with orthonormal columns: A^TA = I, so x = A^Tb -- the projection coefficients directly",
        "the 3-point regression design used in math-statistics' instance checks"],
  cxd={"full_column_rank": "if rank A < n the normal equations have a whole affine family of solutions; the minimum-NORM one is A^+b via the pseudoinverse (moore_penrose_pseudoinverse). Collinear predictors in regression are exactly this case",
       "conditioning": "kappa(A^TA) = kappa(A)^2, so forming the normal equations SQUARES the condition number. For an A with kappa = 10^8 the normal equations lose all double precision, while qr_factorisation does not. This is why no serious least-squares solver forms A^TA"},
  misuse=["solving least squares by explicitly forming and inverting A^TA (see above) -- use QR or SVD",
          "concluding no solution exists when A is rank deficient: a minimiser always exists, it is merely non-unique"],
  apps=["ordinary least squares regression -- the normal equations ARE the OLS estimating equations, and math-statistics' linear_model block rests on this node",
        "curve fitting, calibration, and the linear step of Gauss-Newton and Levenberg-Marquardt"],
  related={"special_case_of": ["best_approximation"], "required_by": ["hat_matrix", "qr_factorisation", "moore_penrose_pseudoinverse"]},
  sources=["strang_5e", "golub_van_loan_4e"], status="reviewed")

N("hat_matrix", "construction", "inner_product",
  "For A of full column rank, H = A(A^TA)^{-1}A^T is the orthogonal projection onto col(A). It is symmetric, idempotent, of rank = rank A, and tr(H) = rank A. The residual maker is M = I - H, also an orthogonal projection, onto col(A)^perp, with tr(M) = m - rank A.",
  parseable="H := A(A^TA)^{-1}A^T;  H^2 = H = H^T;  rank H = tr H = rank A;  M = I - H, tr M = m - rank A",
  symbols={"H": "the hat matrix, type: element of R^{m x m}",
           "M = I - H": "the residual maker"},
  tcn="Well-formed when A^TA is invertible, i.e. when A has full column rank -- which by psd_characterisations is exactly when the Gram matrix is positive DEFINITE rather than merely semidefinite.",
  hyps=["rank A = n (full column rank)"],
  field_scope="real_or_complex",
  proof=("symmetry and idempotence by direct computation; H a = a for a in col(A) and Hb = 0 for b in col(A)^perp identify it as the projection; tr(H) = tr(A(A^TA)^{-1}A^T) = tr((A^TA)^{-1}A^TA) = tr(I_n) = n by trace_cyclic", "projection_matrix_characterisation", "core", "LinAlg.hat_trace_eq_rank (3x2 Int decide instance: tr H = 2)"),
  spec=["A a single column a: H = aa^T/(a^Ta), the rank-one projection onto its span",
        "A = I_m: H = I, tr = m",
        "the diagonal entries h_ii are the LEVERAGES in regression, with sum equal to the number of parameters"],
  cxd={"full_column_rank": "if A is rank deficient, A^TA is singular and H is undefined by this formula. The projection onto col(A) still exists -- use AA^+ with the pseudoinverse",
       "orthogonality_needs_symmetry": "a weighted fit uses H_W = A(A^TW A)^{-1}A^TW, which is idempotent but NOT symmetric: an OBLIQUE projection. Quoting tr H = rank still works, but the Pythagorean decomposition of sums of squares does not"},
  misuse=["applying the unweighted formula and the orthogonal sum-of-squares decomposition to a weighted or GLS fit",
          "computing H explicitly for large m: it is m x m, while the fit needs only Ax"],
  apps=["THE object of linear regression diagnostics: leverages h_ii, Cook's distance, and tr(M) = n - p as the residual degrees of freedom that make s^2 = RSS/(n-p) unbiased -- this is exactly what math-statistics' unbiased_error_variance_estimator cites"],
  related={"special_case_of": ["orthogonal_projection"]},
  sources=["strang_5e", "rao_linear_statistical_inference"], status="reviewed")

N("qr_factorisation", "construction", "inner_product",
  "A in F^{m x n} with m >= n and full column rank factors as A = QR with Q in F^{m x n} having orthonormal columns and R in F^{n x n} upper triangular with positive diagonal; the factorisation is then UNIQUE. Least squares becomes Rx = Q^Tb, a triangular solve.",
  parseable="A = QR,  Q^*Q = I_n,  R upper triangular with R_{ii} > 0;  unique;  least squares: Rx = Q^*b",
  symbols={"Q": "orthonormal columns spanning col(A)", "R": "upper triangular, the Gram-Schmidt coefficients"},
  tcn="Well-formed under full column rank; the positivity normalisation of the diagonal is what pins down uniqueness (otherwise Q and R can absorb signs or phases).",
  hyps=["m >= n", "rank A = n for the unique reduced form"],
  field_scope="real_or_complex",
  proof=("run gram_schmidt on the columns of A; e_j depends only on v_1..v_j, which is exactly upper-triangularity of R. Uniqueness because Q^*A = R forces R, and positivity of the diagonal fixes the signs", "gram_schmidt", "core", "LinAlg.qr_uniqueness (triangularity from the Gram-Schmidt recursion; 2x2 Int instance)"),
  spec=["A square invertible: QR with both factors square, and |det A| = prod |R_ii|",
        "A with orthonormal columns already: Q = A, R = I",
        "A^TA = R^TR, so R is the Cholesky factor of the Gram matrix -- the link to cholesky_factorisation"],
  cxd={"full_column_rank": "a rank-deficient A has no unique QR; column-pivoted QR (AP = QR) is the standard remedy and also reveals the numerical rank",
       "why_QR_over_the_normal_equations": "solving Rx = Q^Tb has condition number kappa(A), while the normal equations have kappa(A)^2. For kappa(A) = 10^8 this is the difference between 8 lost digits and 16"},
  misuse=["using classical Gram-Schmidt to compute Q in floating point (see gram_schmidt): Householder reflections or MGS are the stable implementations",
          "assuming QR is available for a rank-deficient matrix without pivoting"],
  apps=["the default dense least-squares solver in LAPACK and in every statistics package's lm() implementation -- R's lm uses a pivoted QR, not the normal equations",
        "the QR algorithm for eigenvalues repeatedly factors and re-multiplies, converging to schur_triangularisation"],
  related={"special_case_of": ["gram_schmidt"]},
  sources=["golub_van_loan_4e", "trefethen_bau"], status="reviewed")

N("adjoint_operator", "definition", "inner_product",
  "The adjoint T^* is the unique operator with <Tv, w> = <v, T^* w> for all v, w. It exists and is unique in finite dimension; its matrix in ORTHONORMAL bases is the conjugate transpose. (ST)^* = T^*S^*, (T^*)^* = T, and (aT)^* = conj(a) T^*.",
  parseable="<Tv, w> = <v, T^*w> for all v,w;  [T^*]_E = ([T]_E)^*  for E orthonormal;  (ST)^* = T^* S^*",
  symbols={"T^*": "type: linear map W -> V (note the direction reverses, as for dual_map)"},
  tcn="Well-formed: existence and uniqueness follow from the Riesz representation of the functional v -> <Tv, w> in finite dimension. The matrix identity holds ONLY in orthonormal bases -- in a general basis the adjoint's matrix is G^{-1}A^*G with G the Gram matrix.",
  hyps=["V, W finite-dimensional inner product spaces"],
  field_scope="real_or_complex",
  proof=("for fixed w, v -> <Tv,w> is a functional, hence equals <v, u> for a unique u by Riesz (finite-dimensional, via an orthonormal basis); set T^*w := u and check linearity", "orthonormal_basis", "core", "LinAlg.adjoint_matrix_conj_transpose (2x2 Int instance)"),
  spec=["V = W = F^n standard: T^* is A^*",
        "T unitary: T^* = T^{-1}", "T self-adjoint: T^* = T"],
  cxd={"orthonormality_of_the_basis": "in a non-orthonormal basis with Gram matrix G, the adjoint's matrix is G^{-1}A^*G, not A^*. Assuming otherwise is the source of the 'why is my adjoint wrong in a weighted inner product' error",
       "finite_dimensionality": "in infinite dimension the adjoint of an UNBOUNDED operator need not exist on the whole space -- densely defined adjoints and domain issues are the substance of unbounded operator theory, entirely outside this capsule",
       "adjoint_versus_dual": "the dual map T^t: W^* -> V^* needs no inner product and does not conjugate; T^* does both. They correspond under the Riesz identification, which is CONJUGATE-linear over C"},
  misuse=["using A^T rather than A^* over C",
          "assuming the matrix of the adjoint is the conjugate transpose in an arbitrary basis"],
  related={"contrasts_with": ["dual_map"], "required_by": ["self_adjoint", "adjoint_kernel_image", "normal_matrix"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("adjoint_kernel_image", "theorem", "inner_product",
  "ker(T^*) = (im T)^perp and im(T^*) = (ker T)^perp. In matrix terms: null(A^T) = col(A)^perp and null(A) = row(A)^perp -- the four fundamental subspaces are TWO ORTHOGONAL PAIRS.",
  parseable="ker(T^*) = (im T)^perp;  im(T^*) = (ker T)^perp;  null(A^*) = col(A)^perp;  null(A) = row(A)^perp",
  symbols={"perp": "orthogonal complement in the relevant space"},
  tcn="Well-formed over R or C only: it upgrades the dimension statement of four_subspaces (valid over any field) to an ORTHOGONALITY statement, which needs an inner product.",
  hyps=["F = R or C", "finite-dimensional"],
  field_scope="real_or_complex",
  proof=("w in ker T^* iff <v, T^*w> = 0 for all v iff <Tv, w> = 0 for all v iff w perp im T. The second identity follows by applying the first to T^* and using (T^*)^* = T plus orthogonal_decomposition", "adjoint_operator", "core", "LinAlg.ker_adjoint_perp_image"),
  spec=["A with full column rank: null(A) = {0}, so row(A) = F^n",
        "the consistency condition for Ax = b is exactly b perp null(A^T) -- the Fredholm alternative in finite dimension"],
  cxd={"the_field_being_R_or_C": "over F_2 the 'orthogonal complement' of the span of (1,1) contains (1,1) itself, so the pair is not complementary. The DIMENSIONS from four_subspaces are still correct; the orthogonality is not. This node is precisely where the capsule's any_field results end and the real_or_complex ones begin",
       "finite_dimensionality": "in infinite dimension one gets ker(T^*) = (im T)^perp with the CLOSURE of the image, so im(T^*) = (ker T)^perp requires closed range"},
  misuse=["stating 'the row space and null space are orthogonal complements' as a general-field fact",
          "using it for an operator with non-closed range in infinite dimension"],
  apps=["the Fredholm alternative: Ax = b is solvable iff b is orthogonal to every solution of A^Ty = 0 -- the standard consistency test",
        "the geometry of the four subspaces as taught in applied linear algebra"],
  related={"required_by": ["least_squares", "svd_four_subspaces", "projection_matrix_characterisation"]},
  sources=["strang_5e", "axler_lada_4e"], status="reviewed")
