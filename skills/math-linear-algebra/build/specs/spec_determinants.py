"""Determinants: the alternating-form definition, Leibniz, Laplace, the
multiplicative property, the adjugate, Cramer, and volume."""
from nodespec import N

N("permutation_sign", "definition", "determinants",
  "S_n is the group of bijections of {1..n}. Every permutation is a product of transpositions, and the PARITY of the number of transpositions is well-defined; sgn: S_n -> {+1,-1} is the resulting group homomorphism.",
  parseable="sgn(sigma) = (-1)^{#transpositions};  sgn(sigma tau) = sgn(sigma) sgn(tau);  sgn well-defined",
  symbols={"S_n": "the symmetric group, type: group of order n!",
           "sgn": "type: group homomorphism S_n -> {+1,-1}"},
  tcn="The type check IS the well-definedness check: a permutation has many decompositions into transpositions, and the claim is that their parities agree. Proved by tracking the number of inversions, which changes parity under each transposition.",
  hyps=["n finite"],
  field_scope="any_field",
  welldef="Parity is independent of the decomposition because each transposition changes the inversion count by an odd number. Without this, 'sgn' would not be a function and the Leibniz formula would be meaningless.",
  proof=("inversion-count argument; the homomorphism property then follows by concatenating decompositions", "bijection", "core", "LinAlg.sign_welldefined (inversion parity, n <= 4 decide instances)"),
  spec=["n = 2: S_2 = {id, swap} with signs +1 and -1",
        "a cycle of length k has sign (-1)^{k-1}",
        "over a field of characteristic 2, +1 = -1 and sgn is trivial -- which is why the determinant over F_2 is the PERMANENT"],
  cxd={"well_definedness_of_parity": "if parity were decomposition-dependent, sgn would not exist and the alternating condition could not be expressed",
       "characteristic_two": "over F_2 the alternating condition 'D = 0 when two columns are equal' is strictly stronger than 'D changes sign under a swap', because -1 = 1 makes the latter vacuous. This is why multilinear_alternating_form is defined by the VANISHING condition, not the sign-change condition"},
  misuse=["defining alternating as 'antisymmetric' (sign-changing) -- equivalent only when char F != 2"],
  related={"required_by": ["determinant_existence_uniqueness", "leibniz_formula"]},
  sources=["hoffman_kunze_2e", "dummit_foote_3e"], status="reviewed")

N("multilinear_alternating_form", "definition", "determinants",
  "D: (F^n)^n -> F is multilinear if it is linear in each column separately with the others fixed, and ALTERNATING if D(...) = 0 whenever two columns are EQUAL. Alternating implies sign-change under a swap; the converse needs char F != 2.",
  parseable="D linear in each argument;  D(..., v, ..., v, ...) = 0;  hence D(swap) = -D",
  symbols={"D": "type: function (F^n)^n -> F",
           "the n arguments": "the COLUMNS of a matrix, type: elements of F^n"},
  tcn="Well-formed. Defining alternating by VANISHING rather than by sign change is deliberate and is what makes the theory characteristic-independent (see permutation_sign's counterexample).",
  hyps=["D multilinear"],
  field_scope="any_field",
  proof=("vanishing implies antisymmetry: expand D(u+v, u+v) = 0 by multilinearity and cancel the two vanishing terms", "matrix", "core", "LinAlg.alternating_implies_antisym"),
  spec=["n = 1: the multilinear alternating forms on F^1 are exactly the scalar multiples of the identity, D(a) = ca",
        "n = 2: D([a,c],[b,d]) = ad - bc up to a scalar"],
  cxd={"the_vanishing_definition": "over F_2, 'D changes sign under a swap' is satisfied by the PERMANENT (which never changes sign, and -1 = 1), yet the permanent is not the determinant. The vanishing definition excludes it",
       "multilinearity": "a form that is merely additive in each slot, without homogeneity, does not pin down D even with alternation"},
  misuse=["treating 'alternating' and 'antisymmetric' as synonyms in characteristic 2"],
  related={"required_by": ["determinant_existence_uniqueness", "determinant"]},
  sources=["hoffman_kunze_2e", "lang_algebra_3e"], status="reviewed")

N("determinant_existence_uniqueness", "theorem", "determinants",
  "There is EXACTLY ONE multilinear alternating form D on the columns of F^{n x n} with D(I) = 1. Stated deliberately WITHOUT naming 'the determinant' -- this is what makes the subsequent definition non-circular (edges/cycles.md, cycle 2).",
  parseable="exists! D : (F^n)^n -> F multilinear, alternating, with D(e_1,...,e_n) = 1",
  symbols={"D": "the unique normalised alternating multilinear form"},
  tcn="Well-formed as an existence-and-uniqueness statement about a class of functions, with no forward reference. Uniqueness is what makes 'the determinant' a definite description.",
  hyps=["F a commutative field", "n fixed"],
  field_scope="any_field",
  proof=("UNIQUENESS: expand each column in the standard basis by multilinearity, giving a sum over all index functions; alternation kills every non-injective one, leaving a sum over S_n with coefficients forced by the swap rule and D(I) = 1. EXISTENCE: verify that the resulting Leibniz sum is multilinear and alternating", "multilinear_alternating_form", "core", "LinAlg.det_unique (uniqueness argument; existence checked by decide at n = 2, 3)"),
  spec=["n = 2: uniqueness forces D = ad - bc",
        "dropping the normalisation D(I) = 1 leaves a ONE-DIMENSIONAL space of alternating forms, all scalar multiples of det -- normalisation is what selects one"],
  cxd={"the_normalisation": "without D(I) = 1, D = 0 also qualifies and uniqueness fails outright",
       "alternation": "without it, many multilinear forms exist (the permanent among them)"},
  misuse=["proving existence by writing the Leibniz formula and calling it the definition -- the point of this ordering is that the CHARACTERISATION comes first and the formula is derived"],
  related={"required_by": ["determinant", "leibniz_formula"]},
  sources=["hoffman_kunze_2e", "lang_algebra_3e"], status="reviewed")

N("determinant", "definition", "determinants",
  "det: F^{n x n} -> F is THE unique normalised alternating multilinear function of the columns. Defined with no reference to eigenvalues (edges/cycles.md, cycle 1) and with no reference to minors, so rank stays determinant-free (cycle 3).",
  parseable="det := the unique D with D multilinear, alternating in the columns, D(I) = 1",
  symbols={"det A": "type: element of F", "|A|": "alternative notation, avoided here to prevent confusion with norms"},
  tcn="Well-formed as a definite description ONLY because determinant_existence_uniqueness precedes it.",
  hyps=["A square"],
  field_scope="any_field",
  spec=["n = 1: det[a] = a", "n = 2: det = ad - bc",
        "n = 3: the six-term Sarrus expansion -- valid ONLY for n = 3 and not a general pattern"],
  cxd={"squareness": "there is no determinant of a rectangular matrix; the nearest analogues are the Gram determinant det(A^TA) and the singular values",
       "commutativity_of_F": "over a noncommutative division ring no function with these properties exists (the Dieudonne determinant lands in an abelianisation instead)"},
  misuse=["extending Sarrus' rule to n >= 4, where it is simply wrong",
          "reading det as a measure of how invertible A is: det != 0 is a yes/no test, and magnitude is not conditioning (condition_number)"],
  apps=["the Jacobian determinant as the local volume-change factor in the change-of-variables formula",
        "the normalising constant of the multivariate normal density involves det(Sigma)"],
  related={"required_by": ["leibniz_formula", "determinant_multiplicative", "characteristic_polynomial"]},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("leibniz_formula", "theorem", "determinants",
  "det A = sum_{sigma in S_n} sgn(sigma) prod_{i=1}^n A_{i, sigma(i)}. A THEOREM identifying the unique normalised alternating form, not the definition.",
  parseable="det A = sum_{sigma in S_n} sgn(sigma) prod_{i=1}^n A_{i,sigma(i)}",
  symbols={"sigma": "a permutation, type: element of S_n", "n!": "the number of terms"},
  tcn="Well-formed; the sum has n! terms, each a product of n entries, one from each row and each column.",
  hyps=["A square"],
  field_scope="any_field",
  proof=("this is exactly the formula produced by the uniqueness argument of determinant_existence_uniqueness", "determinant_existence_uniqueness", "core", "LinAlg.leibniz_2x2 / LinAlg.leibniz_3x3 (decide instances); the general identification is the uniqueness proof"),
  spec=["n = 2: two terms, ad - bc", "n = 3: six terms",
        "A triangular: only sigma = id contributes a nonzero product (determinant_triangular)"],
  cxd={"none_all_hypotheses_essential": "an identity for square A over any field"},
  misuse=["using it as an ALGORITHM: n! terms is catastrophic (20! exceeds 10^18). Compute determinants by elimination in O(n^3) via determinant_row_operations and determinant_triangular",
          "assuming the formula is the definition, which obscures why det is multiplicative"],
  related={"required_by": ["determinant_transpose", "determinant_triangular", "laplace_expansion"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("determinant_row_operations", "proposition", "determinants",
  "Under R1 (swap) det negates; under R2 (scale a row by c) det scales by c; under R3 (add a multiple of one row to another) det is UNCHANGED. Hence elimination computes det in O(n^3).",
  parseable="R1: det -> -det;  R2 (factor c): det -> c det;  R3: det unchanged",
  symbols={"c": "the scaling factor in R2"},
  tcn="Well-formed. Stated for ROWS; the column versions follow from determinant_transpose, and the alternating property is a column property by definition -- so this proposition is where rows and columns are first tied together.",
  hyps=["A square"],
  field_scope="any_field",
  proof=("R2 from multilinearity, R1 from antisymmetry, R3 by splitting the modified row by multilinearity: the extra term has two equal rows and so vanishes by alternation", "multilinear_alternating_form", "core", "LinAlg.det_row_ops (2x2/3x3 decide instances)"),
  spec=["A with two equal rows: repeated R3 produces a zero row, so det = 0",
        "A with a zero row: det = 0 by R2 with c = 0"],
  cxd={"R3_requires_distinct_rows": "r_i -> r_i + c r_i is R2 in disguise and DOES change det by (1+c); the i != j condition in elementary_row_operation is what makes R3 determinant-preserving",
       "R2_scales_ONE_row": "scaling the whole matrix by c scales det by c^n, not by c -- det(cA) = c^n det(A) is the single most common determinant error"},
  misuse=["writing det(cA) = c det(A)",
          "writing det(A + B) = det A + det B: the determinant is multilinear in COLUMNS, not linear in the matrix"],
  related={"required_by": ["determinant_multiplicative", "determinant_invertible_iff"]},
  sources=["hoffman_kunze_2e", "strang_5e"], status="reviewed")

N("determinant_triangular", "proposition", "determinants",
  "The determinant of a triangular matrix is the product of its diagonal entries.",
  parseable="A triangular  =>  det A = prod_{i=1}^n A_{ii}",
  symbols={"A_{ii}": "the diagonal entries"},
  tcn="Well-formed for upper or lower triangular A, and in particular for diagonal A.",
  hyps=["A triangular"],
  field_scope="any_field",
  proof=("in the Leibniz sum, a permutation other than the identity must pick an entry above the diagonal in some column and below it in another; triangularity makes one of these zero", "leibniz_formula", "core", "LinAlg.det_triangular (3x3 decide instance plus the Leibniz argument)"),
  spec=["A = I: det = 1, recovering the normalisation",
        "combined with determinant_row_operations, this is the O(n^3) algorithm: eliminate to triangular, multiply the diagonal, correct for swaps and scalings"],
  cxd={"triangularity": "a general matrix's determinant is NOT the product of its diagonal: [[0,1],[1,0]] has diagonal product 0 but determinant -1",
       "block_version": "for a BLOCK triangular matrix det is the product of the DIAGONAL BLOCKS' determinants -- but only when the off-diagonal block is on one side; see schur_complement for the general case"},
  misuse=["reading the diagonal of a general matrix as its eigenvalues (true only for triangular matrices)"],
  related={"required_by": ["determinant_invertible_iff", "characteristic_polynomial"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("determinant_transpose", "theorem", "determinants",
  "det(A^T) = det(A) for every square A over any field. Consequently every row statement about determinants has an identical column statement.",
  parseable="det(A^T) = det(A)",
  symbols={"A^T": "the transpose"},
  tcn="Well-formed. The result is not obvious from the definition, which privileges COLUMNS; it is what makes the row/column asymmetry of the setup disappear.",
  hyps=["A square"],
  field_scope="any_field",
  proof=("in the Leibniz sum, reindex by sigma^{-1}: prod_i A_{i,sigma(i)} = prod_j A_{sigma^{-1}(j), j}, and sgn(sigma^{-1}) = sgn(sigma)", "leibniz_formula", "core", "LinAlg.det_transpose (reindexing argument; 3x3 decide instance)"),
  spec=["A symmetric: the statement is trivially true",
        "the reason 'det A != 0 iff the ROWS are independent' and 'iff the COLUMNS are independent' are the same criterion"],
  cxd={"none_all_hypotheses_essential": "true for every square matrix over every field. Over a noncommutative ring it fails, which is one reason determinants are a commutative-algebra notion"},
  misuse=["assuming the analogous det(A^*) = det(A) over C: in fact det(A^*) = conj(det A), so det(A^*A) = |det A|^2 is real and nonnegative -- a fact used constantly for Gram determinants"],
  related={"required_by": ["determinant_multiplicative"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("determinant_multiplicative", "theorem", "determinants",
  "det(AB) = det(A) det(B) for square A, B of the same size. Hence det is a group homomorphism from GL_n(F) to F^*.",
  parseable="det(AB) = det(A) det(B);  det : GL_n(F) -> F^* a group homomorphism",
  symbols={"GL_n(F)": "the group of invertible n x n matrices"},
  tcn="Well-formed for equal square shapes. This is the deepest elementary property of the determinant and is what all its invariance results (similarity, volume) reduce to.",
  hyps=["A, B square of the same size"],
  field_scope="any_field",
  proof=("for fixed B, the map A -> det(AB) is an alternating multilinear function of the ROWS of A, equal to det(B) when A = I; by uniqueness it equals det(B)det(A). (Equivalently: factor A into elementary matrices and apply determinant_row_operations to each.)", "determinant_row_operations", "core", "LinAlg.det_mul (2x2 decide instance; uniqueness argument for the general case)"),
  spec=["B = A^{-1}: det(A)det(A^{-1}) = 1, so det(A^{-1}) = 1/det(A)",
        "B = P^{-1}, A = PA': recovers determinant_similarity_invariant",
        "det(A^k) = (det A)^k"],
  cxd={"squareness_and_equal_size": "for rectangular factors both sides are undefined; the Cauchy-Binet formula is the correct generalisation and is NOT a product of determinants",
       "addition_has_no_analogue": "det(A+B) != det A + det B, and there is no useful formula"},
  misuse=["expecting an additive analogue",
          "using det(AB) = det(A)det(B) to conclude rank(AB) = rank(A)rank(B) -- rank is not multiplicative (rank_inequalities)"],
  apps=["the change-of-variables Jacobian factor composes multiplicatively under composition of maps, which is exactly this theorem",
        "the determinant of a product of elementary operations is how elimination tracks det in O(n^3)"],
  related={"required_by": ["determinant_invertible_iff", "determinant_similarity_invariant", "determinant_volume"]},
  sources=["hoffman_kunze_2e", "lang_algebra_3e"], status="reviewed")

N("determinant_invertible_iff", "theorem", "determinants",
  "A square matrix over a FIELD is invertible if and only if det A != 0, and then det(A^{-1}) = 1/det(A).",
  parseable="A invertible  <=>  det A != 0;  det(A^{-1}) = (det A)^{-1}",
  symbols={"det A": "an element of F, tested against 0"},
  tcn="Well-formed over a field. Over a commutative RING the correct statement is 'invertible iff det A is a UNIT', which over Z means det = +-1, not merely nonzero.",
  hyps=["A square", "F a field"],
  field_scope="any_field",
  proof=("if A is invertible, multiplicativity on AA^{-1} = I gives det(A)det(A^{-1}) = 1 so det A != 0. Conversely, if A is singular its columns are dependent, so an R3 sequence produces a zero column and det A = 0", "determinant_multiplicative", "core", "LinAlg.det_ne_zero_iff_invertible (2x2/3x3 decide instances)"),
  spec=["n = 1: [a] invertible iff a != 0",
        "A triangular: invertible iff no diagonal entry is 0, combining this with determinant_triangular"],
  cxd={"F_a_field": "over Z, det[[2]] = 2 != 0 but [[2]] has no inverse with integer entries. The correct ring statement requires det to be a unit",
       "exact_arithmetic": "in floating point, det A != 0 is not a usable invertibility test: a matrix with det = 10^{-300} may be perfectly well conditioned and one with det = 1 may be numerically singular. Use the condition number"},
  misuse=["testing numerical singularity by the determinant (see above) -- this is the single most common misuse of the determinant in computation",
          "applying it over a ring"],
  related={"equivalent_to": ["invertibility_equivalences"], "contrasts_with": ["condition_number"]},
  sources=["hoffman_kunze_2e", "higham_asna_2e"], status="reviewed")

N("laplace_expansion", "theorem", "determinants",
  "det A = sum_{j=1}^n (-1)^{i+j} A_{ij} M_{ij} for ANY fixed row i, and symmetrically along any fixed column, where M_{ij} is the (i,j) minor (the determinant of A with row i and column j deleted).",
  parseable="det A = sum_j (-1)^{i+j} A_{ij} M_{ij}  (any fixed i);  = sum_i (-1)^{i+j} A_{ij} M_{ij}  (any fixed j)",
  symbols={"M_{ij}": "the (i,j) MINOR, type: element of F",
           "C_{ij} = (-1)^{i+j} M_{ij}": "the (i,j) COFACTOR"},
  tcn="Well-formed. The checkerboard sign (-1)^{i+j} depends on the index_convention starting at 1; 0-based indexing flips every sign.",
  hyps=["A square, n >= 2"],
  field_scope="any_field",
  proof=("group the Leibniz sum by the value of sigma at the chosen index, and identify each group with a signed minor", "leibniz_formula", "core", "LinAlg.laplace_3x3 (decide instance); the general grouping is the Leibniz argument"),
  spec=["n = 2 along the first row: det = a(d) - b(c)",
        "A with a row of mostly zeros: expanding along it is efficient -- the one case where cofactor expansion beats elimination",
        "expanding along a row with a DIFFERENT row's cofactors gives 0, which is the identity behind the adjugate"],
  cxd={"indexing_from_one": "0-based indexing negates every cofactor sign",
       "cost": "recursive cofactor expansion costs O(n!) -- the same disaster as Leibniz. It is a structural tool, not an algorithm"},
  misuse=["using cofactor expansion to compute a determinant of size beyond about 4",
          "forgetting the alternating sign entirely (computing sum_j A_{ij} M_{ij}, which is not the determinant)"],
  related={"required_by": ["adjugate", "determinant_rank_minors"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("adjugate", "construction", "determinants",
  "adj(A)_{ij} = C_{ji} = (-1)^{i+j} M_{ji} (the TRANSPOSED cofactor matrix). Then A adj(A) = adj(A) A = det(A) I, so A^{-1} = adj(A)/det(A) whenever det A != 0.",
  parseable="adj(A)_{ij} := (-1)^{i+j} M_{ji};  A adj(A) = adj(A) A = det(A) I",
  symbols={"adj(A)": "the adjugate (classical adjoint), type: element of F^{n x n}"},
  tcn="Well-formed. The TRANSPOSE in the definition is essential and is the usual slip. Note the identity A adj(A) = det(A) I holds for EVERY square A, including singular ones -- no invertibility is assumed.",
  hyps=["A square"],
  field_scope="any_field",
  proof=("the (i,i) entry of A adj(A) is the Laplace expansion along row i; an off-diagonal entry is the expansion of a matrix with two equal rows, hence 0", "laplace_expansion", "core", "LinAlg.adjugate_identity (2x2/3x3 decide instances over Int)"),
  spec=["n = 2: adj([[a,b],[c,d]]) = [[d,-b],[-c,a]], the familiar swap-and-negate rule",
        "A singular: A adj(A) = 0, and adj(A) has rank at most 1 when rank A = n-1",
        "over Z: adj(A) has INTEGER entries, so A^{-1} is integral exactly when det A = +-1 -- the ring statement determinant_invertible_iff needs"],
  cxd={"the_transpose": "omitting it gives the cofactor matrix, for which the identity is false already at n = 2 unless the matrix is symmetric",
       "cost": "computing A^{-1} via the adjugate costs O(n!) or at best O(n^4); elimination costs O(n^3)"},
  misuse=["using the adjugate formula as a numerical inverse",
          "confusing adj(A) (classical adjoint) with A^* (the Hermitian adjoint) -- unrelated objects sharing an unfortunate name"],
  apps=["Cramer's rule and the integrality criterion above; the adjugate is also how cayley_hamilton is proved in this release"],
  related={"required_by": ["cramers_rule", "cayley_hamilton"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("cramers_rule", "corollary", "determinants",
  "For invertible A, the unique solution of Ax = b is x_i = det(A_i)/det(A), where A_i is A with its i-th COLUMN replaced by b.",
  parseable="A invertible  =>  x_i = det(A_i)/det(A),  A_i = A with column i replaced by b",
  symbols={"A_i": "the modified matrix, type: element of F^{n x n}"},
  tcn="Well-formed when det A != 0. Note the substitution is into a COLUMN, matching the column-multilinearity of det.",
  hyps=["A square and invertible"],
  field_scope="any_field",
  proof=("x = A^{-1}b = adj(A)b/det(A); the i-th entry of adj(A)b is the Laplace expansion of det(A_i)", "adjugate", "core", "LinAlg.cramer_2x2 (decide instance over Int)"),
  spec=["n = 2: x_1 = (b_1 d - b_2 b)/det, the two-equation formula from school algebra",
        "over Z with det A = +-1: the solution is automatically integral"],
  cxd={"invertibility": "for singular A the formula divides by zero; a consistent singular system has infinitely many solutions and Cramer says nothing about them",
       "cost": "n+1 determinants, so O(n * n^3) at best against O(n^3) for elimination -- Cramer is exponentially worse if the determinants are computed by expansion"},
  misuse=["using it to solve systems of any real size",
          "applying it to an underdetermined or inconsistent system"],
  apps=["still useful for SYMBOLIC 2x2 and 3x3 solves, and for proving that solutions depend rationally (hence smoothly) on the entries -- the implicit function theorem's linear core"],
  related={"historically_precedes": ["gaussian_elimination"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("determinant_similarity_invariant", "corollary", "determinants",
  "det(P^{-1}AP) = det(A). The determinant depends only on the operator, not on the basis, and is therefore a similarity invariant.",
  parseable="det(P^{-1} A P) = det(A)",
  symbols={"P": "invertible"},
  tcn="Well-formed. This is what licenses defining det(T) for an OPERATOR T, by taking the determinant of its matrix in any basis.",
  hyps=["P invertible"],
  field_scope="any_field",
  proof=("multiplicativity: det(P^{-1})det(A)det(P) = det(A) since det(P^{-1}) = 1/det(P) and F is commutative", "determinant_multiplicative", "core", "LinAlg.det_conj_invariant"),
  spec=["P orthogonal: det(P) = +-1, so det is also a CONGRUENCE invariant in that special case",
        "but for general congruence det(P^TAP) = det(P)^2 det(A), which is NOT invariant -- only its SIGN is (see sylvester_law_of_inertia)"],
  cxd={"similarity_versus_congruence": "under congruence the determinant scales by det(P)^2 > 0 over R. So congruence preserves the SIGN of det but not its value -- which is exactly the one-dimensional shadow of Sylvester's law"},
  misuse=["assuming det is a congruence invariant"],
  related={"required_by": ["characteristic_polynomial", "similar_invariants"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("determinant_rank_minors", "theorem", "determinants",
  "rank A = the largest r for which some r x r submatrix of A (an r x r MINOR) has nonzero determinant. A THEOREM -- not the definition of rank (edges/cycles.md, cycle 3).",
  parseable="rank A = max{r : some r x r minor of A is nonzero}",
  symbols={"an r x r minor": "the determinant of the submatrix on some r rows and r columns"},
  tcn="Well-formed. It relates two independently defined quantities -- rank as a dimension, minors as determinants -- and is exactly the bridge cycle 3 was resolved to make possible.",
  hyps=["A in F^{m x n}"],
  field_scope="any_field",
  proof=("if rank A = r, pick r independent columns and then r independent rows of the resulting m x r matrix: the r x r block is invertible so its determinant is nonzero. Conversely a nonzero r x r minor exhibits r independent columns", "matrix_rank", "core", "LinAlg.rank_eq_max_minor (3x3 decide instance)"),
  spec=["r = 1: rank >= 1 iff some entry is nonzero",
        "r = n = m: rank n iff det A != 0, recovering determinant_invertible_iff",
        "the leading principal minors version is Sylvester's criterion for positive definiteness (psd_characterisations) -- but that needs the LEADING PRINCIPAL minors, which is a strictly stronger condition than 'some minor'"],
  cxd={"all_minors_versus_leading_ones": "A = [[0,0],[0,-1]] has a nonzero leading... no: its leading 1x1 minor is 0 while it has a nonzero 2x2... take A = [[0,1],[1,0]]: both leading principal minors are 0 and -1, so 'leading minors nonzero' fails, yet rank A = 2. Only ALL minors, not the leading ones, characterise rank",
       "cost": "there are C(m,r)C(n,r) minors -- this is a characterisation, never an algorithm"},
  misuse=["computing rank by minors rather than elimination",
          "confusing this with Sylvester's criterion, which uses only LEADING PRINCIPAL minors and characterises positive definiteness, not rank"],
  related={"required_by": ["psd_characterisations"]},
  sources=["horn_johnson_2e", "hoffman_kunze_2e"], status="reviewed")

N("determinant_volume", "proposition", "determinants",
  "Over R, |det A| is the factor by which A scales n-dimensional (Lebesgue) volume, and sgn(det A) records whether A preserves or reverses orientation.",
  parseable="vol(A(S)) = |det A| * vol(S);  orientation preserved  <=>  det A > 0",
  symbols={"vol": "n-dimensional Lebesgue measure, CITED from outside this capsule",
           "A(S)": "the image of a measurable set S"},
  tcn="Well-formed only over R and only given a notion of volume, which this capsule does NOT construct -- the measure-theoretic content is cited. What IS internal is the uniqueness argument: any translation-invariant, multilinear-in-columns volume function must be a multiple of det.",
  hyps=["F = R", "a translation-invariant measure (cited)"],
  field_scope="real_or_complex",
  proof=("the volume of the parallelepiped spanned by the columns is multilinear and alternating in them and equals 1 on the unit cube, so by determinant_existence_uniqueness it IS |det|. The measure-theoretic justification that such a volume exists is cited", "determinant_multiplicative", "cited", None),
  spec=["n = 1: |det[a]| = |a|, the length scaling",
        "n = 2: |ad - bc| is the area of the parallelogram spanned by the columns",
        "A orthogonal: |det A| = 1, so rigid motions preserve volume -- see orthogonal_matrix",
        "A singular: det = 0 and the image lies in a proper subspace, of volume 0"],
  cxd={"the_field_being_R": "over C the analogue involves |det A|^2 as the real 2n-dimensional volume factor, not |det A|",
       "the_existence_of_volume": "the measure-theoretic input is genuinely external; this node is the capsule's clearest example of a result whose INTUITION is standard and whose FOUNDATION is cited"},
  misuse=["reading det as a volume in a general field where no measure exists",
          "forgetting the absolute value and reporting a negative volume"],
  apps=["the Jacobian factor in the change-of-variables theorem for multiple integrals",
        "the |det Sigma|^{-1/2} normalising constant of the multivariate normal density"],
  sources=["lang_algebra_3e", "strang_5e"], status="reviewed")

N("vandermonde_determinant", "identity", "determinants",
  "The Vandermonde matrix V with V_{ij} = x_i^{j-1} has det V = prod_{1 <= i < j <= n} (x_j - x_i). It is nonzero exactly when the x_i are pairwise DISTINCT.",
  parseable="det[x_i^{j-1}]_{i,j=1..n} = prod_{i<j} (x_j - x_i)",
  symbols={"x_1,...,x_n": "the nodes, type: elements of F",
           "V": "type: element of F^{n x n}"},
  tcn="Well-formed over any field. The product has C(n,2) factors.",
  hyps=["A square Vandermonde matrix"],
  field_scope="any_field",
  proof=("subtract x_1 times each column from the next (an R3-type column operation, which preserves det), factor (x_i - x_1) out of each row, and induct on n", "determinant_row_operations", "core", "LinAlg.vandermonde_2 / LinAlg.vandermonde_3 (decide instances over Int)"),
  spec=["n = 2: det[[1,x_1],[1,x_2]] = x_2 - x_1",
        "n = 3 at x = (1,2,3): det = (2-1)(3-1)(3-2) = 2",
        "two equal nodes: a repeated row, so det = 0 -- consistent with the product having a zero factor"],
  cxd={"distinctness_of_the_nodes": "with a repeated node the matrix is singular, which is exactly the statement that polynomial interpolation at repeated points is not uniquely solvable (one needs derivative data -- Hermite interpolation)"},
  misuse=["using the Vandermonde system to fit a high-degree polynomial numerically: it is notoriously ill-conditioned, with condition number growing exponentially in n. Use orthogonal polynomials or a QR-based fit instead (condition_number, qr_factorisation)"],
  apps=["existence and uniqueness of the Lagrange interpolating polynomial through n distinct points",
        "the design matrix of polynomial regression is a Vandermonde matrix -- and its ill-conditioning is why centring and orthogonal polynomial bases are standard practice"],
  sources=["horn_johnson_2e"], status="reviewed")
