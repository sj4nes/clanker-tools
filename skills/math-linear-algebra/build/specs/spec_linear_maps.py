"""Linear maps: kernel, image, rank-nullity, isomorphism, L(V,W), duality."""
from nodespec import N

N("linear_map", "definition", "linear_maps",
  "T: V -> W is linear if T(au + bv) = aT(u) + bT(v) for all a,b in F and u,v in V. Equivalently T preserves addition and scalar multiplication separately.",
  parseable="T(au + bv) = a T(u) + b T(v)  for all a,b in F, u,v in V",
  symbols={"T": "the map, type: function V -> W between F-vector spaces",
           "V, W": "vector spaces over the SAME field F"},
  tcn="Well-formed only when V and W are over the same field: aT(u) requires the scalar a to act on W. A map between an R-space and a C-space is linear only after fixing which field is acting.",
  hyps=["V, W vector spaces over the same field F"],
  field_scope="any_field",
  proof=("definition", None, "core", "LinAlg.linear_add / LinAlg.linear_smul (Int-coefficient encoding)"),
  spec=["T(0) = 0 always -- a linear map cannot be an affine translation",
        "V = W = F: the linear maps are exactly x -> cx; the map x -> x + 1 is affine, NOT linear",
        "differentiation on polynomials, and integration over a fixed interval, are both linear"],
  cxd={"homogeneity": "over C, complex CONJUGATION is additive and R-homogeneous but not C-homogeneous: conj(iz) = -i conj(z) != i conj(z). Such maps are called conjugate-linear (antilinear) and appear in the second slot of the complex inner product",
       "additivity": "x -> |x| on R is homogeneous for positive scalars only and fails additivity",
       "same_field": "a Q-linear map R -> R need not be R-linear: AC gives discontinuous additive functions (see basis_existence_general)"},
  misuse=["calling x -> Ax + b linear: it is affine. This is the standard abuse in machine learning and statistics, harmless only if the bias is absorbed into an extra coordinate",
          "assuming linear implies continuous: true in finite dimension, false in general"],
  apps=["every model of the form 'output is a weighted sum of inputs': regression, convolution, the Fourier and Laplace transforms, quantum-mechanical observables"],
  related={"required_by": ["kernel", "image_subspace", "matrix_of_linear_map"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("linear_map_determined_by_basis", "theorem", "linear_maps",
  "Given a basis (b_i)_{i in I} of V and ANY family (w_i)_{i in I} in W, there is EXACTLY ONE linear T: V -> W with T(b_i) = w_i for every i.",
  parseable="B basis of V, (w_i) arbitrary in W  =>  exists! linear T with T(b_i) = w_i",
  symbols={"(w_i)": "arbitrary target vectors -- no condition on them whatsoever"},
  tcn="Well-formed. The strength is that (w_i) is ARBITRARY: unlike group or ring homomorphisms, there are no relations to respect, which is why linear algebra is classification-complete.",
  hyps=["B a basis of V"],
  field_scope="any_field",
  proof=("define T(sum a_i b_i) := sum a_i w_i, well-defined by uniqueness of coordinates; linearity is immediate, uniqueness because two linear maps agreeing on a spanning set agree everywhere", "basis_unique_representation", "core", "LinAlg.extend_by_basis"),
  spec=["V = F^n with the standard basis: T is determined by the n columns T(e_j), which is exactly matrix_of_linear_map"],
  cxd={"B_being_a_basis": "for a merely SPANNING family the map need not exist: prescribing T(e_1) = T(e_2) = 0 but T(e_1 + e_2) = w != 0 is inconsistent. For a merely INDEPENDENT family T exists but is not unique -- it is unconstrained off the span"},
  misuse=["forgetting that this fails for a dependent family and 'defining' a map on a redundant generating set without checking consistency -- that check is exactly a well-definedness obligation"],
  related={"required_by": ["space_of_linear_maps", "dual_basis"]},
  sources=["axler_lada_4e"], status="reviewed")

N("kernel", "definition", "linear_maps",
  "ker T = {v in V : T(v) = 0}, the null space of T. It is a subspace of V.",
  parseable="ker T := T^{-1}({0}) = {v in V : T(v) = 0}",
  symbols={"ker T": "type: subspace of V (the DOMAIN)"},
  tcn="Well-formed and a subspace: 0 is in it, and linearity gives closure -- T(au + bv) = aT(u) + bT(v) = 0.",
  hyps=["T linear"],
  field_scope="any_field",
  proof=("subspace by the criterion applied to linearity", "subspace_criterion", "core", "LinAlg.kernel_subspace"),
  spec=["T = 0: ker T = V", "T injective: ker T = {0} (injective_iff_trivial_kernel)",
        "T = differentiation on polynomials: ker T = the constants, dimension 1"],
  cxd={"linearity_of_T": "for a nonlinear f, f^{-1}(0) is generally not a subspace: the preimage of 0 under x -> x^2 - 1 on R is {-1, 1}",
       "the_target_being_zero": "T^{-1}({w}) for w != 0 is an AFFINE subspace (a coset of ker T), not a subspace -- this is exactly the solution-set structure of linear_system"},
  misuse=["confusing ker T (in V) with the zero subspace of W",
          "computing 'the kernel' of a matrix without saying whether the matrix acts on the left or right -- null(A) and null(A^T) are different (left_null_space)"],
  related={"required_by": ["rank_nullity", "eigenspace", "null_space"]},
  sources=["axler_lada_4e"], status="reviewed")

N("image_subspace", "definition", "linear_maps",
  "im T = T(V) = {T(v) : v in V}, the range of T. It is a subspace of W.",
  parseable="im T := {T(v) : v in V}",
  symbols={"im T": "type: subspace of W (the CODOMAIN)"},
  tcn="Well-formed and a subspace: it contains T(0) = 0 and is closed because aT(u) + bT(v) = T(au + bv).",
  hyps=["T linear"],
  field_scope="any_field",
  proof=("closure from linearity", "subspace_criterion", "core", "LinAlg.image_subspace"),
  spec=["T surjective: im T = W", "T = 0: im T = {0}",
        "T = x -> Ax: im T = col(A), the column space -- this identification is why column_space is defined as it is"],
  cxd={"linearity": "the image of a nonlinear map is usually not a subspace: x -> x^2 maps R onto [0, infinity)"},
  misuse=["conflating the image (a subspace of W) with the codomain W: T is surjective exactly when they coincide, which is a theorem to prove, not a definition"],
  related={"required_by": ["rank", "first_isomorphism_theorem"]},
  sources=["axler_lada_4e"], status="reviewed")

N("injective_iff_trivial_kernel", "proposition", "linear_maps",
  "A linear map T is injective if and only if ker T = {0}.",
  parseable="T injective  <=>  ker T = {0}",
  symbols={"{0}": "the zero subspace of V"},
  tcn="Well-formed. The reduction of a global condition (injectivity, a statement about all pairs) to a local one (a single subspace being trivial) is available ONLY for linear maps, and it is what makes injectivity computable by elimination.",
  hyps=["T linear"],
  field_scope="any_field",
  proof=("T(u) = T(v) iff T(u - v) = 0 iff u - v in ker T", "linear_map", "core", "LinAlg.injective_iff_ker_trivial"),
  spec=["V = F^n: T injective iff Ax = 0 has only the trivial solution iff A has full column rank"],
  cxd={"linearity": "for nonlinear f, f^{-1}(0) = {0} does not give injectivity: x -> x^2 on R has only 0 in its zero set but is not injective"},
  misuse=["applying the criterion to an affine map x -> Ax + b: injectivity is still governed by ker A, but the 'zero set' is not the kernel"],
  related={"required_by": ["injective_surjective_equivalence"]},
  sources=["axler_lada_4e"], status="reviewed")

N("rank", "definition", "linear_maps",
  "rank T = dim(im T). DEFINED WITHOUT DETERMINANTS OR MINORS -- see edges/cycles.md, cycle 3.",
  parseable="rank T := dim(im T)",
  symbols={"rank T": "type: natural number (or cardinal), at most dim W and at most dim V"},
  tcn="Well-formed whenever im T is finite-dimensional, which holds automatically when V is (the image of a spanning set spans the image).",
  hyps=["im T finite-dimensional"],
  field_scope="any_field",
  spec=["T = 0: rank 0", "T an isomorphism: rank = dim V = dim W",
        "T a projection onto a k-dimensional subspace: rank k"],
  cxd={"finite_dimensionality_of_the_image": "for an infinite-rank operator the number is a cardinal and the counting theorems below carry no content"},
  misuse=["defining rank by minors and then using it before determinants exist -- the minor characterisation is the downstream theorem determinant_rank_minors",
          "assuming rank(AB) = rank(A)rank(B): rank is not multiplicative, see rank_inequalities"],
  related={"required_by": ["rank_nullity", "matrix_rank"]},
  sources=["axler_lada_4e"], status="reviewed")

N("nullity", "definition", "linear_maps",
  "nullity T = dim(ker T), the dimension of the solution space of the homogeneous equation T(v) = 0.",
  parseable="nullity T := dim(ker T)",
  symbols={"nullity T": "type: natural number, at most dim V"},
  tcn="Well-formed when ker T is finite-dimensional, automatic if V is.",
  field_scope="any_field",
  spec=["T injective: nullity 0", "T = 0 on V: nullity = dim V"],
  cxd={"none_all_hypotheses_essential": "a definition; the content is entirely in rank_nullity"},
  misuse=["reading nullity as a count of SOLUTIONS rather than a DIMENSION: over an infinite field a one-dimensional kernel already contains infinitely many solutions (over F_q it contains exactly q)"],
  sources=["hoffman_kunze_2e"], status="reviewed")

N("rank_nullity", "theorem", "linear_maps",
  "For T: V -> W with V finite-dimensional: dim V = rank T + nullity T.",
  parseable="dim V = dim(im T) + dim(ker T)",
  symbols={"dim V": "dimension of the DOMAIN -- not of W"},
  tcn="Well-formed; note the theorem constrains the DOMAIN's dimension, so nothing follows about W. This is the most-misquoted point about it.",
  hyps=["V finite-dimensional", "T linear"],
  field_scope="any_field",
  proof=("take a basis of ker T, extend it to a basis of V; the images of the added vectors are a basis of im T -- spanning by linearity, independent because a vanishing combination of images puts a combination of the added vectors into ker T", "basis_existence_finite", "core", "LinAlg.rank_nullity (Nat identity from the two basis counts)"),
  spec=["T injective: rank T = dim V, so im T is a copy of V inside W",
        "T = 0: rank 0, nullity dim V",
        "A in F^{m x n}: n = rank A + dim null(A), the count that drives gaussian_elimination's free-variable bookkeeping"],
  cxd={"finite_dimensionality_of_V": "the right shift S on F^infinity (sequences) is injective with nullity 0 but NOT surjective; and the left shift is surjective with nullity 1. Neither satisfies any finite count. Rank-nullity is the canonical casualty of dropping finite_dimensional",
       "linearity": "for a nonlinear map neither side is defined"},
  misuse=["quoting it as dim W = rank + nullity -- it is dim V",
          "concluding surjectivity from full rank without knowing dim W = dim V (that is injective_surjective_equivalence, which needs the extra hypothesis)"],
  apps=["the degrees-of-freedom count in every linear model: n observations, p parameters, n - rank(X) residual degrees of freedom",
        "the constraint count in linear programming and in structural mechanics (Maxwell's rule for determinacy)"],
  related={"required_by": ["injective_surjective_equivalence", "four_subspaces"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("injective_surjective_equivalence", "corollary", "linear_maps",
  "If dim V = dim W < infinity then for T: V -> W the conditions injective, surjective, and bijective are EQUIVALENT.",
  parseable="dim V = dim W < infinity  =>  (T injective <=> T surjective <=> T bijective)",
  symbols={"T": "a linear map between spaces of equal finite dimension"},
  tcn="Well-formed. Both the equality of dimensions AND their finiteness are needed; the node carries edges to finite_dimensional for exactly this reason.",
  hyps=["dim V = dim W", "V finite-dimensional"],
  field_scope="any_field",
  proof=("injective gives nullity 0, so rank = dim V = dim W, so im T = W by dimension_of_subspace; conversely surjective gives rank = dim W = dim V, so nullity 0", "rank_nullity", "core", "LinAlg.inj_iff_surj_of_dim_eq"),
  spec=["V = W = F^n: a square matrix is injective iff surjective iff invertible -- one clause of invertibility_equivalences",
        "the finite-field analogue of the pigeonhole principle for linear maps"],
  cxd={"finite_dimensionality": "on F[t], multiplication by t is injective but not surjective (nothing maps to the constant 1); formal differentiation is surjective but not injective. Both live on the SAME space, so equality of dimensions holds trivially and it is finiteness alone that fails",
       "equal_dimensions": "F^2 -> F^3 can be injective and never surjective; F^3 -> F^2 can be surjective and never injective"},
  misuse=["applying it to operators on function spaces, where it is exactly false and is the reason Fredholm theory exists",
          "applying it to a non-square matrix"],
  related={"illustrated_by": ["infinite_dimensional_boundary"]},
  sources=["axler_lada_4e"], status="reviewed")

N("linear_isomorphism", "definition", "linear_maps",
  "An isomorphism is a bijective linear map. Its set-theoretic inverse is AUTOMATICALLY linear, so 'isomorphism' needs no separate condition on the inverse.",
  parseable="T isomorphism  <=>  T linear and bijective;  and then T^{-1} is linear",
  symbols={"T^{-1}": "the inverse map, type: linear map W -> V"},
  tcn="Well-formed. The automatic linearity of the inverse is genuine content: T^{-1}(aw_1 + bw_2) = T^{-1}(aT(v_1) + bT(v_2)) = T^{-1}(T(av_1 + bv_2)) = av_1 + bv_2.",
  hyps=["T linear and bijective"],
  field_scope="any_field",
  proof=("apply T to both sides of the claimed linearity of T^{-1} and use injectivity", "bijection", "core", "LinAlg.inverse_is_linear"),
  spec=["the coordinate map [.]_B is an isomorphism V -> F^n (coordinate_isomorphism)",
        "V isomorphic to W iff dim V = dim W, for finite-dimensional spaces over the same field"],
  cxd={"bijectivity": "an injective non-surjective linear map has a left inverse but no two-sided one",
       "linearity_of_T": "a nonlinear bijection has a nonlinear inverse, and none of the structure transfers"},
  misuse=["requiring separately that the inverse be linear -- it is free here, unlike for continuous maps (a continuous bijection need not have continuous inverse) or smooth maps"],
  related={"required_by": ["invertible_operator", "first_isomorphism_theorem"]},
  sources=["axler_lada_4e"], status="reviewed")

N("first_isomorphism_theorem", "theorem", "linear_maps",
  "For linear T: V -> W, the map v + ker T -> T(v) is a well-defined isomorphism V/ker T -> im T.",
  parseable="V / ker T  isomorphic to  im T,  via v + ker T -> T(v)",
  symbols={"V/ker T": "the quotient by the kernel", "im T": "the image in W"},
  tcn="The type check is the WELL-DEFINEDNESS check: if v + ker T = v' + ker T then v - v' is in ker T so T(v) = T(v'). This is the canonical instance of well_defined_on_quotient.",
  hyps=["T linear"],
  field_scope="any_field",
  welldef="Independence of the coset representative is exactly the definition of ker T, so the map is well-defined for EVERY linear T with no extra hypothesis.",
  proof=("well-definedness as above; injectivity because T(v) = 0 puts v in ker T, i.e. the coset is zero; surjectivity onto im T by construction", "quotient_space", "core", "LinAlg.first_iso_welldefined"),
  spec=["T injective: V/{0} isomorphic to V, so T is an isomorphism onto its image",
        "taking dimensions recovers rank_nullity via quotient_dimension -- the two theorems are the same fact, one counted and one structural"],
  cxd={"linearity": "for a nonlinear map the fibres are not cosets of a subspace and there is no quotient to form",
       "none_all_hypotheses_essential": "no finite-dimensionality is needed: this is the structural statement that SURVIVES in infinite dimension while its dimension-counting shadow (rank_nullity) does not"},
  misuse=["treating V/ker T as a subspace of V: see quotient_space"],
  related={"equivalent_to": [], "required_by": []},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("space_of_linear_maps", "structure", "linear_maps",
  "L(V,W) is the set of linear maps V -> W, a vector space under (S+T)(v) = S(v) + T(v) and (aT)(v) = aT(v). If dim V = n and dim W = m are finite then dim L(V,W) = mn.",
  parseable="L(V,W) vector space;  dim L(V,W) = dim V * dim W  when both are finite",
  symbols={"L(V,W)": "type: vector space over F", "L(V)": "abbreviation for L(V,V)"},
  tcn="Well-formed: the pointwise operations land in L(V,W) because a sum and a scalar multiple of linear maps is linear.",
  hyps=["V, W over the same field F"],
  field_scope="any_field",
  proof=("the dimension count follows from linear_map_determined_by_basis: a map is freely specified by the n images, each with m coordinates", "linear_map_determined_by_basis", "core", "LinAlg.dim_hom_eq_mul"),
  spec=["W = F: L(V,F) = V^*, the dual space, of dimension n",
        "V = W = F^n: L(V) is isomorphic to F^{n x n}, of dimension n^2"],
  cxd={"finite_dimensionality": "for infinite-dimensional V the algebraic dual and hence L(V,W) is strictly larger than V in cardinality, and dim L(V,F) > dim V -- which is exactly why double_dual fails"},
  misuse=["assuming L(V) is commutative: composition is not (matrix_mult_noncommutative)"],
  related={"required_by": ["dual_space", "minimal_polynomial"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("composition_of_linear_maps", "proposition", "linear_maps",
  "The composite of linear maps is linear; composition is associative, and bilinear in each argument: (S_1 + S_2)T = S_1T + S_2T, S(T_1 + T_2) = ST_1 + ST_2, (aS)T = S(aT) = a(ST).",
  parseable="S, T linear => S o T linear;  associative;  bilinear over L",
  symbols={"S o T": "written ST, type: linear map V -> X when T: V -> W and S: W -> X"},
  tcn="Well-formed only when the shapes conform (codomain of T equals domain of S); this conformability is the abstract source of the matrix shape rule.",
  hyps=["shapes conformable"],
  field_scope="any_field",
  proof=("direct substitution; associativity is inherited from composition of functions", "linear_map", "core", "LinAlg.comp_linear / LinAlg.comp_assoc"),
  spec=["V = W = X: L(V) becomes an associative unital algebra, the setting for minimal_polynomial and cayley_hamilton"],
  cxd={"conformability": "composing non-conformable maps is not a false statement but an ill-typed one -- the type check is the whole content",
       "commutativity_is_NOT_available": "ST != TS in general; see matrix_mult_noncommutative"},
  misuse=["cancelling: ST = SU does not give T = U unless S is injective"],
  related={"required_by": ["linear_operator", "matrix_mult_is_composition", "dual_map"]},
  sources=["axler_lada_4e"], status="reviewed")

N("linear_operator", "definition", "linear_maps",
  "A linear operator on V is a linear map T: V -> V. L(V) is an associative unital algebra under composition, with identity I.",
  parseable="T in L(V) := L(V,V);  (L(V), +, o, I) an associative unital F-algebra",
  symbols={"I": "the identity operator, type: element of L(V)",
           "T^k": "the k-fold composite, with T^0 := I"},
  tcn="Well-formed. Same domain and codomain is what makes powers T^k, hence polynomials in T, meaningful -- the whole of eigentheory depends on it.",
  field_scope="any_field",
  spec=["V = F^n: L(V) is isomorphic to the algebra F^{n x n}",
        "p(T) for p in F[t] is defined because T^k makes sense -- the map p -> p(T) is the algebra homomorphism whose kernel defines minimal_polynomial"],
  cxd={"equal_domain_and_codomain": "for T: V -> W with V != W, T^2 is undefined, so eigenvalues, characteristic polynomials, and diagonalisability are all meaningless. This is why SVD (which handles rectangular A) needs two different bases"},
  misuse=["applying eigenvalue language to a non-square matrix -- use singular_values instead"],
  related={"required_by": ["eigenvalue", "invertible_operator", "minimal_polynomial"]},
  sources=["axler_lada_4e"], status="reviewed")

N("invertible_operator", "definition", "linear_maps",
  "T in L(V) is invertible if there is S in L(V) with ST = TS = I. In FINITE dimension a one-sided inverse is automatically two-sided.",
  parseable="T invertible  <=>  exists S in L(V) with ST = TS = I;  finite dim: ST = I => TS = I",
  symbols={"S = T^{-1}": "the inverse operator, unique when it exists"},
  tcn="Well-formed. Uniqueness of the inverse follows from associativity: S = S(TS') = (ST)S' = S'.",
  hyps=["V finite-dimensional for the one-sided clause"],
  field_scope="any_field",
  proof=("one-sided suffices: ST = I forces T injective, hence surjective by injective_surjective_equivalence, hence bijective", "injective_surjective_equivalence", "core", "LinAlg.one_sided_inverse_suffices"),
  spec=["V = F^n: invertible operators correspond to invertible matrices (invertibility_equivalences)"],
  cxd={"finite_dimensionality": "on F[t] the shift operators L (delete the constant term) and R (multiply by t) satisfy LR = I but RL != I. A one-sided inverse is genuinely weaker in infinite dimension, and this pair is the standard witness"},
  misuse=["checking only ST = I for an operator on a function space and concluding invertibility"],
  related={"required_by": ["invertible_matrix"]},
  sources=["axler_lada_4e"], status="reviewed")

N("dual_space", "definition", "linear_maps",
  "V^* = L(V, F), the space of linear FUNCTIONALS on V (linear maps into the scalar field).",
  parseable="V^* := L(V, F) = {f : V -> F, f linear}",
  symbols={"f": "a functional, type: element of V^*", "V^*": "type: vector space over F"},
  tcn="Well-formed as a special case of L(V,W) with W = F, which is a one-dimensional F-space.",
  field_scope="any_field",
  spec=["V = F^n: V^* is the space of row vectors, f(x) = a^T x -- the transpose convention made concrete",
        "V = polynomials of degree <= d: evaluation at a point, and integration over an interval, are functionals"],
  cxd={"finite_dimensionality": "dim V^* = dim V only in finite dimension (dual_basis). For infinite-dimensional V the ALGEBRAIC dual is strictly larger in cardinality, which is why double_dual fails there"},
  misuse=["identifying V with V^* without an inner product: the identification requires a choice of basis and is NOT canonical. With an inner product it becomes canonical (the Riesz correspondence), which is why the inner-product half of the capsule can use A^T freely"],
  related={"required_by": ["dual_basis", "dual_map", "annihilator"]},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("dual_basis", "theorem", "linear_maps",
  "If B = (b_1,...,b_n) is a basis of a finite-dimensional V, the functionals b_i^* defined by b_i^*(b_j) = delta_{ij} form a basis of V^*. Hence dim V^* = dim V.",
  parseable="b_i^*(b_j) = delta_{ij};  (b_1^*,...,b_n^*) a basis of V^*;  dim V^* = dim V",
  symbols={"b_i^*": "the i-th coordinate functional, type: element of V^*",
           "delta_{ij}": "1 if i = j, else 0"},
  tcn="Well-formed: each b_i^* exists and is unique by linear_map_determined_by_basis applied with W = F. Note b_i^* depends on the WHOLE basis B, not just on b_i.",
  hyps=["V finite-dimensional"],
  field_scope="any_field",
  proof=("independence: applying a vanishing combination to b_j isolates the j-th coefficient. Spanning: f = sum_i f(b_i) b_i^* since both sides agree on the basis", "linear_map_determined_by_basis", "core", "LinAlg.dual_basis_spans"),
  spec=["V = F^n with the standard basis: b_i^* is the i-th coordinate function, the i-th row of I"],
  cxd={"finite_dimensionality": "for V = F[t] with basis (1, t, t^2, ...), the functionals t^i -> coefficient are independent but do NOT span V^*: the functional 'sum of all coefficients' is not a finite combination of them. dim V^* > dim V strictly",
       "the_whole_basis_matters": "b_1^* changes if b_2 is replaced, even though b_1 is untouched -- the dual basis is not computed entrywise"},
  misuse=["treating b_i^* as depending only on b_i, which leads to wrong duals after a partial basis change"],
  related={"required_by": ["double_dual", "annihilator"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("dual_map", "definition", "linear_maps",
  "For T: V -> W the dual (transpose) map T^t: W^* -> V^* is T^t(g) = g o T. It REVERSES direction, and (ST)^t = T^t S^t.",
  parseable="T^t : W^* -> V^*,  T^t(g) := g o T;  (S o T)^t = T^t o S^t",
  symbols={"T^t": "the dual map, type: linear map W^* -> V^*",
           "g": "a functional on W, type: element of W^*"},
  tcn="Well-formed: g o T is a composite of linear maps V -> W -> F, hence an element of V^*. The direction reversal is forced by the types, and it is the abstract reason (AB)^T = B^T A^T.",
  hyps=["T linear"],
  field_scope="any_field",
  proof=("linearity of T^t from bilinearity of composition; the contravariance from associativity", "composition_of_linear_maps", "core", "LinAlg.dual_map_contravariant"),
  spec=["in dual bases, the matrix of T^t is the TRANSPOSE of the matrix of T -- the conceptual content of the transpose operation",
        "T = I: T^t = I"],
  cxd={"none_all_hypotheses_essential": "the definition needs only linearity; it is the MATRIX statement that needs finite-dimensionality and a choice of dual bases"},
  misuse=["expecting T^t: V^* -> W^*: the arrow reverses. Every 'why is it B^T A^T and not A^T B^T' question is answered here",
          "confusing the dual map (no inner product needed) with the ADJOINT T^* (needs an inner product and conjugates over C) -- they agree over R under the Riesz identification only"],
  related={"dual_of": ["transpose"], "contrasts_with": ["adjoint_operator"]},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("double_dual", "theorem", "linear_maps",
  "The evaluation map ev: V -> V^**, ev(v)(f) = f(v), is linear and INJECTIVE for every V, and is an isomorphism exactly when V is finite-dimensional. It is CANONICAL -- defined with no choice of basis.",
  parseable="ev(v)(f) := f(v);  ev injective always;  ev isomorphism  <=>  dim V < infinity",
  symbols={"ev": "the canonical evaluation, type: linear map V -> V^**",
           "V^**": "the dual of the dual"},
  tcn="Well-formed: ev(v) is a functional on V^*, since f -> f(v) is linear in f. The definition mentions no basis, which is precisely the point.",
  hyps=["V finite-dimensional for surjectivity"],
  field_scope="any_field",
  proof=("injectivity: if v != 0 extend v to a basis and take the coordinate functional, which is nonzero on v (this uses basis existence, hence AC in infinite dimension). Surjectivity in finite dimension by the dimension count dim V^** = dim V^* = dim V", "dual_basis", "core", "LinAlg.eval_injective"),
  choice_grade="choice_free",
  spec=["V = F^n: ev identifies column vectors with functionals on row vectors, the familiar 'a vector is what it does to covectors'"],
  cxd={"finite_dimensionality": "for V = F[t], dim V^* > dim V and dim V^** > dim V^*, so ev is injective but far from surjective. In functional analysis the CONTINUOUS dual restores surjectivity for reflexive spaces only -- L^1 is a standard non-reflexive example",
       "canonicity_vs_existence": "V is isomorphic to V^* in finite dimension too, but only after choosing a basis. The point of this node is that V -> V^** needs NO choice, which is what makes 'double dual' a genuine identification and 'dual' merely an isomorphism"},
  misuse=["citing dim V = dim V^* as if it made V and V^* canonically the same -- it does not, and the distinction is the whole content of tensor variance (contravariant vs covariant indices)"],
  related={"illustrated_by": ["infinite_dimensional_boundary"]},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("annihilator", "definition", "linear_maps",
  "For a subspace U of V, the annihilator is U^0 = {f in V^* : f(u) = 0 for all u in U}, a subspace of V^*. In finite dimension dim U + dim U^0 = dim V.",
  parseable="U^0 := {f in V^* : f|_U = 0};  dim U + dim U^0 = dim V  (finite dim)",
  symbols={"U^0": "type: subspace of V^*"},
  tcn="Well-formed: the conditions f(u) = 0 are linear in f, so U^0 is an intersection of kernels of functionals on V^*, hence a subspace.",
  hyps=["V finite-dimensional for the dimension identity"],
  field_scope="any_field",
  proof=("extend a basis of U to a basis of V; the dual functionals indexed by the ADDED vectors form a basis of U^0", "dual_basis", "core", "LinAlg.dim_annihilator"),
  spec=["U = {0}: U^0 = V^*", "U = V: U^0 = {0}",
        "U a hyperplane: U^0 is one-dimensional, spanned by any functional cutting it out"],
  cxd={"finite_dimensionality": "the dimension identity fails in infinite dimension, where it must be replaced by a statement about codimension",
       "no_inner_product_needed": "U^0 lives in V^*, NOT in V. The analogous object inside V is the orthogonal complement U^perp, which requires an inner product; conflating them is the standard error"},
  misuse=["writing U^0 as a subspace of V -- it is a subspace of the DUAL. The two coincide only after the Riesz identification supplied by an inner product"],
  related={"dual_of": ["orthogonal_complement"]},
  sources=["hoffman_kunze_2e"], status="reviewed")
