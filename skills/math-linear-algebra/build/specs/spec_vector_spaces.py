"""Vector spaces: axioms, subspaces, span, independence, bases, dimension,
sums and quotients."""
from nodespec import N

N("vector_space", "structure", "vector_spaces",
  "A vector space over F is an abelian group (V, +, 0) together with a scalar multiplication F x V -> V satisfying a(bv) = (ab)v, 1v = v, a(u+v) = au + av, and (a+b)v = av + bv.",
  parseable="(V,+,0) abelian group;  a(bv) = (ab)v;  1v = v;  a(u+v) = au+av;  (a+b)v = av+bv",
  symbols={"V": "the vector space, type: set with + and scalar multiplication",
           "F": "the scalar field", "v, u": "vectors, type: elements of V",
           "a, b": "scalars, type: elements of F", "0": "the zero VECTOR (distinct from the zero SCALAR)"},
  tcn="Well-formed: + has type V x V -> V and scalar multiplication F x V -> V, so every expression above type-checks. The overloading of 0 (scalar and vector) and of + (in F and in V) is resolved by position.",
  hyps=["F is a field"],
  field_scope="any_field",
  spec=["V = F^n with componentwise operations -- the model for the whole capsule",
        "V = F itself, a one-dimensional space over F",
        "V = {0}, the zero space: a legitimate vector space with the empty basis and dimension 0",
        "V = F[t], or the set of functions X -> F -- infinite-dimensional instances showing the axioms alone do not give finiteness"],
  cxd={"the_axiom_1v_equals_v": "without it, the map a*v := 0 for all a satisfies every other axiom on any abelian group, making scalar multiplication trivial. It is the axiom that ties the field action to the group and is the one most often omitted from informal lists",
       "F_being_a_field": "over a ring the structure is a MODULE: Z/6 as a Z-module has torsion, no basis, and no well-defined dimension"},
  misuse=["assuming an inner product, a norm, or coordinates: none is part of the definition. A bare vector space has no notion of length, angle, or preferred basis",
          "assuming finite-dimensionality (see finite_dimensional)"],
  apps=["the ambient structure for every linear model in statistics, every state space in control theory, and every feature space in machine learning",
        "solution sets of homogeneous linear ODEs are vector spaces, which is why superposition works"],
  related={"required_by": ["subspace", "linear_map", "inner_product"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("vector_space_basic_consequences", "proposition", "vector_spaces",
  "In any vector space: 0v = 0, a0 = 0, (-1)v = -v, and av = 0 implies a = 0 or v = 0.",
  parseable="0v = 0;  a0 = 0;  (-1)v = -v;  av = 0 => (a = 0 or v = 0)",
  symbols={"0": "context-dependent: the zero scalar on the left of 0v, the zero vector elsewhere"},
  tcn="Well-formed. The last clause is the 'no zero divisors' statement and is exactly where the FIELD structure (invertibility of nonzero a) is used, not merely the ring axioms.",
  hyps=["V a vector space over a field F"],
  field_scope="any_field",
  proof=("0v = (0+0)v = 0v + 0v, then cancel in the abelian group; for the last clause, if a != 0 multiply by a^{-1}", "vector_space", "core", "LinAlg.smul_zero_eq_zero / LinAlg.no_zero_smul_divisors (Nat/Int encoding)"),
  spec=["in F^n these are the componentwise facts, but the proof uses only the axioms and so holds in every V"],
  cxd={"invertibility_of_nonzero_scalars": "over the ring Z/6 as a module over itself, 2*3 = 0 with neither factor zero -- the no-zero-divisors clause genuinely needs F to be a field"},
  misuse=["treating 0v = 0 as an axiom: it is a consequence, and deriving it is the standard check that an axiom list is not redundant"],
  sources=["axler_lada_4e"], status="reviewed")

N("subspace", "definition", "vector_spaces",
  "A subset U of V is a subspace if 0 is in U and U is closed under addition and scalar multiplication. Equivalently, U is a vector space under the operations restricted from V.",
  parseable="0 in U;  u, w in U => u + w in U;  a in F, u in U => au in U",
  symbols={"U": "the subspace, type: subset of V closed under the operations"},
  tcn="Well-formed. Requiring 0 in U rather than merely U nonempty is equivalent GIVEN closure under scalar multiplication (take a = 0), but stating it explicitly rules out the empty set, which satisfies both closure conditions vacuously and is NOT a vector space.",
  hyps=["V a vector space", "U a subset of V"],
  field_scope="any_field",
  spec=["{0} and V are subspaces of V (the trivial ones)",
        "in R^3: the origin, every line through the origin, every plane through the origin, and R^3 -- and nothing else",
        "ker T and im T are subspaces, which is what makes rank and nullity dimensions"],
  cxd={"containing_zero": "the empty set is closed under both operations vacuously but is not a vector space -- the axioms require an identity",
       "closure_under_scalar_multiplication": "the first quadrant of R^2 is closed under addition but not under multiplication by -1",
       "closure_under_addition": "the union of the two coordinate axes in R^2 is closed under scalar multiplication but (1,0) + (0,1) leaves it -- this is why subspace_intersection is about INTERSECTIONS, not unions"},
  misuse=["calling an affine subspace (a line NOT through the origin) a subspace -- solution sets of Ax = b are affine, not linear, unless b = 0; see linear_system"],
  related={"required_by": ["span", "kernel", "image_subspace"]},
  sources=["axler_lada_4e"], status="reviewed")

N("subspace_criterion", "proposition", "vector_spaces",
  "A nonempty subset U of V is a subspace if and only if au + bw is in U for all a, b in F and all u, w in U.",
  parseable="U != empty;  U subspace <=> (for all a,b in F, u,w in U: au + bw in U)",
  symbols={"a, b": "arbitrary scalars", "u, w": "arbitrary elements of U"},
  tcn="Well-formed. The single condition packages both closures; nonemptiness then supplies 0 by taking a = b = 0.",
  hyps=["U nonempty"],
  field_scope="any_field",
  proof=("forward by applying the two closures in turn; backward by specialising (a,b) to (1,1) and (a,0)", "subspace", "core", "LinAlg.subspace_criterion (Int-coefficient encoding)"),
  spec=["a = b = 0 recovers 0 in U; b = 0 recovers closure under scalars; a = b = 1 recovers closure under addition"],
  cxd={"nonemptiness": "the empty set satisfies the quantified condition vacuously; without nonemptiness the criterion would wrongly certify it as a subspace"},
  misuse=["checking only a = b = 1 (closure under addition) and concluding subspace -- see the quadrant counterexample on subspace"],
  sources=["axler_lada_4e"], status="reviewed")

N("subspace_intersection", "proposition", "vector_spaces",
  "An arbitrary intersection of subspaces of V is a subspace. A UNION of two subspaces is a subspace if and only if one contains the other.",
  parseable="U_i subspaces => intersection of U_i is a subspace;  U union W subspace <=> (U subset W or W subset U)",
  symbols={"(U_i)_{i in I}": "an arbitrary family of subspaces, I possibly infinite"},
  tcn="Well-formed for an arbitrary index set: the intersection of the empty family is V by convention, which is a subspace. The union clause is an iff, so it also records when the union IS a subspace.",
  hyps=["each U_i a subspace of V"],
  field_scope="any_field",
  proof=("intersection: each closure is checked coordinatewise across the family. Union: if neither contains the other pick u in U\\W and w in W\\U; then u + w lies in neither", "subspace_criterion", "core", "LinAlg.inter_subspace / LinAlg.union_subspace_iff"),
  spec=["two distinct lines through the origin in R^2 intersect in {0}, a subspace, but their union is not",
        "the intersection of all subspaces containing S is span(S) -- this is span_is_smallest_subspace"],
  cxd={"intersection_rather_than_union": "the x-axis and y-axis in R^2 are subspaces; their union contains (1,0) and (0,1) but not (1,1)"},
  misuse=["forming 'the subspace generated by U and W' as the union -- it is the SUM U + W (sum_of_subspaces)"],
  related={"required_by": ["span_is_smallest_subspace", "direct_sum"]},
  sources=["axler_lada_4e"], status="reviewed")

N("linear_combination", "definition", "vector_spaces",
  "A linear combination of a family (v_i)_{i in I} is a finite sum sum_{i in J} a_i v_i with J a FINITE subset of I and a_i in F.",
  parseable="sum_{i in J} a_i v_i,  J finite subset of I,  a_i in F",
  symbols={"J": "a finite index subset, type: finite subset of I",
           "a_i": "coefficients, type: elements of F"},
  tcn="Well-formed. FINITENESS of J is essential and is not a technicality: a vector space has no topology, so an infinite sum has no meaning. This is what keeps span, independence, and basis purely algebraic.",
  hyps=["J finite"],
  field_scope="any_field",
  spec=["J empty: the empty sum is 0 by convention, which is why span(empty) = {0}",
        "|J| = 1: the combinations of a single v are the scalar multiples of v"],
  cxd={"finiteness": "allowing infinite sums requires convergence, hence a topology and completeness -- that is Hilbert space theory, not linear algebra. Under the algebraic definition, the standard basis of the space of ALL sequences does NOT span it: (1,1,1,...) is not a finite combination of the e_i. This single point is why a Hamel basis of an infinite-dimensional space is enormous and unusable"},
  misuse=["writing 'v = sum_{i=1}^infinity a_i e_i' inside a vector-space argument -- that is a limit statement and needs a norm"],
  related={"required_by": ["span", "linear_independence"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("span", "definition", "vector_spaces",
  "span(S) is the set of all linear combinations of finitely many elements of S. By convention span(empty) = {0}.",
  parseable="span(S) = {sum_{i=1}^k a_i v_i : k >= 0, a_i in F, v_i in S};  span(empty) = {0}",
  symbols={"S": "a subset of V, possibly infinite", "span(S)": "type: subspace of V"},
  tcn="Well-formed for any S, finite or not, because only finite subsums are formed. The empty convention is forced by the empty-sum convention, not chosen freely.",
  field_scope="any_field",
  spec=["span of a single nonzero v is the line Fv",
        "span(empty) = {0}, so the empty set is a basis of the zero space and dim{0} = 0",
        "span of the standard basis of F^n is F^n"],
  cxd={"none_all_hypotheses_essential": "span is a definition with no hypotheses; the only trap is the empty case, which is why the convention is stated"},
  misuse=["confusing span(S) with S: span is closed under the operations, S need not be",
          "assuming span(S) has |S| dimensions -- only if S is independent (dimension_well_defined)"],
  related={"required_by": ["basis", "row_space", "column_space"]},
  sources=["axler_lada_4e"], status="reviewed")

N("span_is_smallest_subspace", "proposition", "vector_spaces",
  "span(S) is a subspace containing S, and it is contained in every subspace of V containing S. Equivalently, span(S) is the intersection of all subspaces containing S.",
  parseable="span(S) subspace;  S subset span(S);  (U subspace and S subset U) => span(S) subset U",
  symbols={"U": "any subspace containing S"},
  tcn="Well-formed. The two characterisations (constructive: finite combinations; and top-down: intersection of all containing subspaces) are proved equal, which is what licenses calling span(S) 'the subspace generated by S'.",
  hyps=["U a subspace containing S"],
  field_scope="any_field",
  proof=("closure of span(S) from the criterion; minimality because a subspace containing S is closed under the finite combinations that make up span(S), by induction on the number of terms", "subspace_criterion", "core", "LinAlg.span_minimal"),
  spec=["S a subspace already: then span(S) = S"],
  cxd={"U_being_a_subspace": "the minimality claim fails for arbitrary supersets: S is contained in many sets that do not contain span(S)"},
  misuse=["using the intersection characterisation to compute a span: it is a definition-level equivalence, not an algorithm. Computation goes through gaussian_elimination"],
  sources=["axler_lada_4e"], status="reviewed")

N("linear_independence", "definition", "vector_spaces",
  "A family (v_i)_{i in I} is linearly independent if the only finite linear combination of its members equal to 0 is the one with all coefficients 0. Otherwise it is dependent.",
  parseable="sum_{i in J} a_i v_i = 0 with J finite  =>  a_i = 0 for all i in J",
  symbols={"(v_i)": "a FAMILY (repetition allowed), type: indexed family in V"},
  tcn="Well-formed. Independence is a property of the indexed FAMILY, not of the underlying set: a family with a repeated vector is dependent even though its image set may be independent. The capsule uses families throughout for this reason.",
  field_scope="any_field",
  spec=["the empty family is independent vacuously",
        "a single vector v is independent iff v != 0",
        "any family containing 0 is dependent (take that coefficient 1, the rest 0)",
        "two vectors are independent iff neither is a scalar multiple of the other"],
  cxd={"all_coefficients_zero": "requiring only SOME coefficient to be nonzero would make every family dependent; the quantifier is over ALL representations of 0",
       "the_field": "over F_2 the vectors (1,1) and (1,1) are dependent, but independence is also field-sensitive in subtler ways: (1, sqrt 2) and (1, 1) are independent over Q as a Q-vector space and dependent over R in R^2 only if proportional -- always check which field"},
  misuse=["checking independence of n vectors in F^n by 'they look different' rather than by rank or determinant",
          "treating independence as a property of a SET when the family has repeats"],
  related={"required_by": ["basis", "dependence_lemma"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("dependence_lemma", "lemma", "vector_spaces",
  "If (v_1,...,v_k) is dependent and v_1 != 0, then there is a j >= 2 with v_j in span(v_1,...,v_{j-1}), and removing that v_j does not change the span.",
  parseable="dependent and v_1 != 0  =>  exists j >= 2 with v_j in span(v_1..v_{j-1}) and span(v_1..v_k) = span(list with v_j removed)",
  symbols={"j": "the index of the first redundant vector, type: element of {2,...,k}"},
  tcn="Well-formed. The ordering matters: the lemma identifies the FIRST redundant vector in the given order, which is what makes it usable as an induction step.",
  hyps=["the list is dependent", "v_1 != 0", "the list is finite"],
  field_scope="any_field",
  proof=("take a nontrivial vanishing combination, let j be the largest index with a_j != 0; j >= 2 since v_1 != 0; solve for v_j using a_j^{-1}", "linear_independence", "core", "LinAlg.dependence_step"),
  spec=["k = 2: if v_2 = c v_1 then removing v_2 leaves the span unchanged"],
  cxd={"v_1_not_zero": "if v_1 = 0 the list is dependent but no LATER vector need be redundant -- (0, e_1) is dependent, yet removing e_1 changes the span. The conclusion must then be about removing v_1 itself",
       "invertibility_of_a_j": "solving for v_j divides by a_j, which needs F to be a field; over Z this step fails and is the origin of the module-theoretic complications in smith_normal_form_boundary"},
  misuse=["concluding the LAST vector is always the removable one -- the lemma names the largest index with a nonzero coefficient, which depends on the relation chosen"],
  related={"required_by": ["steinitz_exchange", "basis_existence_finite"]},
  sources=["axler_lada_4e"], status="reviewed")

N("basis", "definition", "vector_spaces",
  "A basis of V is a linearly independent family that spans V. DEFINED WITHOUT REFERENCE TO ANY NUMBER -- this is what makes dimension provable rather than assumed (see edges/cycles.md, cycle 4).",
  parseable="(b_i)_{i in I} basis of V  <=>  independent and span((b_i)) = V",
  symbols={"(b_i)_{i in I}": "the basis, type: indexed family in V; I may be infinite"},
  tcn="Well-formed with no appeal to dimension. The definition is stated for arbitrary index sets, so it covers the infinite-dimensional case handled by basis_existence_general.",
  field_scope="any_field",
  spec=["the standard basis (e_1,...,e_n) of F^n",
        "the empty family is a basis of {0}",
        "(1, t, t^2, ...) is a basis of F[t]: infinite, but every element is still a FINITE combination",
        "(1, i) is a basis of C as an R-vector space, but (1) is a basis of C as a C-vector space -- dimension depends on the field of scalars"],
  cxd={"independence": "a spanning set that is dependent gives non-unique coordinates: in R^2, (e_1, e_2, e_1+e_2) spans but 0 has infinitely many representations",
       "spanning": "an independent family that does not span gives coordinates only on a proper subspace"},
  misuse=["assuming a basis is unique: every space of dimension >= 1 over an infinite field has infinitely many. What IS unique is the CARDINALITY (dimension_well_defined) and the coordinates relative to a FIXED ordered basis",
          "assuming a basis of an infinite-dimensional space is countable or usable -- a Hamel basis of R over Q has cardinality of the continuum and cannot be exhibited"],
  related={"required_by": ["steinitz_exchange", "ordered_basis", "dimension_well_defined"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("steinitz_exchange", "lemma", "vector_spaces",
  "If (u_1,...,u_m) is linearly independent in V and (w_1,...,w_n) spans V, then m <= n; moreover the u's can be exchanged into the spanning list, so that (u_1,...,u_m, w_{i_1},...,w_{i_{n-m}}) still spans V.",
  parseable="independent list of length m, spanning list of length n  =>  m <= n, and m of the w's may be replaced by the u's keeping the span",
  symbols={"m": "length of the independent list", "n": "length of the spanning list"},
  tcn="Well-formed. The inequality m <= n is the whole engine of dimension theory: it is the statement that no independent family can be longer than any spanning family.",
  hyps=["(u_i) independent", "(w_j) spans V", "both lists finite"],
  field_scope="any_field",
  proof=("induction on m: adjoin u_1 to the spanning list making it dependent, apply dependence_lemma to remove some w, and repeat. The removed vector is always a w, not a previously inserted u, because the u's are independent", "dependence_lemma", "core", "LinAlg.steinitz_bound (Nat length bound, list induction)"),
  spec=["m = 1: any single nonzero vector can replace some member of a spanning list",
        "m = n: the u's themselves then span, which is the 'n independent vectors in an n-dimensional space form a basis' corollary"],
  cxd={"independence_of_the_u": "without it the bound is false: in F^1, (1, 1, 1) is a 'longer than spanning' list precisely because it is dependent",
       "finiteness": "in F[t], (1, t, t^2, ...) is independent and infinite while no finite list spans -- the inequality is vacuous, and dimension theory must be redone with cardinals"},
  misuse=["reading it as 'any independent set extends to a basis' -- that is basis_existence_finite, a consequence",
          "using it to compare two INDEPENDENT lists: it compares independent against spanning"],
  apps=["the source of every 'you cannot have more than n independent constraints in n dimensions' argument, including the rank bound in statistics and the degrees-of-freedom count in ANOVA"],
  related={"required_by": ["dimension_well_defined"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("basis_existence_finite", "theorem", "vector_spaces",
  "If V is finite-dimensional then every finite spanning set CONTAINS a basis, and every linearly independent list EXTENDS to a basis. CHOICE-FREE: both processes terminate after finitely many steps.",
  parseable="V finite-dimensional  =>  (S finite spanning => some subfamily of S is a basis) and (L independent => L extends to a basis)",
  symbols={"S": "a finite spanning set", "L": "an independent list"},
  tcn="Well-formed. The choice-freeness is a real content claim, not a technicality: it is what distinguishes this from basis_existence_general.",
  hyps=["V finite-dimensional"],
  field_scope="any_field", choice_grade="choice_free",
  proof=("contains: repeatedly apply dependence_lemma to delete a redundant vector; the list is finite so this halts. Extends: adjoin any vector outside the current span, which preserves independence; Steinitz bounds the number of steps", "dependence_lemma", "core", "LinAlg.basis_from_spanning (list recursion, terminating)"),
  spec=["S already independent: the basis is S itself",
        "L empty: extending the empty list yields a basis from scratch"],
  cxd={"finite_dimensionality": "for an infinite-dimensional V the deletion process need not halt and the extension process needs transfinite recursion -- i.e. Zorn. See basis_existence_general, whose choice grade is needs_full_AC"},
  misuse=["believing every vector space has a CONSTRUCTIBLE basis: this theorem gives one only in the finitely generated case"],
  related={"generalizes": [], "contrasts_with": ["basis_existence_general"]},
  sources=["axler_lada_4e"], status="reviewed")

N("basis_existence_general", "theorem", "vector_spaces",
  "Every vector space has a basis. Proved by Zorn's lemma applied to the poset of independent subsets ordered by inclusion. Over ZF this statement is EQUIVALENT to the axiom of choice (Blass 1984).",
  parseable="for every vector space V there exists a basis B of V;  (ZF) this statement <=> AC",
  symbols={"B": "a maximal independent subset, type: subset of V"},
  tcn="Well-formed. The Zorn hypothesis holds because the union of a chain of independent sets is independent -- a fact that uses the FINITENESS of linear combinations, since any vanishing combination involves finitely many vectors and hence lies in one member of the chain.",
  hyps=["Zorn's lemma (equivalently AC)"],
  field_scope="any_field", choice_grade="needs_full_AC",
  proof=("Zorn on independent subsets; a maximal independent set spans, since a vector outside its span could be adjoined", "zorns_lemma", "cited", None),
  constructive=False, choice=True,
  spec=["V finite-dimensional: reduces to basis_existence_finite, which needs no choice at all",
        "V = R as a Q-vector space: a Hamel basis exists but none can be written down; its existence yields a non-measurable set and a discontinuous additive function R -> R"],
  cxd={"the_axiom_of_choice": "Blass showed that in ZF, 'every vector space has a basis' implies AC. So there are models of ZF containing a vector space with NO basis. This is the only genuinely non-choice-free result in the capsule"},
  misuse=["using it to justify 'pick a basis' in an infinite-dimensional argument and then treating the basis as computable or even nameable",
          "confusing a Hamel basis (this theorem, finite combinations) with a Schauder/orthonormal basis of a Hilbert space (infinite convergent series) -- different objects with different existence theorems"],
  apps=["the existence of a non-measurable set and of pathological additive functions, both standard consequences in measure theory",
        "the routine 'extend to a basis' step in algebra, valid unconditionally only in finite dimension"],
  related={"equivalent_to": ["axiom_of_choice"], "contrasts_with": ["basis_existence_finite"]},
  sources=["blass_1984", "hoffman_kunze_2e"], status="reviewed")

N("dimension_well_defined", "theorem", "vector_spaces",
  "Any two bases of a finitely generated vector space have the same (finite) number of elements.",
  parseable="B, B' bases of V, V finitely generated  =>  |B| = |B'|",
  symbols={"|B|": "the cardinality of the basis, type: natural number"},
  tcn="Well-formed and logically prior to 'dimension': this theorem is what makes the definition of dim V a definite description rather than a choice.",
  hyps=["V finitely generated"],
  field_scope="any_field",
  proof=("apply steinitz_exchange twice: B independent and B' spanning gives |B| <= |B'|; swapping the roles gives the reverse", "steinitz_exchange", "core", "LinAlg.basis_card_eq (antisymmetry of <= on Nat from two Steinitz bounds)"),
  spec=["F^n: every basis has exactly n elements, so dim F^n = n",
        "C over R has dimension 2; C over C has dimension 1 -- the theorem fixes the number only once the SCALAR FIELD is fixed"],
  cxd={"finite_generation": "for infinite-dimensional spaces the statement survives as a cardinality equality, but only with AC-dependent cardinal arithmetic; the elementary Steinitz argument does not apply",
       "F_being_a_field": "over a general ring the analogous statement (the invariant basis number property) can FAIL: there are rings with R isomorphic to R^2 as modules. Fields always have IBN"},
  misuse=["assuming two bases are related by a permutation -- they are related by an invertible change_of_basis matrix, which is far more general"],
  related={"required_by": ["dimension"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("dimension", "definition", "vector_spaces",
  "dim V is the common cardinality of any basis of V, well-defined by the preceding theorem. dim{0} = 0. Written dim_F V when the scalar field needs emphasis.",
  parseable="dim_F V := |B| for any basis B of V;  dim {0} = 0",
  symbols={"dim V": "type: natural number (finite case) or cardinal (general case)",
           "dim_F V": "dimension over the scalar field F"},
  tcn="Well-formed only BECAUSE dimension_well_defined precedes it -- this is the definitional-order resolution recorded as cycle 4.",
  hyps=["V finite-dimensional (for the natural-number-valued version)"],
  field_scope="any_field",
  spec=["dim F^n = n", "dim {0} = 0 with the empty basis",
        "dim_R C = 2 but dim_C C = 1: the subscript is not decorative",
        "dim F^{m x n} = mn, the fact behind matrix_addition_scalar"],
  cxd={"well_definedness": "without dimension_well_defined the notation dim V would depend on the basis chosen and every counting argument in the capsule would collapse",
       "the_scalar_field": "R has dimension 1 over R and infinite (continuum) dimension over Q"},
  misuse=["comparing dimensions over different fields",
          "assuming dim(U + W) = dim U + dim W -- that needs the sum to be DIRECT (dimension_formula_sum)"],
  related={"required_by": ["rank", "nullity", "rank_nullity"]},
  sources=["axler_lada_4e"], status="reviewed")

N("dimension_of_subspace", "proposition", "vector_spaces",
  "If U is a subspace of a finite-dimensional V then U is finite-dimensional, dim U <= dim V, and dim U = dim V if and only if U = V.",
  parseable="U subspace of V, dim V < infinity  =>  dim U <= dim V, with equality iff U = V",
  symbols={"U": "a subspace of V"},
  tcn="Well-formed. The equality clause is the workhorse: it converts a dimension count into a set equality, which is how most 'these two subspaces coincide' arguments in the capsule are closed.",
  hyps=["V finite-dimensional", "U a subspace of V"],
  field_scope="any_field",
  proof=("a basis of U is independent in V so has at most dim V elements (Steinitz); if dim U = dim V, a basis of U is a maximal independent family in V hence spans V", "steinitz_exchange", "core", "LinAlg.subspace_dim_le"),
  spec=["U = {0}: dimension 0 <= dim V",
        "dim U = dim V - 1: a hyperplane, the kernel of a nonzero functional (see annihilator)"],
  cxd={"finite_dimensionality_of_V": "in F[t] the subspace of polynomials with zero constant term is a PROPER subspace with the same (infinite) dimension -- the equality clause fails outright. This is the same phenomenon as the infinite-dimensional failure of injective_surjective_equivalence"},
  misuse=["applying the equality clause without checking dim V is finite"],
  sources=["axler_lada_4e"], status="reviewed")

N("basis_unique_representation", "proposition", "vector_spaces",
  "A family B is a basis of V if and only if every v in V has EXACTLY ONE representation as a finite linear combination of members of B.",
  parseable="B basis  <=>  for all v in V there are unique finitely-supported (a_b) with v = sum_b a_b b",
  symbols={"(a_b)": "the coordinate family, type: finitely supported function B -> F"},
  tcn="Well-formed. Existence of a representation is spanning; uniqueness is independence. The proposition is the bridge from the abstract definition to coordinates.",
  hyps=["B a family in V"],
  field_scope="any_field",
  proof=("existence <=> spanning by definition; uniqueness <=> independence, since two representations of v differ by a vanishing combination", "linear_independence", "core", "LinAlg.unique_repr_iff_basis"),
  spec=["B the standard basis of F^n: the coordinates are the components themselves"],
  cxd={"independence": "with a dependent spanning family, 0 has more than one representation and coordinates are not functions",
       "spanning": "with an independent non-spanning family, some v has no representation at all"},
  misuse=["assuming coordinates are basis-independent: they are defined only relative to a FIXED ORDERED basis, and change_of_basis is the whole theory of what happens otherwise"],
  related={"required_by": ["ordered_basis", "direct_sum"]},
  sources=["axler_lada_4e"], status="reviewed")

N("ordered_basis", "definition", "vector_spaces",
  "An ordered basis B = (b_1,...,b_n) of an n-dimensional V determines the coordinate map [.]_B : V -> F^n sending v to the column of its unique coefficients. It is a linear bijection.",
  parseable="[v]_B := (a_1,...,a_n)^T where v = sum_i a_i b_i;  [.]_B : V -> F^n linear and bijective",
  symbols={"[v]_B": "the coordinate column of v, type: element of F^n",
           "B": "an ORDERED basis, type: finite sequence, not a set"},
  tcn="Well-formed by basis_unique_representation (coordinates exist and are unique) plus the index_convention (they are assembled as a COLUMN). Ordering is essential: permuting B permutes the coordinates.",
  hyps=["V finite-dimensional", "B an ordered basis"],
  field_scope="any_field",
  spec=["V = F^n with the standard ordered basis: [v]_B = v, the identity",
        "V = polynomials of degree <= 2 with B = (1, t, t^2): [3 + 5t^2]_B = (3, 0, 5)^T"],
  cxd={"orderedness": "an unordered basis gives coordinates only up to permutation, so no well-defined map to F^n and no well-defined matrix of a linear map"},
  misuse=["writing [v] without recording which basis -- almost every sign and transposition error in applied linear algebra is a dropped basis label"],
  related={"required_by": ["coordinate_isomorphism", "matrix_of_linear_map"]},
  sources=["hoffman_kunze_2e"], status="reviewed")

N("coordinate_isomorphism", "theorem", "vector_spaces",
  "If dim V = n < infinity then [.]_B : V -> F^n is an isomorphism for every ordered basis B. Hence any two F-spaces of the same finite dimension are isomorphic, and dimension is a COMPLETE invariant of finite-dimensional vector spaces.",
  parseable="dim V = dim W = n < infinity  =>  V isomorphic to W;  and dim is a complete isomorphism invariant",
  symbols={"[.]_B": "the coordinate isomorphism V -> F^n"},
  tcn="Well-formed. 'Complete invariant' means dimension alone decides the isomorphism class -- an unusually strong classification, and the reason linear algebra is so much simpler than group or ring theory.",
  hyps=["V finite-dimensional"],
  field_scope="any_field",
  proof=("linearity from the uniqueness of coordinates; bijectivity from spanning and independence respectively", "basis_unique_representation", "core", "LinAlg.coord_bijective"),
  spec=["V = F^n, B standard: the isomorphism is the identity",
        "every 3-dimensional real space -- polynomials of degree <= 2, symmetric 2x2 matrices, R^3 -- is isomorphic to R^3"],
  cxd={"finite_dimensionality": "two infinite-dimensional spaces of the same Hamel dimension are still isomorphic, but the isomorphism is AC-dependent and unusable; more importantly, as TOPOLOGICAL spaces they need not be isomorphic at all",
       "same_scalar_field": "C and R^2 are isomorphic as R-spaces but not as C-spaces (dimensions 1 and 2 over C... and R^2 is not even a C-space without extra structure)"},
  misuse=["concluding that because V is isomorphic to F^n there is a CANONICAL such isomorphism: there is not, it depends on B. The canonical map is V -> V^** (double_dual), which is why that node is singled out"],
  related={"required_by": ["linear_isomorphism", "change_of_basis"]},
  sources=["axler_lada_4e", "hoffman_kunze_2e"], status="reviewed")

N("sum_of_subspaces", "definition", "vector_spaces",
  "U + W = {u + w : u in U, w in W}, the smallest subspace of V containing both. More generally sum_i U_i is the set of finite sums of elements of the U_i.",
  parseable="U + W := {u + w : u in U, w in W} = span(U union W)",
  symbols={"U + W": "type: subspace of V"},
  tcn="Well-formed, and equal to span(U union W) -- which is why the union itself (usually not a subspace, see subspace_intersection) is not the right object.",
  field_scope="any_field",
  spec=["U + {0} = U", "U + U = U", "in R^3, the sum of two distinct planes through the origin is all of R^3"],
  cxd={"none_all_hypotheses_essential": "a definition; the trap is the union, which is generally not a subspace"},
  misuse=["writing U union W for the span of two subspaces",
          "assuming dim(U + W) = dim U + dim W without directness -- see dimension_formula_sum"],
  related={"required_by": ["direct_sum", "dimension_formula_sum"]},
  sources=["axler_lada_4e"], status="reviewed")

N("direct_sum", "definition", "vector_spaces",
  "V = U (+) W means V = U + W and U ∩ W = {0}. Equivalently, every v in V has a UNIQUE decomposition v = u + w with u in U and w in W.",
  parseable="V = U (+) W  <=>  (V = U + W and U ∩ W = {0})  <=>  every v = u + w uniquely",
  symbols={"(+)": "direct sum", "U ∩ W": "the intersection, required trivial"},
  tcn="Well-formed. The equivalence of the two formulations is part of the node's content: uniqueness of the decomposition is exactly the triviality of the intersection, since a nonzero x in U ∩ W gives x + 0 = 0 + x as two decompositions.",
  hyps=["U, W subspaces of V"],
  field_scope="any_field",
  proof=("if x in U ∩ W is nonzero, x = x + 0 = 0 + x are distinct decompositions; conversely two decompositions differ by an element of U ∩ W", "subspace_intersection", "core", "LinAlg.directsum_iff_unique"),
  spec=["R^2 = (x-axis) (+) (y-axis), but also R^2 = (x-axis) (+) (diagonal) -- a complement is NOT unique",
        "V = U (+) U^perp for a finite-dimensional subspace of an inner product space (orthogonal_decomposition) -- the ORTHOGONAL complement is the canonical choice among many complements"],
  cxd={"trivial_intersection": "in R^2 with U = W = the x-axis, U + W = U but the decomposition is wildly non-unique. The sum formula dim(U+W) = dim U + dim W then fails (1 != 2)",
       "pairwise_triviality_for_three_or_more": "for three subspaces, PAIRWISE trivial intersections do NOT suffice: the x-axis, y-axis, and diagonal in R^2 meet pairwise in {0}, yet the sum of the three is not direct -- (1,1) decomposes in more than one way. The correct condition is that each U_i meets the sum of the others trivially"},
  misuse=["assuming a complement is unique, or that 'the' complement is meaningful without an inner product",
          "extending directness from two summands to many by checking only pairwise intersections (see the counterexample above)"],
  related={"required_by": ["dimension_formula_sum", "orthogonal_decomposition", "primary_decomposition"]},
  sources=["axler_lada_4e"], status="reviewed")

N("dimension_formula_sum", "theorem", "vector_spaces",
  "For finite-dimensional subspaces U, W of V: dim(U + W) = dim U + dim W - dim(U ∩ W). In particular the sum is direct iff dim(U + W) = dim U + dim W.",
  parseable="dim(U + W) + dim(U ∩ W) = dim U + dim W",
  symbols={"dim(U ∩ W)": "the overlap term, type: natural number"},
  tcn="Well-formed; all four terms are finite by dimension_of_subspace. Often called the Grassmann formula, and it is the linear-algebra analogue of inclusion-exclusion for two sets.",
  hyps=["U, W finite-dimensional subspaces"],
  field_scope="any_field",
  proof=("take a basis of U ∩ W, extend it to a basis of U and separately to a basis of W; the union is a basis of U + W, and the count follows", "basis_existence_finite", "core", "LinAlg.dim_sum_inter (Nat arithmetic from the basis count)"),
  spec=["U ∩ W = {0}: recovers dim(U (+) W) = dim U + dim W",
        "two planes in R^3: 3 = 2 + 2 - 1, so they must meet in a line -- the standard non-obvious consequence",
        "U subset W: dim(U + W) = dim W and dim(U ∩ W) = dim U, so the identity is trivially true"],
  cxd={"finite_dimensionality": "for infinite-dimensional subspaces the cardinal arithmetic degenerates (infinity + infinity = infinity) and the formula carries no information",
       "two_summands": "the three-subspace analogue dim(U+W+X) = sum dim - sum dim(pairwise) + dim(triple) is FALSE. The x-axis, y-axis and diagonal in R^2 give 2 versus 1+1+1-0-0-0+0 = 3. Unlike sets, subspaces do not satisfy inclusion-exclusion beyond two terms"},
  misuse=["using an inclusion-exclusion formula for three or more subspaces (see above) -- this is the single most common error with this theorem"],
  apps=["the dimension count behind degrees of freedom in ANOVA decompositions and behind codimension arguments in constrained optimisation"],
  sources=["axler_lada_4e", "halmos_fdvs"], status="reviewed")

N("quotient_space", "construction", "vector_spaces",
  "For a subspace U of V, the quotient V/U is the set of cosets v + U with (v+U) + (w+U) = (v+w) + U and a(v+U) = av + U. WELL-DEFINED precisely because U is a subspace.",
  parseable="V/U := {v + U : v in V};  (v+U)+(w+U) := (v+w)+U;  a(v+U) := av+U",
  symbols={"v + U": "a coset, type: element of the quotient set V/U",
           "pi": "the canonical projection V -> V/U, v -> v + U, linear and surjective"},
  tcn="The type check IS the well-definedness check, as in every quotient construction: if v + U = v' + U and w + U = w' + U then (v+w) - (v'+w') = (v - v') + (w - w') lies in U by closure under addition; and av - av' = a(v - v') lies in U by closure under scalars. Both closures are used, one per operation.",
  hyps=["U a subspace of V"],
  field_scope="any_field",
  welldef="Each operation is defined on representatives; independence of representative follows from U being closed under + (for addition) and under scalar multiplication (for scaling). A subset failing either closure yields no quotient.",
  proof=("verify the vector-space axioms on cosets; each reduces to the corresponding axiom in V applied to representatives", "quotient_set", "core", "LinAlg.quotient_welldefined (coset representative independence, Int encoding)"),
  spec=["U = {0}: V/U is isomorphic to V", "U = V: V/U = {0}",
        "V = R^2, U = the x-axis: V/U is the set of horizontal lines, isomorphic to R by the y-value"],
  cxd={"U_being_a_subspace": "if U is merely a subset closed under addition but not scaling -- say the nonnegative x-axis in R^2 -- the scalar operation on cosets is not well-defined, since v + U = v' + U no longer implies av + U = av' + U",
       "closure_under_addition": "similarly, a subset closed under scaling but not addition (the union of the axes) breaks coset addition"},
  misuse=["picturing V/U as a subspace of V: it is not, it is a space of COSETS. A complement W with V = U (+) W is isomorphic to V/U but is a genuine choice, not canonical",
          "assuming the projection has a canonical section: it does not without extra structure (an inner product supplies one)"],
  related={"required_by": ["first_isomorphism_theorem", "quotient_dimension"]},
  sources=["hoffman_kunze_2e", "axler_lada_4e"], status="reviewed")

N("quotient_dimension", "proposition", "vector_spaces",
  "For a finite-dimensional V and a subspace U, dim(V/U) = dim V - dim U. The number dim(V/U) is the CODIMENSION of U in V.",
  parseable="dim(V/U) = dim V - dim U",
  symbols={"codim U": "= dim(V/U), type: natural number"},
  tcn="Well-formed; the subtraction is legitimate because dim U <= dim V by dimension_of_subspace.",
  hyps=["V finite-dimensional", "U a subspace"],
  field_scope="any_field",
  proof=("extend a basis of U to a basis of V; the images of the added vectors form a basis of V/U", "basis_existence_finite", "core", "LinAlg.dim_quotient (Nat subtraction guarded by dim U <= dim V)"),
  spec=["U = {0}: dim(V/U) = dim V", "U a hyperplane: codimension 1, and V/U is one-dimensional -- the setting of a single linear functional"],
  cxd={"finite_dimensionality": "codimension can be finite while both dimensions are infinite (the polynomials with zero constant term have codimension 1 in F[t]), so the SUBTRACTION is meaningless even though the quotient is fine. Codimension, not dimension, is the robust notion"},
  misuse=["computing codimension as dim V - dim U when V is infinite-dimensional: define it as dim(V/U) directly"],
  related={"required_by": ["first_isomorphism_theorem"]},
  sources=["hoffman_kunze_2e"], status="reviewed")
