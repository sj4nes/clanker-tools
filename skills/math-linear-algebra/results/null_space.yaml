id: matrices.null_space
node: null_space
status_label: definition
display: 'null(A) = {x in F^n : Ax = 0}, the kernel of x -> Ax, a subspace of F^n. Invariant under row equivalence.'
parseable: 'null(A) := {x : Ax = 0} subset F^n;  A ~_r B => null(A) = null(B)'
field_scope: any_field
choice_grade: choice_free
symbols:
  'null(A)': 'type: subspace of F^n'
type_check_status: well_formed
type_check_note: 'Well-formed as the kernel of a linear map; the invariance under row equivalence is the reason elimination solves homogeneous systems.'
hypotheses: []
preconditions: []
dependencies:
  - 'kernel'
  - 'matrix'
uses_choice: false
constructive: true
specialization_cases:
  - 'A invertible: null(A) = {0}'
  - 'dim null(A) = n - rank A by rank_nullity, so the free columns count the dimension'
counterexamples_when_dropped:
  none_all_hypotheses_essential: 'a definition'
common_misuse:
  - 'confusing null(A) with null(A^T) (the left null space), which lives in F^m and is what makes a system INCONSISTENT rather than underdetermined'
related:
  dual_of: ['left_null_space']
  required_by: ['four_subspaces', 'linear_system']
sources:
  - 'strang_5e'
status: reviewed
checks:
  type: validation/type-checks.md#null_space
  specialization: validation/specialization-cases.md#null_space
  instances: validation/instance-checks.bc
  proof: validation/proof-checks.md#null_space
