# Counterexample index (generated from results/*.yaml)

`dropped hypothesis`  ->  `witnessing counterexample`  (from which result)

- **absolutely_continuous_rv** — absolute_continuity_vs_continuous_cdf: 'the Cantor distribution: F_X continuous and strictly increasing on the Cantor set, F_X'' = 0 lambda-a.e., P_X concentrated on a lambda-null set -- continuous CDF, no density'
- **abstract_integral** — measurability_of_f: 'a non-measurable f has no well-defined integral -- the defining sup is over an ill-specified family and Fubini/Tonelli fail'
- **almost_sure** — countable_intersection_only: 'an uncountable family of a.s. events can have intersection of probability 0: on [0,1] uniform, A_x = { omega != x } has P(A_x) = 1 but bigcap_x A_x = empty'
- **beta_distribution** — support_is_the_unit_interval: 'for a proportion that can be exactly 0 or 1, a zero-or-one-inflated beta is needed; plain beta puts zero mass on the endpoints'
- **binomial_distribution** — independence_of_trials: 'correlated trials (e.g. sampling without replacement) give the hypergeometric distribution, with the SAME mean np but SMALLER variance (finite-population correction)'
- **binomial_distribution** — identical_p: 'varying p_i per trial gives the Poisson-binomial distribution; mean sum p_i, variance sum p_i(1-p_i)'
- **boole_inequality** — none_unconditional: 'the bound holds for every countable family; it is tight iff the A_n are pairwise disjoint (mod null sets)'
- **borel_cantelli_first** — none_unconditional_direction: 'the converse needs independence (borel_cantelli_second); without it sum P(A_n) = inf gives no lower bound -- take A_n all equal to a fixed A with 0 < P(A) < 1: sum diverges but P(A i.o.) = P(A) < 1'
- **borel_cantelli_second** — independence: 'A_n = A fixed with 0 < P(A) < 1: sum P(A_n) = inf but P(A i.o.) = P(A) != 1. Pairwise independence is not quite enough in general; mutual (or the Kochen-Stone refinement) is the clean hypothesis.'
- **borel_sigma_algebra** — generated_by_a_pi_system: 'if one generates from a collection that is not a pi-system, the pi-lambda uniqueness argument for measures fails'
- **cauchy_schwarz_expectation** — finite_second_moments: 'X ~ Cauchy, Y = 1: E[X^2] = inf and E[XY] = E[X] does not exist -- the inequality is between undefined quantities'
- **cauchy_schwarz_expectation** — equality_needs_linear_dependence: 'if X, Y are not proportional a.s. the inequality is strict'
- **cdf** — none_defined_for_every_rv: 'F_X always exists; the content is in cdf_properties (which functions are CDFs) and cdf_determines_law'
- **cdf_determines_law** — pi_system_generates_the_sigma_algebra: 'agreeing on a non-generating or non-pi-system family is not enough -- see the dynkin_pi_lambda counterexample'
- **cdf_properties** — right_continuity: 'a function that is left-continuous with jumps is not a CDF in this convention -- it would be x |-> P(X < x)'
- **cdf_properties** — the_limit_conditions: 'F(x) = arctan(x)/pi + 1/2 works; F(x) = arctan(x) does not reach 1 (defective / sub-probability distribution -- mass escapes to +inf)'
- **central_limit_theorem** — finite_nonzero_variance: 'X_i ~ Cauchy (cauchy_no_mean): mean_n is again Cauchy for every n -- no normalization makes it converge to a normal. Infinite variance breaks the theorem; stable laws with index < 2 are the correct limits.'
- **central_limit_theorem** — iid_identical: 'independent but not identically distributed can fail without a further condition -- the Lindeberg condition (lindeberg_clt) is what restores the conclusion.'
- **central_limit_theorem** — iid_independent: 'strongly dependent X_i (e.g. X_i = X_1 for all i): the standardized mean is sqrt(n)(X_1 - mu)/sigma, which diverges, not converges.'
- **central_limit_theorem** — finite_mean: 'if E|X_1| = inf the centring constant mu does not exist.'
- **cf_properties** — moment_for_smoothness: 'Cauchy: phi(t) = e^{-|t|} is NOT differentiable at 0 -- consistent with E|X| = inf'
- **cf_properties** — continuity_points_for_inversion: 'the inversion formula recovers P_X((a,b]) only at continuity points a, b of F_X'
- **characteristic_function** — none_always_defined: 'the CF is the tool precisely because it needs no hypothesis -- every law has one'
- **chebyshev_inequality** — finite_variance: 'X ~ Cauchy (cauchy_no_mean): Var and E both fail to exist; the tail P(|X| >= k) ~ 2/(pi k) decays like 1/k, not 1/k^2, and no variance bound applies.'
- **chebyshev_inequality** — k_positive: 'k <= 0: the right side is infinite or undefined and the statement is vacuous or meaningless.'
- **conditional_distribution** — positive_marginal_density: 'where f_X(x) = 0 the ratio is 0/0 -- conditioning on a value X never takes is undefined'
- **conditional_distribution** — existence_of_a_joint_density: 'for singular joints (e.g. Y = X^2) there is no f_{X,Y}; conditioning requires the abstract construction (Borel-Kolmogorov paradox: the answer depends on how the conditioning event is approached)'
- **conditional_expectation_elementary** — positive_mass_or_density_at_y: 'conditioning on { Y = y } with P(Y = y) = 0 and no joint density is ambiguous (Borel-Kolmogorov) -- needs the abstract construction'
- **conditional_expectation_existence** — sigma_finiteness_is_automatic: 'P|_G is a probability measure, so the sigma-finiteness hypothesis of Radon-Nikodym is free here'
- **conditional_expectation_l2_projection** — finite_second_moment: 'the projection picture needs X in L^2; for X in L^1 only, E[X | G] still exists (conditional_expectation_existence) but is not a projection and is not a mean-square minimizer'
- **continuous_mapping_theorem** — same_mode_out: 'if the input is only in distribution, the output is only in distribution -- you cannot upgrade'
- **continuous_uniform_distribution** — bounded_support: 'there is no uniform distribution on R or on (0, inf) -- ''improper uniform'' priors are not probability measures (they still yield proper posteriors sometimes)'
- **convergence_almost_sure** — common_probability_space: 'a.s. convergence is meaningless for X_n and X defined on different spaces -- only the WEAKER convergence in distribution makes sense then'
- **convergence_implications** — p_does_not_imply_a_s: 'same typewriter sequence -- in probability but not a.s.'
- **convergence_implications** — d_does_not_imply_p: 'X ~ N(0,1), X_n = (-1)^n X: each X_n ~ N(0,1) so X_n -> N(0,1) in distribution, but |X_n - X_{n+1}| = 2|X| does not -> 0, so not in probability'
- **convergence_implications** — d_to_a_nonconstant_does_not_imply_p: 'the same example -- the limit must be a CONSTANT for the reverse arrow'
- **convergence_in_distribution** — continuity_points_only: 'X_n = 1/n (constant): F_{X_n} is a step at 1/n, F_X a step at 0; F_{X_n}(0) = 0 for all n but F_X(0) = 1 -- convergence fails AT the jump but X_n -> 0 in distribution is still true'
- **convergence_in_lp** — uniform_integrability_for_the_converse: 'X_n = n 1_{(0, 1/n]} on ([0,1], lambda): X_n -> 0 in probability and a.s., but E[|X_n|] = 1 not -> 0 -- convergence in probability does NOT imply L^1 without a dominating / uniformly integrable family'
- **convergence_in_probability** — eps_quantifier: 'P(|X_n - X| > eps) -> 0 for ONE eps is not enough; it must hold for every eps > 0'
- **convex_function** — convexity: 'phi(x) = -x^2 (concave): Jensen reverses, phi(E[X]) >= E[phi(X)]'
- **convolution_formula** — independence: 'for dependent X, Y the density of X + Y involves the joint density and is not a convolution -- e.g. Y = -X gives X + Y = 0, a point mass'
- **convolution_formula** — existence_of_densities: 'if X is discrete and Y continuous, X + Y has a density (a mixture of shifted f_Y), but the pure convolution-of-densities formula does not apply directly'
- **correlation** — positive_finite_variances: 'a constant Y has sd(Y) = 0 -- rho is 0/0, undefined'
- **correlation** — linearity_of_the_relationship: 'Y = X^2 with X ~ Uniform(-1,1): perfect functional dependence but rho = 0 -- correlation only sees LINEAR association'
- **countable_set** — countable_choice: 'without countable choice it is consistent that a countable union of countable sets of reals is uncountable (Feferman-Levy)'
- **covariance** — finite_second_moments: 'if E[X^2] or E[Y^2] is infinite, E[XY] may not exist and Cov is undefined'
- **delta_method** — g_prime_theta_nonzero: 'g(x) = x^2 at theta = 0: g''(0) = 0, the first-order limit is degenerate; n(g(T_n) - g(theta)) -> sigma^2 chi-squared_1 instead (a different rate AND a non-normal limit)'
- **delta_method** — differentiability_at_theta: 'g with a kink at theta (e.g. |x - theta|): no single derivative, the limit is a folded normal'
- **dominated_convergence_theorem** — integrable_dominator_g: 'f_n = n 1_{(0, 1/n]} on ([0,1], lambda): f_n -> 0 a.e. but integral f_n = 1; no integrable g dominates (sup_n f_n is not integrable)'
- **dynkin_pi_lambda** — pi_system: 'measures can agree on a generating collection that is NOT a pi-system yet differ: on Omega={1,2,3,4}, uniform vs the measure (3/8,1/8,1/8,3/8) agree on {1,2} and {1,3} but not on {1} = {1,2} cap {1,3}'
- **expectation_monotonicity** — a_s_inequality: 'X <= Y on a null set only is not enough to compare -- but changing X, Y on a null set changes neither expectation, so ''a.s.'' is exactly the right hypothesis'
- **expectation_monotonicity** — existence_of_both_expectations: 'if E[Y] = +inf and E[X] = -inf the inequality is vacuous/ill-posed'
- **exponential_distribution** — memorylessness: 'the Weibull, gamma (shape != 1), and lognormal are NOT memoryless -- using an exponential for aging components (increasing hazard) underestimates late failures'
- **exponential_distribution** — constant_hazard_rate: 'f_X(x)/P(X > x) = lambda is constant; real lifetimes usually have a bathtub or increasing hazard'
- **fatou_lemma** — nonnegativity_f_n: 'f_n = -1_{[n,n+1]}: liminf f_n = 0 with integral 0, but liminf integral f_n = -1 -- the inequality direction needs f_n >= 0'
- **finite_additivity** — disjointness: 'P(A cup B) = P(A) + P(B) - P(A cap B) in general; without disjointness the sum overcounts the overlap (inclusion_exclusion)'
- **fubini_tonelli** — sigma_finiteness_or_integrability: 'f(x,y) = (x^2 - y^2)/(x^2 + y^2)^2 on (0,1)^2: the two iterated integrals are +pi/4 and -pi/4 -- f is not integrable, so Fubini does not apply'
- **gamma_distribution** — common_scale_for_the_sum_rule: 'Gamma(k_1, theta_1) + Gamma(k_2, theta_2) with theta_1 != theta_2 is NOT gamma -- the sum rule needs a shared scale'
- **gamma_distribution** — shape_and_scale_vs_shape_and_rate: 'software differs; mixing them scales the mean by theta^2'
- **generated_sigma_algebra** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **geometric_distribution** — memorylessness_pins_it_down: 'any non-geometric law on {1,2,...} fails P(X > m+k | X > m) = P(X > k) for some m, k'
- **hoeffding_inequality** — independence: 'for dependent X_i the MGF does not factor; McDiarmid''s / Azuma''s inequality handles bounded-difference dependence'
- **hoeffding_lemma** — boundedness: 'an unbounded centered X (e.g. centered exponential) is not sub-Gaussian -- its MGF grows faster than any e^{ct^2}, only e^{c|t|} for small t (sub-exponential); Hoeffding''s inequality is replaced by Bernstein''s'
- **iid** — identically_distributed: 'independent but not identical: the Lindeberg CLT (lindeberg_clt) is what replaces ''identical'' for a non-degenerate limit'
- **iid** — independence: 'identically distributed but dependent (e.g. a stationary time series): the LLN can still hold (ergodic theorem) but the CLT rate and variance change (long-range dependence)'
- **inclusion_exclusion** — none_all_terms_needed: 'dropping the higher-order terms gives only the Bonferroni one-sided bounds, not equality'
- **independence_events** — all_subsets_not_just_pairs: 'pairwise_not_mutual: X, Y iid fair bits, Z = X XOR Y -- each pair independent, but P(X=Y=Z=0) = 1/4 != 1/8'
- **independence_expectation** — independence_not_merely_uncorrelated: 'X ~ Uniform(-1,1), Y = X^2: E[XY] = E[X^3] = 0 = E[X] E[Y], so uncorrelated, but X, Y are strongly dependent -- the converse is FALSE (uncorrelated_not_independent)'
- **independence_expectation** — integrability: 'independent Cauchy X, Y: E[XY] does not exist even though E[X], E[Y] individually fail too'
- **independence_factorization** — factorization_over_the_full_support: 'f_{X,Y}(x,y) = 2 on the triangle 0 < x < y < 1: the density is ''constant'' but the support is not a rectangle, so X, Y are NOT independent (Y | X = x is Uniform(x, 1))'
- **independence_random_variables** — all_n_together_not_just_pairs: 'three variables can be pairwise independent but not mutually independent -- e.g. X_1, X_2 iid uniform on {-1,1}, X_3 = X_1 X_2'
- **independence_random_variables** — the_joint_law_not_just_marginals: 'same marginals, different dependence: (X, X) vs (X, -X) vs (X, Y independent)'
- **independence_sigma_algebras** — factorization_on_a_pi_system_that_generates: 'checking factorization on a generating family that is not intersection-closed does not extend (dynkin_pi_lambda counterexample)'
- **jacobian_transformation** — g_a_diffeomorphism: 'if g is not injective (e.g. (x_1,x_2) -> (x_1^2, x_2)) the preimage has multiple sheets and their Jacobian contributions must be summed'
- **jensen_inequality** — convexity_of_phi: 'phi concave (e.g. sqrt, log): the inequality REVERSES, phi(E[X]) >= E[phi(X)] -- applying Jensen the wrong way is the single most common error'
- **jensen_inequality** — integrability: 'phi(x) = x^2 on a Cauchy X: E[phi(X)] = inf, the inequality is vacuous'
- **joint_distribution** — none_the_marginals_do_not_determine_it: '(X,Y) and (X, X) can have the same marginals (both standard normal) but wildly different joint laws -- Cov 0 vs Cov 1'
- **kolmogorov_axioms** — countable_additivity: 'on Omega = N with F = 2^N, "P(A) = 0 if A finite, 1 if cofinite" is finitely additive and normalized but NOT countably additive (singletons have measure 0 but union to measure 1) -- and it is not sigma-additive-extendable, so it is not a probability measure. This is why the third axiom is countable, not finite.'
- **kolmogorov_axioms** — normalization: 'drop P(Omega)=1 and any measure qualifies; "probability" statements (expectations as weighted averages, Bayes) collapse.'
- **kolmogorov_axioms** — nonnegativity: 'signed measures are not probabilities; P(A) in [0,1] and monotonicity both fail.'
- **kolmogorov_extension_theorem** — consistency_of_the_f_d_d_s: 'an inconsistent family (e.g. mu_{1,2} with a marginal on coordinate 1 disagreeing with mu_1) realizes no process -- consistency is necessary and sufficient'
- **kolmogorov_extension_theorem** — the_product_sigma_algebra_only: 'the resulting measure only sees cylinder events; { t |-> X_t continuous } is NOT in the product sigma-algebra -- path properties need a separate modification argument (Kolmogorov-Chentsov)'
- **law_of_total_variance** — finite_second_moment: 'X must be in L^2 for all three variances to be finite'
- **law_of_total_variance** — both_terms_nonnegative: 'E[Var(X|G)] >= 0 and Var(E[X|G]) >= 0, so conditioning can only REDUCE expected variance: E[Var(X|G)] <= Var(X)'
- **lebesgue_measure_caratheodory** — caratheodory_measurability: 'lambda* is only finitely subadditive-not-additive on all of 2^R; a Vitali set V has lambda*(V) > 0 but the translates of V partition [0,1] into countably many congruent pieces, so countable additivity would force a contradiction -- V is not measurable'
- **levy_continuity_theorem** — pointwise_convergence_for_all_t: 'convergence of phi_{X_n}(t) on a bounded interval of t is not enough in general'
- **limsup_liminf** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **lindeberg_clt** — lindeberg_condition: 'X_{n,1} ~ N(0, n), X_{n,2}, ..., X_{n,n} ~ iid N(0,1): s_n^2 = 2n - 1 but the first term carries ~half the variance; the normalized sum is NOT asymptotically normal (it stays a 50-50 mix). One dominant term breaks the CLT.'
- **lindeberg_clt** — independence_within_rows: 'strong dependence needs a different CLT (martingale CLT, mixing conditions)'
- **lp_space** — probability_space_for_nesting: 'on (R, lambda), 1/x is in L^2(1, inf) but not L^1(1, inf) -- the nesting L^p subset L^q (p > q) needs a FINITE measure'
- **marginal_distribution** — none_well_defined_always: 'the marginal always exists; the caution is that MANY joints share it'
- **markov_inequality** — a_positive: 'a <= 0 makes E[X]/a <= 0 (or undefined), while P(X >= a) can be 1.'
- **martingale** — adaptedness_and_integrability: 'if X_n is not F_n-measurable or not integrable the conditional expectation is ill-posed'
- **measurable_function** — generating_collection_check: 'checking f^{-1} of a non-generating family is not enough; and a pointwise limit of measurable functions is measurable but a limit of continuous functions need not be continuous'
- **measurable_space** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **measure** — countable_additivity: 'a merely finitely additive set function (e.g. a Banach limit / finitely additive extension of density on N) is not a measure and breaks continuity from below and Borel-Cantelli'
- **measure_continuity** — finite_measure_for_continuity_from_above: 'Lebesgue measure, A_n = [n, inf): A_n down empty but lambda(A_n) = inf for all n, so lambda(A_n) does not converge to lambda(empty) = 0'
- **measure_monotonicity** — nonnegativity_of_mu: 'for a signed measure monotonicity fails: a set of negative measure can contain one of positive measure'
- **memorylessness** — the_characterization_is_exact: 'gamma with shape 2: P(X > s + t | X > s) DECREASES in s -- it ''remembers'' (positive aging). Weibull with shape > 1 likewise. Pareto: negative aging.'
- **mgf_sum_independent** — independence: 'X + X = 2X: phi_{2X}(t) = phi_X(2t) != phi_X(t)^2 in general -- the product rule is exactly the independence assumption'
- **mgf_uniqueness** — finiteness_on_an_interval: 'agreement of M_X and M_Y only AT t = 0 (both equal 1) says nothing; and two different laws can share every moment (lognormal and a discrete perturbation) -- but then neither has an MGF finite near 0'
- **moment** — finite_k_th_absolute_moment: 'Student''s t with nu degrees of freedom has E[|X|^k] = inf for k >= nu -- moments beyond a point simply do not exist for heavy-tailed laws'
- **moment_ladder** — finite_total_measure: 'on an infinite measure space the inclusion reverses partially and can fail entirely -- the ladder is a probability-space phenomenon (P(Omega) = 1 is the hidden hypothesis)'
- **monotone_convergence_theorem** — monotonicity_f_n_up: 'f_n = 1_{[n, n+1]} -> 0 pointwise but integral f_n = 1 not -> 0 (not monotone; DCT also fails, no dominating integrable g)'
- **multiplication_rule** — positive_probability_of_the_conditioning_events: 'if some prefix intersection has probability 0, that conditional factor is undefined -- but then the whole intersection also has probability 0, so the identity is read as 0 = 0'
- **normal_affine_closure** — independence_for_the_sum: 'X ~ N(0,1), Y = -X ~ N(0,1): X + Y = 0, a point mass, NOT N(0, 2) -- the sum rule needs independence (or joint normality with the right covariance)'
- **normal_affine_closure** — joint_normality_for_linear_combos: 'X ~ N(0,1) and Y = X if |X| < 1 else -X: each is N(0,1) but X + Y is not normal -- marginal normality is not joint normality'
- **normal_distribution** — light_tails: 'financial returns, network delays, and many natural quantities have heavier-than-Gaussian tails -- assuming normality drastically underestimates extreme-event probabilities (a 5-sigma event is ~1 in 3.5 million under normality, far more common in reality)'
- **null_set** — countability_of_the_union: 'an UNCOUNTABLE union of null sets need not be null: R = bigcup_{x} {x}, each {x} Lebesgue-null, but lambda(R) = inf'
- **pdf** — absolute_continuity: 'a discrete or singular X has no density; writing ''f_X'' for them is a type error'
- **pmf** — discreteness: 'for an absolutely continuous X, p_X(x) = P(X = x) = 0 everywhere -- the object that works is the pdf'
- **poisson_distribution** — equal_mean_and_variance: 'real count data with Var > mean (contagion, heterogeneity) is NOT Poisson -- fitting Poisson underestimates the variance and overstates significance'
- **poisson_limit_theorem** — n_p_n_converges_to_a_finite_positive_limit: 'if n p_n -> inf the correct limit is Gaussian (CLT); if n p_n -> 0 the count is 0 a.s. in the limit'
- **portmanteau_theorem** — boundedness_of_g: 'g(x) = x is continuous but unbounded: E[X_n] -> E[X] can FAIL under X_n -> X in distribution (escaping mass) -- convergence of unbounded moments needs uniform integrability'
- **portmanteau_theorem** — continuity_of_g: 'g = 1_{(-inf, 0]} (discontinuous): P(X_n <= 0) -> P(X <= 0) fails when X has an atom at 0'
- **preimage_algebra** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **probability_measure** — normalization: 'drop P(Omega)=1 and expectations are no longer weighted averages; Bayes'' normalizing constant is meaningless'
- **quantile_function** — u_in_open_interval: 'at u = 0 the inf is over all of R (gives -inf or the essential infimum); at u = 1 it may be +inf -- the transform is stated for u in (0,1)'
- **radon_nikodym** — absolute_continuity_nu_ll_mu: 'nu = delta_0, mu = Lebesgue on R: nu({0}) = 1 but mu({0}) = 0, so nu is NOT << mu and has no density; nu is purely singular'
- **random_variable** — measurability: 'an arbitrary function Omega -> R (e.g. the indicator of a non-measurable set) is not a random variable -- P(X in B) is undefined'
- **real_field** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **sequence_limit** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **series_convergence** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **set_algebra** — none_all_hypotheses_essential: 'every listed hypothesis is used in the proof; see the block above'
- **sigma_algebra** — countable_union_closure: 'the family of finite-or-cofinite subsets of N is closed under complement and FINITE unions but not countable ones -- it is an algebra, not a sigma-algebra'
- **slutsky_theorem** — the_limit_c_must_be_a_constant: 'X_n = Z ~ N(0,1) for all n (so X_n -> Z in distribution), Y_n = -Z (so Y_n -> -Z in distribution, NOT to a constant): X_n + Y_n = 0, not N(0,1) + N(0,1). Slutsky needs Y_n -> constant.'
- **standard_normal** — none_it_is_a_fixed_law: 'the standard normal is a single distribution; the caution is downstream (assuming data is standard normal after a bad standardization)'
- **stochastic_process** — one_common_probability_space: 'a process is more than its marginals -- the joint law across times (the dependence) is the object of interest'
- **stochastic_process** — path_regularity_is_extra: 'the f.d.d.s do not determine path properties; { X_t continuous } may not even be measurable without choosing a good modification'
- **strong_law_large_numbers** — finite_mean: 'X_i ~ Cauchy or any law with E|X_1| = inf: limsup |Xbar_n| = inf a.s. -- the sample mean has no a.s. limit and in fact oscillates unboundedly'
- **strong_law_large_numbers** — identically_distributed: 'Kolmogorov''s SLLN for independent non-identical X_i needs sum Var(X_i)/i^2 < inf'
- **tower_property** — integrability: 'needs X in L^1 for all the conditional expectations to exist'
- **transformation_univariate** — monotonicity_of_g: 'g(x) = x^2 with X ~ N(0,1): the naive one-branch formula gives half the correct density; Y ~ chi-squared_1 requires summing both preimages'
- **variance** — finite_second_moment: 'X ~ t-distribution with 2 degrees of freedom, or Cauchy: E[X^2] = inf, Var undefined -- the CLT fails, the sample variance does not stabilize'
- **variance_computational** — finite_second_moment: 'if E[X^2] = inf both sides are +inf (or ill-posed); the identity is only useful when Var is finite'
- **variance_of_sum** — uncorrelatedness_for_the_clean_form: 'X_1 ~ N(0,1), X_2 = -X_1: Var(X_1 + X_2) = Var(0) = 0, not Var(X_1) + Var(X_2) = 2 -- the -2 Cov term matters'
- **weak_law_large_numbers** — finite_mean: 'X_i ~ Cauchy: Xbar_n ~ Cauchy for every n -- no convergence to any constant (cauchy_no_mean)'
- **weak_law_large_numbers** — identically_distributed_ish: 'for non-identical independent X_i, Xbar_n -> the average of the means in probability provided (1/n^2) sum Var(X_i) -> 0'

