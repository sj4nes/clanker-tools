# joint_distribution

## Type
definition

## Statement
The joint distribution of X_1, ..., X_n is the law of the random vector X = (X_1, ..., X_n) on (R^n, B(R^n)): P_X(B) = P((X_1,...,X_n) in B). Described by the joint CDF, or a joint pmf / pdf.

## Symbols
- `X` — the random vector, type: Omega -> R^n
- `P_X` — the joint law, type: probability measure on B(R^n)

## Epistemic status
definition

## Prerequisites (tsort edges into this node)
borel_sigma_algebra, distribution_pushforward, random_variable

## Hypotheses
(none — unconditional within scope)

## Well-definedness
(X_1,...,X_n) is measurable iff each X_i is (B(R^n) is generated by the coordinate projections); the pushforward is a probability measure on B(R^n).

## Type / well-formedness check
the joint law carries strictly more information than the collection of marginal laws -- it also encodes the dependence structure (correlation, copula). Rectangles B_1 x ... x B_n form a pi-system generating B(R^n), so the joint CDF determines it.

## Specialization / boundary cases
- independent components: P_X = P_{X_1} tensor ... tensor P_{X_n}, joint density factorizes
- X_2 = X_1: the joint law is concentrated on the diagonal { x_1 = x_2 }, a lambda_2-null set (no joint density)

## Hypothesis-dropped counterexamples
- **none_the_marginals_do_not_determine_it**: (X,Y) and (X, X) can have the same marginals (both standard normal) but wildly different joint laws -- Cov 0 vs Cov 1

## Common misuse
- reconstructing the joint law from marginals (impossible without a dependence assumption)
- assuming a joint density exists (it does not when components are functionally related)

## Related nodes (non-prerequisite)
- projects_to: marginal_distribution
- used_by: independence_random_variables, conditional_distribution, covariance

## Sources
billingsley_probability_measure, durrett_pte
